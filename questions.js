/* QUESTION BANK */
window.QUESTION_BANK = [
/* ============================================================
   DOMAIN 1 — Agentic Architecture & Orchestration (27%)
   ============================================================ */

/* --- 1.1 Agentic loop --- */
{domain:"d1",task:"1.1 Agentic loop",scenario:"You are a backend engineer at a fintech startup. Your team built a data-reconciliation agent that calls database query tools, file-write tools, and notification tools in sequence. The agent has been running in production for two weeks and must handle both single-step and multi-step tasks reliably.",question:"You are implementing an agentic loop with the Claude Agent SDK. After each API response, what is the correct signal to determine whether the loop should continue executing tool calls?",options:["The value of <code>stop_reason</code> — continue when it is <code>\"tool_use\"</code>, terminate when it is <code>\"end_turn\"</code>","Parsing the assistant's response text for phrases like 'I'm done' or 'task complete' — natural language signals are non-deterministic and an explicit anti-pattern","An arbitrary maximum iteration count (e.g., 10 iterations) as the primary stopping criterion — a fixed cap used as the primary stop condition terminates tasks prematurely or wastes iterations","The status code returned by the most recent tool call — tool execution success does not indicate whether the model has finished reasoning"],correct:0,explanation:"stop_reason drives the loop: 'tool_use' means the model wants to call tools and continue; 'end_turn' means the model has finished. Parsing natural language for completion signals is an explicit anti-pattern. Using iteration caps as the primary stop mechanism truncates legitimate work. Tool return status reflects execution success, not model completion."},
{domain:"d1",task:"1.1 Agentic loop",scenario:"You are a backend engineer at a fintech startup. Your team built a data-reconciliation agent that calls database query tools, file-write tools, and notification tools in sequence. The agent has been running in production for two weeks and must handle both single-step and multi-step tasks reliably.",question:"After Claude returns a <code>tool_use</code> response and your code executes the requested tool, what must happen before the next API call so the model can reason about the next action?",options:["The tool result must be appended to the conversation history as a <code>tool_result</code> message before the next request","The previous turns should be summarized to save context space","The system prompt should be regenerated to include the new information","The tool result should be stored externally and referenced by ID in the next prompt"],correct:0,explanation:"Tool results must be appended to conversation history so the model can incorporate the new information into its next reasoning step. Summarizing prematurely, regenerating the system prompt, or storing results externally would deprive the model of the data it needs to continue the loop coherently."},
{domain:"d1",task:"1.1 Agentic loop",question:"A junior engineer's agent terminates whenever the assistant message contains the word 'finished'. The agent occasionally stops mid-task. What anti-pattern is this?",options:["Using natural language signals as a loop termination indicator","Failing to pass tool results back as a <code>tool_result</code> message, causing each iteration to lose prior context","Configuring <code>tool_choice: \"any\"</code> instead of <code>\"auto\"</code>, which forces the model to call tools even when finished","Not using <code>fork_session</code> for the agentic loop, which causes state to leak between iterations"],correct:0,explanation:"Parsing natural-language signals like 'finished' from assistant text is the anti-pattern — the model may use words like that mid-reasoning, not as a stop signal. The correct mechanism is <code>stop_reason: \"end_turn\"</code>. The distractors describe real errors but not the specific anti-pattern being demonstrated here."},
{domain:"d1",task:"1.1 Agentic loop",question:"Your DevOps team is automating incident response. One proposal uses a hardcoded runbook executor: detect alert → look up runbook → execute step 1 → execute step 2 → notify. A colleague proposes replacing it with a Claude-powered agentic loop. A junior engineer argues both call the same tools in roughly the same order, so the difference is cosmetic. What is the fundamental architectural distinction?",options:["In an agentic loop, the model reasons about which tool to call next based on accumulated context; a decision tree follows fixed, pre-configured tool sequences regardless of intermediate results","Both approaches are equivalent when tool lists and descriptions are identical — the selection mechanism (LLM vs. tree) doesn't affect tool execution outcomes","A hardcoded runbook is more reliable for compliance because every step is auditable and pre-approved; agentic loops introduce non-determinism that compliance teams must re-certify","The main difference is latency: agentic loops add LLM inference time to each step while decision trees execute tool calls directly without a reasoning pass"],correct:0,explanation:"The defining distinction is adaptive, context-informed decision-making. After each step the agentic loop uses all accumulated results to decide the next action — it may retry differently, skip steps, or branch based on what it found. A decision tree executes a fixed sequence regardless of intermediate results. Equivalence (B) ignores this fundamental difference. Reliability (C) conflates auditability with correctness. Latency (D) is an operational consideration, not the architectural distinction."},
{domain:"d1",task:"1.1 Agentic loop",question:"Your loop relies on a hard cap of 5 iterations as the primary stop mechanism. Legitimate multi-step tasks get cut off; simple tasks waste iterations. What is the right fix?",options:["Drive termination by <code>stop_reason == \"end_turn\"</code>; reserve a generous iteration cap only as a safety backstop","Increase the cap to 100 and add a <code>task_complete</code> tool the model calls when done, to combine both approaches","Have the model include a structured JSON field in its final message indicating completion, and parse that as the stop signal","Replace the iteration cap with a wall-clock timeout so long-running tools don't cause early termination"],correct:0,explanation:"The fix is to use <code>stop_reason: \"end_turn\"</code> as the primary signal and keep an iteration cap only as a safety backstop against runaway loops. B adds a <code>task_complete</code> tool but this is an anti-pattern — it still depends on a custom signal. C parses structured JSON from message content — still text parsing, still fragile. D substitutes one wrong stop mechanism for another."},
{domain:"d1",task:"1.1 Agentic loop",question:"A junior engineer joins your team and is confused by a Claude-powered pipeline log: Claude's response contains a <code>tool_use</code> block requesting <code>read_database(table='orders')</code>, but the engineer sees no database call in Claude's code. They ask: 'Who actually runs the query — does Claude do it internally?' How do you explain the execution model?",options:["Your application code runs the tool, then returns the result to Claude on the next request","Claude executes the tool through a sandboxed subprocess and returns the result in the same API response","The MCP server receives the tool call, executes it, and writes the result to Claude's context window directly","Tool execution is handled by the Anthropic backend when the model emits a <code>tool_use</code> block"],correct:0,explanation:"Tool execution always happens in your application code. Claude emits a <code>tool_use</code> block describing what to call — your code runs the database query, file read, or API call, then returns a <code>tool_result</code> in the next request. Claude, MCP servers, and the Anthropic backend never execute tools on your behalf. The confusion arises because Claude's description looks authoritative, but it is purely instructional."},
{domain:"d1",task:"1.1 Agentic loop",scenario:"You are a backend engineer at a fintech startup. Your team built a data-reconciliation agent that calls database query tools, file-write tools, and notification tools in sequence. The agent has been running in production for two weeks and must handle both single-step and multi-step tasks reliably.",question:"You receive a response with <code>stop_reason: \"end_turn\"</code> but the agent has not yet completed its multi-step task. What is most likely wrong?",options:["The model decided it had nothing more to do — review the system prompt and tool descriptions so it understands the full scope of the task","Override <code>stop_reason</code> by injecting a system prompt directive that prevents the model from sending <code>end_turn</code> prematurely","Inspect the last tool result — if it succeeded, the model is correct to stop; if it failed, re-invoke with error context","Add a validation step after each <code>end_turn</code> that checks whether all required output files exist, and re-prompt if not"],correct:0,explanation:"<code>end_turn</code> means the model believes it is done. The fix is in the inputs — clearer task descriptions, better tool descriptions, richer initial context. You cannot reliably override <code>stop_reason</code> via system prompt (B). Checking the last tool result (C) is partially useful but misses the root cause. Output file validation (D) is a compensating check, not a fix."},
{domain:"d1",task:"1.1 Agentic loop",question:"Which of these is the most reliable way for the model to communicate that it requires another iteration of tool execution?",options:["The <code>stop_reason</code> field set to <code>\"tool_use\"</code> with one or more <code>tool_use</code> blocks in the content","A <code>continue_loop: true</code> field in a structured JSON response, validated by your application before each iteration","The model emitting a special tool call like <code>request_next_iteration</code> that your application interprets as a loop continuation signal","The absence of a final summary message — if the assistant response contains no prose, the loop should continue"],correct:0,explanation:"<code>stop_reason: \"tool_use\"</code> with concrete <code>tool_use</code> blocks is the standardized signal. B and C are real patterns some engineers build, but they add a custom layer on top of — and can conflict with — the built-in stop_reason mechanism. D is inverted logic and would break on tool-use-only turns."},
{domain:"d1",task:"1.1 Agentic loop",question:"Your team is debating whether to add tool results to conversation history or store them externally and pass only summaries. Which approach preserves the model's ability to reason about prior tool outputs?",options:["Appending the actual tool result content (potentially trimmed) to conversation history so the model can re-read it on subsequent turns","Storing results in a vector database and injecting only the most relevant chunks via semantic retrieval before each turn","Passing only a one-sentence summary so the model has signal without consuming tokens","Storing results externally and passing their database IDs; the model can request full content via a <code>fetch_tool_result</code> tool when needed"],correct:0,explanation:"The model must have access to tool outputs in conversation history to reason about them. Trimming is acceptable, but full removal prevents reasoning. B (vector retrieval) is a valid advanced pattern but adds complexity and may miss context. C loses precision. D requires a second tool call for every retrieval, adds latency, and assumes the model knows which IDs to fetch."},
{domain:"d1",task:"1.1 Agentic loop",question:"During a post-mortem, your team reviews an agentic pipeline that sometimes completes a 12-step workflow perfectly but terminates at step 4 on identical inputs. Reviewing the code: <code>if iteration_count >= 5: break</code> is the only stop condition, with no handling of <code>stop_reason</code>. Senior engineers disagree on whether this is a bug or a reasonable safety limit. Which best describes the problem?",options:["Setting an arbitrary iteration cap as the primary stopping mechanism rather than as a safety net","Relying solely on <code>stop_reason: \"end_turn\"</code> — the model's judgment may differ from the task's actual completion","Executing tool calls in the order the model requests them rather than reordering for efficiency","Appending each tool result to the conversation history before the next API call, even for large payloads"],correct:0,explanation:"Using an iteration cap as the primary stop mechanism is an explicit anti-pattern: for simple tasks the cap is wasteful; for complex tasks it truncates legitimate work. The correct design is to drive termination by <code>stop_reason: 'end_turn'</code> and keep a generous cap only as a safety backstop. Option B describes the correct primary mechanism — not an anti-pattern. C is about execution ordering, orthogonal. D (appending tool results) is the correct pattern."},

/* --- 1.2 Multi-agent coordinator-subagent --- */
{domain:"d1",task:"1.2 Coordinator-subagent",question:"In a hub-and-spoke multi-agent system, which agent is responsible for inter-subagent communication, error handling, and information routing?",options:["The coordinator agent — subagents communicate only through the coordinator, not directly with each other","Each subagent communicates peer-to-peer with the others as needed","A shared message bus that no agent owns","The Claude API itself routes messages automatically"],correct:0,explanation:"Hub-and-spoke means all inter-subagent communication goes through the coordinator. This gives observability, consistent error handling, and controlled information flow. Peer-to-peer subagent communication is an anti-pattern."},
{domain:"d1",task:"1.2 Coordinator-subagent",scenario:"Your consulting firm's market intelligence platform uses a <strong>coordinator agent</strong> that decomposes research topics into sub-tasks and dispatches three specialized subagents: a <strong>WebSearch agent</strong>, a <strong>DocumentAnalysis agent</strong>, and a <strong>Synthesis agent</strong>. Subagents run independently and return structured results to the coordinator.",question:"You are running a multi-agent research system. The synthesis subagent reports it has 'no data to synthesize' despite the web search subagent completing successfully. The coordinator received a tool_result confirming the search completed. What is the most likely root cause?",options:["Subagents have isolated context — the coordinator did not explicitly pass the web search results to the synthesis subagent; they share no automatic context","The web search results were cleared from context when the synthesis subagent was initialized — results are retained in conversation history; they're not automatically cleared between subagent invocations","The synthesis subagent's system prompt is too long and crowded out the search results — system prompt size affects reasoning quality, but the search results would still be present in the tool_result message","The web search subagent hallucinated a success confirmation — the coordinator received a valid tool_result; the issue is in how results were passed to synthesis, not in the search result's authenticity"],correct:0,explanation:"Subagent context isolation is the cause — each subagent starts with only what the coordinator explicitly provides. The coordinator received the search results but didn't inject them into the synthesis subagent's prompt. Results aren't automatically cleared between invocations; they're just never shared unless explicitly passed. System prompt size and hallucination are plausible-sounding but don't explain the specific failure pattern."},
{domain:"d1",task:"1.2 Coordinator-subagent",scenario:"Your consulting firm's market intelligence platform uses a <strong>coordinator agent</strong> that decomposes research topics into sub-tasks and dispatches three specialized subagents: a <strong>WebSearch agent</strong>, a <strong>DocumentAnalysis agent</strong>, and a <strong>Synthesis agent</strong>. Subagents run independently and return structured results to the coordinator.",question:"After running a multi-agent research system on the topic 'impact of AI on creative industries,' your final reports cover only visual arts, completely missing music, writing, and film. The coordinator's logs show it decomposed the topic into: 'AI in digital art creation,' 'AI in graphic design,' and 'AI in photography.' What is the most likely root cause?",options:["The coordinator's task decomposition was too narrow, producing subagent assignments that didn't cover all relevant domains of the topic","The synthesis agent filtered out non-visual creative industries","The web search agent's queries were not comprehensive enough","The document analysis agent applied overly restrictive relevance criteria"],correct:0,explanation:"This is question 7 from the official guide. The coordinator's decomposition is the root cause — subagents executed correctly within the scope they were given. Blaming downstream agents misses that they did exactly what was asked."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"A competitor-intelligence coordinator spawns five subagents to research different rivals. After all five complete, their reports heavily overlap: three subagents researched the same two competitors, tripling API costs and slowing synthesis. Your manager wants the architectural fix before the next quarterly run. What is the recommended change?",options:["The coordinator partitions the research scope across subagents (e.g., distinct competitors or source types) so each agent's work doesn't overlap","Run all subagents with a shared system prompt that instructs them not to repeat findings already reported by other subagents","After all subagents complete, run a deduplication post-processing step on the combined output","Assign one subagent as a gatekeeper that reviews and filters results from the others before they reach the coordinator"],correct:0,explanation:"Upfront scope partitioning prevents duplication before it occurs — each subagent is assigned a distinct competitor or source domain, so overlap is architecturally impossible. B sounds appealing but subagents don't see each other's work in real-time, so the instruction is unenforceable. C deduplicates after the fact but wastes compute and API budget on duplicate research. D introduces peer-gating — an anti-pattern where subagents depend on each other's approval."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"Your coordinator agent always routes every query through the full pipeline (search → analyze → synthesize → report), even for simple lookups. What pattern would make the system more efficient?",options:["Design the coordinator to analyze query requirements and dynamically select which subagents to invoke","Hard-code the pipeline order in code so it cannot be skipped","Replace subagents with a single monolithic agent","Remove the synthesis agent for simple cases"],correct:0,explanation:"Dynamic subagent selection is the recommended pattern. The coordinator should analyze the query and route only through subagents that are actually needed."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"Why is iterative refinement (coordinator evaluates synthesis output → re-delegates with targeted feedback) superior to a single-pass synthesis?",options:["A single pass cannot self-correct for missing coverage; iterative refinement lets the coordinator identify gaps and direct subagents to fill them before finalizing the output","Iterative refinement is always cheaper per token — iterative loops typically cost more per output because each iteration requires additional API calls and synthesis passes","Iterative refinement eliminates the need for specialized subagents — the opposite is true; it relies on subagents and makes them more effective by allowing targeted follow-up","Single-pass pipelines cannot support tool use — tool use is available in any pipeline architecture; the limitation is not tool availability but the inability to incorporate feedback"],correct:0,explanation:"A single synthesis pass produces what it produces — if coverage is incomplete or a topic is shallow, there's no mechanism to correct it. Iterative refinement lets the coordinator evaluate the output and direct targeted follow-up: 'section A lacks recent data, re-research X.' Cost, subagent necessity, and tool use support are all incorrect claims."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"Two subagents in your system communicate directly with each other to share intermediate findings. What problem does this create?",options:["It bypasses the coordinator, losing observability, consistent error handling, and controlled information flow","Direct subagent communication requires both agents to share a context window, doubling memory usage","The Task tool does not support bidirectional communication, so peer-to-peer calls will silently fail","Direct communication works reliably only if both subagents use the same model version"],correct:0,explanation:"Peer-to-peer subagent communication bypasses the coordinator, undermining the architectural properties of hub-and-spoke: observability, error handling, and controlled information flow. B, C, D describe fabricated technical constraints."},
{domain:"d1",task:"1.2 Coordinator-subagent",scenario:"Your consulting firm's market intelligence platform uses a <strong>coordinator agent</strong> that decomposes research topics into sub-tasks and dispatches three specialized subagents: a <strong>WebSearch agent</strong>, a <strong>DocumentAnalysis agent</strong>, and a <strong>Synthesis agent</strong>. Subagents run independently and return structured results to the coordinator.",question:"In a hub-and-spoke pattern, the coordinator's primary responsibilities include all of the following EXCEPT:",options:["Inheriting context automatically from each subagent at the end of their work","Partitioning the research scope to minimize duplication across subagents","Iteratively refining subagent results by re-delegating to fill coverage gaps","Synthesizing findings from multiple subagents into a coherent output"],correct:0,explanation:"There is no automatic context inheritance — subagents must return results explicitly to the coordinator. B, C, and D are all genuine coordinator responsibilities in a well-designed hub-and-spoke system."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"Your multi-agent system researches 'the regulatory landscape for autonomous vehicles in 2024.' The coordinator decomposes this into federal FMVSS updates, NHTSA enforcement actions, and SAE Level 3 liability rulings. All three subagents execute successfully. The synthesis report is thorough — but your legal team immediately flags that it completely omits state-level regulations (California, Texas, New York), which are the most active regulatory arena. What is the root cause?",options:["Overly narrow decomposition that produces subagent assignments which fail to cover all relevant aspects of a broad topic","The synthesis agent filtered out state-level regulatory content as outside its scope","The web search subagent's queries were too specific and missed broader regulatory literature","Subagents returned findings in incompatible formats that the synthesis agent couldn't reconcile"],correct:0,explanation:"The coordinator's decomposition is the root cause — all three subagents executed correctly within the federal scope they were given. State-level regulations were never in any subagent's assignment. Blaming synthesis (B) ignores that it correctly combined what it received. Blaming search queries (C) or format incompatibility (D) misattributes the gap: the problem is what was asked, not how it was researched."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"You design coordinator prompts that specify research goals and quality criteria, not step-by-step procedural instructions. Why?",options:["Goals-and-criteria prompts let subagents adapt their approach to what they discover; step-by-step instructions prevent useful adaptation","Step-by-step procedural instructions produce deterministic subagent behavior, but goals-and-criteria are required when using the <code>Task</code> tool","Procedural instructions work well for structured data tasks but create context overflow for open-ended research questions","Goals-and-criteria prompts allow the coordinator to evaluate subagent outputs against measurable thresholds, which step-by-step instructions don't support"],correct:0,explanation:"Goals-and-criteria prompts allow subagents to adapt their approach based on what they discover — something procedural instructions prevent. B incorrectly claims the Task tool requires this format. C is partially true but not the core reason. D confuses output evaluation (coordinator's job) with subagent prompt structure."},

/* --- 1.3 Subagent invocation, context passing --- */
{domain:"d1",task:"1.3 Subagent invocation",question:"You configured a coordinator agent that needs to spawn subagents, but the coordinator cannot invoke them. What is most likely missing?",options:["The coordinator's <code>allowedTools</code> does not include <code>\"Task\"</code>","The <code>AgentDefinition</code> objects for each subagent type have not been registered with the coordinator's runtime","The coordinator's system prompt does not reference the subagent names, so it does not know they exist","The coordinator's <code>max_tokens</code> budget is too low to fit a <code>Task</code> tool call plus the subagent prompt"],correct:0,explanation:"The <code>Task</code> tool must be listed in the coordinator's <code>allowedTools</code> for it to spawn subagents. B (AgentDefinitions) must be configured but that is a secondary requirement — the tool still needs to be in the allow-list. C is not how the system works — subagent names don't need to be in the system prompt. D (token budget) is irrelevant to the capability being missing."},
{domain:"d1",task:"1.3 Subagent invocation",question:"To explore two different refactoring strategies from a shared codebase analysis baseline, which Agent SDK feature should you use?",options:["<code>fork_session</code> — creates independent branches from a shared analysis baseline","<code>--resume</code> — resumes a single named session","<code>Task</code> tool — only for spawning new subagents","<code>/compact</code> — only for reducing context"],correct:0,explanation:"<code>fork_session</code> is purpose-built for exploring divergent approaches from a shared baseline. Each branch is independent but shares the same starting point."},
{domain:"d1",task:"1.3 Subagent invocation",question:"You want a coordinator to dispatch parallel subagents to research three independent subtopics simultaneously. What's the right pattern?",options:["Emit multiple <code>Task</code> tool calls in a single coordinator response, rather than across separate turns","Emit one <code>Task</code> tool call per turn for three consecutive turns — the Agent SDK batches them for parallel execution","Use <code>fork_session</code> three times from the coordinator, passing each fork a different subtopic assignment","Invoke the coordinator three times with different subtopic prompts from your application code, then merge results"],correct:0,explanation:"Parallel subagents require multiple <code>Task</code> tool calls in a single coordinator response. B is wrong — sequential turns execute sequentially. C uses <code>fork_session</code>, which is for branching from a baseline, not for parallel dispatch. D bypasses the coordinator architecture entirely."},
{domain:"d1",task:"1.3 Subagent invocation",question:"A subagent's prompt receives a list of web search findings. To preserve source attribution through downstream synthesis, you should structure the data so:",options:["Content and metadata (source URLs, document names, page numbers) are separated and explicitly labeled","All findings are concatenated into a single string with no markers","Sources are stripped to save tokens; only claims are kept","Metadata is sent in a separate API request"],correct:0,explanation:"Structured separation of content from metadata is essential for downstream agents to preserve attribution. Concatenation loses traceability."},
{domain:"d1",task:"1.3 Subagent invocation",question:"You are designing a multi-agent research system with three specialized subagent types: a WebResearchAgent, a DataAnalysisAgent, and a CitationAgent. Each type needs a different system prompt, a different tool allow-list, and a description so the coordinator knows when to invoke it. Which SDK construct holds this per-type configuration?",options:["The <code>AgentDefinition</code> object — it specifies the description, system prompt, and tool restrictions for a specific subagent type","The <code>tool_choice</code> parameter on the coordinator's API call — setting it to the subagent's name routes invocations to the correct definition","The MCP server configuration — each subagent type is registered as a separate MCP server with its own tool schema","The <code>allowedTools</code> list on the coordinator — listing a subagent name alongside regular tools registers it as an invocable agent type"],correct:0,explanation:"<code>AgentDefinition</code> is the SDK object that holds per-subagent-type configuration: its description (used by the coordinator to decide when to invoke it), its system prompt (specialized instructions), and its tool restrictions (least-privilege access). <code>tool_choice</code> controls which tool to call on a single request, not agent definitions. MCP servers provide tools to agents but don't define agent types. <code>allowedTools</code> controls tool access, not subagent type configuration."},
{domain:"d1",task:"1.3 Subagent invocation",question:"Your synthesis subagent receives a prompt that references 'the findings' but contains no actual findings. What is the bug?",options:["The coordinator failed to inline the prior agents' findings directly into the synthesis subagent's prompt","The synthesis subagent's <code>AgentDefinition</code> does not include a <code>receive_findings</code> tool that the search subagent could write to","The search subagent's results were too large and were auto-truncated before reaching the synthesis subagent's context window","The synthesis subagent was spawned before the search subagent completed, causing it to receive an empty context"],correct:0,explanation:"Subagents have isolated context — they receive only what is explicitly passed in their prompt. The coordinator must inline the findings. B imagines a shared-write tool that doesn't exist. C (auto-truncation) doesn't happen silently. D (timing) would be a scheduling bug, but the SDK waits for completion before returning results."},
{domain:"d1",task:"1.3 Subagent invocation",question:"A user keeps asking 'what did you find earlier?' after subagents return their work. Where should you persist findings so the coordinator can reference them later?",options:["In the coordinator's own conversation context (e.g., scratchpad files or in-message summaries) — subagents' internal context is not shared","In each subagent's <code>AgentDefinition</code> description field — the coordinator reads it before spawning follow-up subagents","In a shared MCP server that all agents can read and write, enabling persistent cross-agent state","In the coordinator's <code>allowedTools</code> list as tool metadata, so the model can reference it via tool names"],correct:0,explanation:"There is no built-in shared memory. The coordinator must capture findings into its own conversation context — a scratchpad file or summary block. B (AgentDefinition descriptions) are static configuration, not dynamic results. C (shared MCP server) could work architecturally but adds complexity; the recommended approach is coordinator-side state. D is not how tool metadata works."},
{domain:"d1",task:"1.3 Subagent invocation",question:"Why is it preferable to pass complete findings into a subagent's prompt rather than relying on parent context inheritance?",options:["Subagent context is isolated; no inheritance exists, so anything the subagent needs must be passed explicitly","Parent context is available to subagents in read-only mode, but writing findings back requires explicit return values","The coordinator can pass findings as a shared <code>context_id</code> reference, but subagents need explicit findings for extraction tasks","Inheritance works for the coordinator's system prompt but not for dynamic findings accumulated during the session"],correct:0,explanation:"There is no automatic parent-to-subagent context inheritance — subagent context is completely isolated. Everything the subagent needs must be passed explicitly in its prompt. B, C, and D describe partial-inheritance models that don't exist — all assume some form of context sharing that the Agent SDK simply doesn't provide."},
{domain:"d1",task:"1.3 Subagent invocation",question:"You want subagents to handle ambiguous research questions adaptively rather than mechanically follow instructions. How should you phrase coordinator prompts?",options:["Specify research goals and quality criteria, leaving the subagent room to adapt its method","Provide step-by-step procedural instructions for every possible sub-case","Have the coordinator generate a deterministic plan first, then dispatch","Use <code>tool_choice: \"any\"</code> for every subagent invocation"],correct:0,explanation:"Goals-and-criteria prompts enable subagent adaptability; rigid procedural instructions undermine it."},
{domain:"d1",task:"1.3 Subagent invocation",question:"Your team completed a 40-minute codebase analysis that consumed a large context window. Now you need to evaluate two refactoring strategies: Strategy A extracts shared logic into a new module; Strategy B rewrites the module boundaries entirely. Both strategies must start from the same analysis baseline. Re-running the analysis twice would cost another 80 minutes. What Agent SDK feature is designed for this?",options:["<code>fork_session</code> — creates independent branches from the shared analysis baseline so both strategies diverge from the same starting point","<code>--resume &lt;session-name&gt;</code> — resumes the single session and evaluates both strategies sequentially within it","Two separate <code>Task</code> tool calls from the coordinator — these automatically share the parent's context window","<code>/compact</code> the analysis session first, then run both strategies against the compacted summary"],correct:0,explanation:"<code>fork_session</code> is purpose-built for this: two independent branches from the same baseline, each strategy diverging without affecting the other. <code>--resume</code> continues a single session sequentially — you'd evaluate one strategy at a time. Separate <code>Task</code> calls don't share the parent's context (subagent context is isolated). Compacting first loses precision; fork directly from the full analysis."},

/* --- 1.4 Multi-step workflows, enforcement, handoffs --- */
{domain:"d1",task:"1.4 Enforcement & handoffs",scenario:"You are architecting a customer-support agent for a retail bank. The agent can query account data, look up orders, and — when authorized — process refunds and fund transfers. Your compliance team requires that customer identity is verified before any financial operation executes.",question:"Production data shows that in 12% of cases, your customer support agent skips <code>get_customer</code> entirely and calls <code>lookup_order</code> using only the customer's stated name, occasionally leading to misidentified accounts and incorrect refunds. What change would most effectively address this reliability issue?",options:["Add a programmatic prerequisite that blocks <code>lookup_order</code> and <code>process_refund</code> calls until <code>get_customer</code> has returned a verified customer ID","Enhance the system prompt to state that customer verification via <code>get_customer</code> is mandatory before any order operations","Add few-shot examples showing the agent always calling <code>get_customer</code> first, even when customers volunteer order details","Implement a routing classifier that analyzes each request and enables only the subset of tools appropriate for that request type"],correct:0,explanation:"This is question 1 from the official guide. When a specific tool sequence is required for critical business logic (like verifying customer identity before processing refunds), programmatic enforcement provides deterministic guarantees. Prompt-based approaches (B and C) are probabilistic. Option D addresses tool availability, not ordering."},
{domain:"d1",task:"1.4 Enforcement & handoffs",scenario:"You are architecting a customer-support agent for a retail bank. The agent can query account data, look up orders, and — when authorized — process refunds and fund transfers. Your compliance team requires that customer identity is verified before any financial operation executes.",question:"For a workflow with financial consequences (e.g., refunds above $500 require human approval), which approach gives deterministic compliance?",options:["A programmatic hook that intercepts the tool call and blocks/escalates based on the amount","A system prompt instruction with explicit threshold details: 'Refunds above $500 MUST be escalated to human review before processing'","Few-shot examples in the system prompt demonstrating the correct escalation behavior for threshold-crossing refunds","A validation tool the model calls before processing any refund, which returns an approval/denial decision based on the amount"],correct:0,explanation:"Programmatic hooks intercept tool calls before execution, providing deterministic guarantees. B is prompt-based — emphatic but probabilistic. C (few-shot) improves the model's tendency to escalate but still has a non-zero failure rate. D adds a validation step but the model can still skip calling it — it's advisory, not intercepting."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"You need to escalate a complex case to a human agent who will not have access to the conversation transcript. The handoff summary should include:",options:["Customer ID, root cause analysis, refund amount, and recommended action","The entire conversation transcript verbatim","Only the customer's original complaint","A link to the agent's internal scratchpad"],correct:0,explanation:"Structured handoff summaries include the data the human needs to act: customer ID, root cause, amounts, and a recommended action. Dumping a transcript or sending only the complaint forces the human to redo the work."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"A customer message contains multiple unrelated concerns (billing dispute + shipping question + account update). What's the recommended approach?",options:["Decompose the message into distinct items and investigate each in parallel using shared context, then synthesize a unified resolution","Address only the first concern; ask the customer to resend the rest","Escalate the entire message to a human","Pick the one concern with the highest priority and ignore the rest"],correct:0,explanation:"Multi-concern requests should be decomposed and processed in parallel using shared context, then synthesized. This is more efficient and more thorough than sequential single-concern handling."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"Why is prompt-based guidance insufficient when compliance is required (e.g., identity verification before financial operations)?",options:["Prompt instructions have a non-zero failure rate; deterministic compliance requires programmatic mechanisms","Prompt instructions are evaluated at generation time, not at execution time, so a mid-session context shift can cause them to be ignored","Prompt-based rules are not enforceable across multi-agent pipelines where subagents don't see the coordinator's system prompt","Prompts have a non-zero probability of misinterpretation when the instruction conflicts with a user turn earlier in the conversation"],correct:0,explanation:"Prompt instructions have a non-zero failure rate — the model's output is probabilistic by nature. When financial penalties, legal liability, or safety depend on a rule never being violated, deterministic enforcement via programmatic hooks is required. B contains a partial truth about timing but mischaracterizes how system prompts work. C is a real multi-agent concern but not the primary reason. D is plausible but secondary."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"You want to block <code>process_refund</code> until <code>get_customer</code> has returned a verified ID. Where does this logic belong?",options:["In a programmatic prerequisite gate that checks state before allowing <code>process_refund</code>","In the system prompt as a 'never call X before Y' rule so the model sees the constraint on every turn","In a validation tool the model calls before <code>process_refund</code>, which checks whether a verified customer ID is stored in session state","In a follow-up review pass that audits tool call sequences after execution and flags violations for human correction"],correct:0,explanation:"Programmatic gates enforce ordering reliably. System prompt rules can be bypassed by the model on edge cases."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"Which of the following is NOT a recommended element of a structured handoff to a human agent?",options:["The agent's internal reasoning trace from every prior turn","Customer ID and account number to identify the affected record","Root cause analysis of why the agent could not resolve the case","Recommended next action for the human agent to take"],correct:0,explanation:"The full internal reasoning trace from every prior turn is not a recommended handoff element — it's verbose, unstructured, and forces the human to re-trace the agent's work. The recommended elements are structured: customer ID, root cause, the amount or decision in question, and a recommended action. B, C, and D are all genuine handoff components."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"Your agent achieves 55% first-contact resolution — well below the 80% target. Logs show it escalates straightforward cases (e.g., standard damage replacements with photo evidence) while attempting to autonomously handle complex situations requiring policy exceptions. What's the most effective way to improve escalation calibration?",options:["Add explicit escalation criteria to the system prompt with few-shot examples demonstrating when to escalate versus resolve autonomously","Have the agent self-report a confidence score (1-10) before each response and route to humans when confidence falls below a threshold","Deploy a separate classifier model trained on historical tickets to predict escalation","Implement sentiment analysis to detect customer frustration and auto-escalate"],correct:0,explanation:"This is question 3 from the official guide. Explicit criteria with few-shot examples directly addresses the root cause: unclear decision boundaries. LLM self-reported confidence is poorly calibrated, classifier-based routing is over-engineered, and sentiment doesn't correlate with case complexity."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"For a workflow with strict ordering requirements (e.g., 'always preview before execute'), which design is most reliable?",options:["Two separate tools — <code>preview</code> returns a single-use token; <code>execute</code> requires that token","One tool with a <code>preview: boolean</code> flag","A system prompt rule stating 'always preview first'","Few-shot examples that demonstrate preview-first behavior"],correct:0,explanation:"Two tools where <code>execute</code> requires a token from <code>preview</code> makes the unsafe path structurally impossible. Flags and prompt rules can be bypassed."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"A frustrated customer requests a human agent immediately. The issue is straightforward — within policy and solvable by the AI agent. What is the right escalation response?",options:["Acknowledge the customer's frustration directly, offer to resolve the issue since it's within policy and won't require waiting for a human, and escalate only if the customer still insists after being offered resolution","Silently process the resolution without acknowledging the human-agent request — resolves the transaction but ignores the stated preference, which can further escalate frustration and damage the customer relationship","Immediately transfer to a human agent without attempting resolution — honors the explicit request but wastes human agent capacity on a case the AI could handle, increasing queue times for complex cases","Acknowledge the frustration, complete the transaction, and then send a satisfaction survey asking the customer to rate whether they wished they'd spoken to a human — defers resolution acknowledgment to a post-interaction survey rather than addressing it in the moment"],correct:0,explanation:"The correct response acknowledges frustration (validates the customer's experience), offers immediate resolution (avoids the wait for a human), and escalates only if the customer still wants a human after hearing that the AI can solve it now. Silent processing ignores the customer's emotional state. Immediate transfer wastes capacity. Post-interaction surveys defer what should be handled in the moment."},


/* --- 1.5 Hooks --- */
{domain:"d1",task:"1.5 Agent SDK hooks",question:"You have multiple MCP tools returning timestamps in different formats (Unix epoch, ISO 8601, custom). The agent makes incorrect date calculations. What pattern fixes this without modifying each MCP tool?",options:["Implement a <code>PostToolUse</code> hook that normalizes timestamps to a single format before the model processes the tool result","Add detailed format instructions in the system prompt","Use few-shot examples of date conversions","Force the model to call a separate date_normalize tool after each lookup"],correct:0,explanation:"<code>PostToolUse</code> hooks intercept tool results and transform them before the model sees them. This is the deterministic, low-overhead fix for heterogeneous formats."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"Your compliance team discovers that your loan approval agent violated a regulatory rule 3 times last quarter: it approved loans for applicants who failed the KYC check, despite a system prompt instruction that reads 'NEVER approve a loan if <code>kyc_status</code> is FAILED.' Your team debates whether to strengthen the prompt or implement a hook. What is the key distinction that determines which approach provides genuine compliance guarantees?",options:["Hooks provide deterministic guarantees; prompt instructions provide probabilistic compliance","Hooks run synchronously within the agentic loop; prompt instructions run asynchronously in the model's generation process","Prompt-based enforcement degrades gracefully under high load; hooks can block the loop if the interceptor fails","Hooks can modify tool results post-execution; prompt instructions can only constrain the initial tool selection"],correct:0,explanation:"The critical distinction is determinism vs. probability. Even an emphatic 'NEVER' prompt instruction has a non-zero failure rate under adversarial inputs, context pressure, or model edge cases. A hook fires deterministically at execution time: if KYC is FAILED, the hook blocks the approval unconditionally, regardless of what the model decided. For regulatory compliance, only deterministic guarantees are acceptable. B incorrectly contrasts sync/async. C describes operational concerns, not the core distinction. D is partially true but secondary."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"You want to block any outgoing <code>process_refund</code> call exceeding $500 and redirect to a human escalation workflow. Which hook pattern fits?",options:["A tool call interception hook that inspects parameters before execution and either blocks or rewrites the call","A <code>PostToolUse</code> hook on <code>process_refund</code> that detects over-threshold results and triggers a compensating reversal","A pre-call hook that logs the refund parameters and pauses execution until a human confirms via a separate approval channel","A router tool the model invokes before <code>process_refund</code>, which checks the amount and returns an approval token required to proceed"],correct:0,explanation:"A pre-call interception hook is the right choice — it blocks the tool call before execution, allowing redirect to human escalation. B is a PostToolUse hook — it runs after the refund is already processed, so it can reverse but not prevent. C is close but 'pauses execution until human confirms' describes a pre-call hook combined with an async approval flow — valid architecturally but the key distinction from B is timing. D is a voluntary validation tool that the model can skip."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"Your team is architecting a healthcare scheduling agent that can book procedures, override waiting lists, and flag cases as urgent. Legal has identified three rules: clinician review token for urgent flags, supervisor approval for wait-list overrides, guardian consent for minor bookings. A senior engineer argues these should be system prompt rules since they're 'clearly stated.' When are hooks definitively required instead?",options:["Whenever business rules require guaranteed compliance — financial, legal, or safety-critical constraints cannot tolerate a non-zero failure rate from probabilistic prompt enforcement","Only after prompt-based enforcement has failed in production at least once — hooks are a remediation tool, not a proactive safeguard, and should be added only after observed violations","Exclusively for post-execution validation, not pre-execution blocking — hooks run after the fact to detect violations; pre-execution enforcement belongs in the prompt","Primarily when compliance rules are too complex for the model to evaluate correctly — simple binary rules like 'token required' can always be enforced reliably via prompt alone"],correct:0,explanation:"Hooks are required from the start whenever rules demand guaranteed compliance — the exam guide is explicit: don't wait for failures before switching to deterministic enforcement. Treating hooks as post-failure remediation (B) is backwards. Hooks can run pre-execution for blocking (C). Simple rules still require hooks if compliance-critical (D)."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"A multi-tool research agent queries four databases, each returning timestamps in a different format: Unix epoch, ISO 8601, MM/DD/YYYY, and locale-specific strings. The model makes incorrect date comparisons. You want to fix this at the pipeline level without modifying any of the four database tools. Which hook type and purpose solves this?",options:["A <code>PostToolUse</code> hook that intercepts each tool result and normalizes all timestamps to ISO 8601 before the model processes them","A pre-execution hook that rewrites tool input parameters to include a <code>date_format: 'ISO8601'</code> field before dispatch","A system prompt instruction listing all four format patterns and instructing Claude to convert them before performing calculations","A separate <code>normalize_dates</code> tool the model must call after each database query"],correct:0,explanation:"<code>PostToolUse</code> hooks run after a tool returns and before the model processes the result — ideal for format normalization, PII redaction, or enrichment. Pre-execution parameter injection (B) doesn't work because the tools don't accept a date_format parameter. System prompt normalization instructions (C) require the model to reliably apply multi-step reasoning on every turn — probabilistic. A secondary normalization tool (D) the model can skip adds latency and coupling."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"Your e-commerce agent can call <code>apply_discount</code>, <code>process_refund</code>, <code>cancel_order</code>, and <code>update_shipping</code>. Business policy requires that refunds exceeding $200 must be approved by a supervisor before processing. In the last month, two refunds above $200 were processed without supervisor approval — the agent correctly called <code>process_refund</code>, but no approval check occurred. Which hook pattern deterministically fixes this?",options:["A pre-execution interception hook that inspects <code>process_refund</code> parameters before execution and blocks or escalates if the amount exceeds $200","A <code>PostToolUse</code> hook on <code>process_refund</code> that detects over-threshold refunds and triggers a compensating reversal","A system prompt rule: 'Refunds exceeding $200 MUST be escalated to a supervisor before calling <code>process_refund</code>'","A validation tool the model calls before <code>process_refund</code>, which returns an approval token required to proceed"],correct:0,explanation:"Pre-execution interception blocks the call before it executes — the only approach that deterministically prevents an unauthorized refund. PostToolUse reversal (B) fires after the refund already processed, creating an undo that may fail and leaves regulatory exposure at execution time. System prompt rules (C) already failed twice — prompt compliance is probabilistic. A voluntary validation tool (D) the model can skip is not enforced at the execution layer."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"Your team relies on a system prompt rule to enforce that all customer-facing refunds are escalated above a threshold. After a year of operation, you discover 3 violations. The right architectural change is:",options:["Replace the prompt rule with a programmatic interception hook so compliance is deterministic","Add real-time monitoring that alerts when the prompt rule is bypassed and auto-reverts violations after the fact","Use a more capable model — larger models have higher instruction-following rates and are less likely to bypass the rule","Wrap the refund tool in a validation wrapper that logs each call, enabling you to audit violations and improve the prompt iteratively"],correct:0,explanation:"The fix is a programmatic interception hook — not prompt adjustments, monitoring, or model upgrades. B (post-hoc monitoring) detects violations after they occur, which is too late for compliance. C (larger model) improves compliance probability but not to 100% — still probabilistic. D (audit logging) is valuable but doesn't prevent violations."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"You are reviewing the hook architecture for a financial analytics agent that calls six market data tools. Two cross-cutting concerns need addressing: (1) tools return monetary amounts in inconsistent currency formats causing calculation errors; (2) one tool can execute trades above a regulatory threshold without authorization. Your teammate suggests using the same hook type for both. Why is that wrong?",options:["Pre-call interception handles policy enforcement (blocking before execution); <code>PostToolUse</code> handles normalization (transforming results after execution) — they serve different purposes at different points","<code>PostToolUse</code> handles policy enforcement; pre-call interception handles data normalization","Pre-call interception handles both — all policy and normalization logic should be applied before tool execution to minimize side effects","Neither — policy belongs in the system prompt and normalization belongs in the tool implementation itself"],correct:0,explanation:"Pre-call interception is for policy enforcement: it runs before execution and can block a call that violates a rule. PostToolUse is for data normalization: it runs after execution and transforms the result before the model processes it. Using PostToolUse for compliance is wrong because the trade already executed when the hook fires. Using pre-call interception for normalization is impossible because the result doesn't exist yet. D is the pattern that produced the original compliance violations."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"You build a hook to redirect blocked refund attempts to a human escalation workflow. What additional element should the hook return so the agent communicates appropriately?",options:["A structured error or rerouted result with a customer-friendly explanation and the escalation path","An exception that terminates the agentic loop cleanly, preventing any further tool calls until the human resolves the escalation","A modified tool_use block re-routing the call to a <code>human_escalation</code> tool, so the model continues its loop with the escalation as a completed tool call","An empty tool result with <code>isError: true</code>, prompting the model to retry with different parameters"],correct:0,explanation:"The hook should return a structured result with a customer-friendly message and status indicating the escalation was created. This gives the model what it needs to communicate the situation clearly. B (exception/loop termination) is too abrupt and loses context. C (rerouting as a tool_use block) is architecturally interesting but not how hooks return results. D (isError: true with empty result) causes a retry loop."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"An audit of your healthcare scheduling agent finds that over 18 months, 7 out of 140,000 procedure bookings violated the guardian consent rule — a system prompt instruction. Your VP of Engineering argues 99.995% compliance is 'effectively perfect.' What is the strongest argument for hooks?",options:["Hooks fire deterministically every time — prompt instructions have a non-zero failure rate that is unacceptable when each violation represents a patient safety or legal incident","Eliminating the compliance instruction from the system prompt reduces per-request token costs — a secondary benefit, not the binding argument for why determinism is required","Separating compliance enforcement from the system prompt makes it easier to audit and update independently — true, but maintainability is an optimization argument, not a correctness guarantee","Scoping enforcement to specific tools simplifies the system prompt and reduces the chance of the instruction being overridden by persona guidance — valid concern, but still secondary to the determinism requirement"],correct:0,explanation:"Determinism is the binding argument. In healthcare, each of 7 violations represents a potential patient harm or lawsuit — 99.995% is not acceptable. The other options describe real benefits of hooks (cost reduction, auditability, scoping) but they're operational improvements, not the core reason hooks are required for safety-critical compliance."},

/* --- 1.6 Task decomposition --- */
{domain:"d1",task:"1.6 Task decomposition",question:"For a code review that always checks style → security → documentation in the same order, which decomposition pattern fits best?",options:["Prompt chaining — a fixed sequential pipeline","Dynamic decomposition based on intermediate findings","Routing","Orchestrator-workers"],correct:0,explanation:"When the steps are known and stable, prompt chaining is the simplest and cheapest pattern. Dynamic decomposition is overkill for predictable workflows."},
{domain:"d1",task:"1.6 Task decomposition",question:"For an open-ended task like 'add comprehensive tests to a legacy codebase', dynamic decomposition is appropriate because:",options:["First mapping structure identifies high-impact, high-risk modules — without this, test generation proceeds alphabetically or arbitrarily, missing the areas that need coverage most","Pre-defining every test case before any analysis works well when the codebase has complete documentation and a flat module structure — but a legacy codebase with no docs requires exploration before planning","Generating tests file by file alphabetically can succeed if each file has clear docstrings explaining its purpose — but 'legacy codebase' implies missing documentation, making alphabetical generation unreliable","A fixed three-step pipeline (lint → test → commit) provides enough structure for any codebase — but a pipeline without an exploration phase will commit tests for low-priority files while leaving critical modules untested"],correct:0,explanation:"Dynamic decomposition is needed here because the high-impact modules are unknown until the codebase is explored — payment and fraud detection only get discovered as critical through analysis, not alphabetical ordering. Pre-defining cases requires documentation that legacy codebases typically lack. Fixed pipelines commit to an order before knowing what's important."},
{domain:"d1",task:"1.6 Task decomposition",question:"A pull request modifies 14 files across the stock tracking module. Your single-pass review analyzing all files together produces inconsistent results: detailed feedback for some files but superficial comments for others, obvious bugs missed, and contradictory feedback — flagging a pattern as problematic in one file while approving identical code elsewhere in the same PR. How should you restructure the review?",options:["Split into focused passes: analyze each file individually for local issues, then run a separate integration-focused pass examining cross-file data flow","Require developers to split large PRs into smaller submissions of 3-4 files before the automated review runs","Switch to a higher-tier model with a larger context window to give all 14 files adequate attention in one pass","Run three independent review passes on the full PR and only flag issues that appear in at least two of the three runs"],correct:0,explanation:"This is question 12 from the official guide. Splitting reviews into focused passes addresses attention dilution. A larger context window doesn't solve attention quality. Requiring smaller PRs shifts burden to developers. Triple-pass voting suppresses real bugs caught intermittently."},
{domain:"d1",task:"1.6 Task decomposition",question:"Your team runs daily code reviews on every pull request through the same three checks in fixed order: (1) style and formatting, (2) security vulnerability scan, (3) documentation completeness. The checks are independent of each other and the order never changes. Which task decomposition pattern fits this pipeline best?",options:["Prompt chaining — a fixed sequential pipeline where each check is a focused pass","Routing — classify the PR type first, then dispatch to the appropriate check module","Orchestrator-workers — a coordinator dynamically assigns checks to specialized subagents","Dynamic decomposition — the model decides which checks are relevant for each PR based on files changed"],correct:0,explanation:"Prompt chaining is correct for workflows where steps are known, fixed, ordered, and independent. Routing adds classification complexity when all PRs run all three checks regardless. Orchestrator-workers adds coordination overhead for a workflow with no dynamic assignment needed. Dynamic decomposition introduces variability — the business requirement is that all three checks always run in the same order."},
{domain:"d1",task:"1.6 Task decomposition",question:"A financial institution uses Claude to investigate unusual transaction patterns. Each investigation starts with the same alert metadata, but next steps depend entirely on what the agent finds: a geographic anomaly might lead to device fingerprinting, which might reveal a compromised credential, which might point to a phishing campaign. The structure of each investigation is different. Which decomposition pattern fits?",options:["Dynamic decomposition — adaptive subtask generation based on intermediate findings","Prompt chaining — a fixed sequential pipeline: anomaly check → device check → credential check → phishing check","Routing — classify the transaction type first, then dispatch to a fraud specialist subagent","Orchestrator-workers — a coordinator pre-assigns investigation threads to specialized subagents before any findings exist"],correct:0,explanation:"Dynamic decomposition is correct because the structure cannot be predetermined: each finding reshapes the plan. Forcing this into a fixed pipeline (B) runs all steps regardless of relevance — wasting compute and missing paths outside the fixed sequence. Routing (C) handles known input classifications, not emergent investigative branches. Orchestrator-workers (D) requires knowing the work breakdown upfront — but the investigation tree is unknown until runtime."},
{domain:"d1",task:"1.6 Task decomposition",question:"Why split a large multi-file code review into per-file local passes plus a separate cross-file integration pass?",options:["Splitting avoids attention dilution that occurs when a single pass tries to give equal depth across many files","Splitting reduces per-review token cost by processing files individually with shorter contexts rather than one large combined prompt","A single long-context pass produces too many false positives because the model flags cross-file patterns as local issues — per-file passes isolate the scope of each finding","Separate passes allow different review criteria to be applied to different file types in the same PR without modifying the shared system prompt"],correct:0,explanation:"Attention dilution is the core issue. Per-file passes give consistent depth on local issues; a separate cross-file pass focuses on data-flow concerns."},
{domain:"d1",task:"1.6 Task decomposition",question:"You are deciding between prompt chaining and dynamic decomposition. The task is: 'investigate an unknown error in a legacy module.' Which fits?",options:["Dynamic decomposition — the next subtask depends on what is discovered at each step","Prompt chaining — error investigation is always a fixed sequence","Routing — classify the error first, then dispatch","Neither — use a single-prompt approach"],correct:0,explanation:"Investigation tasks are open-ended; adaptive decomposition is the right pattern. The right next step depends on findings."},
{domain:"d1",task:"1.6 Task decomposition",question:"Decomposing 'add comprehensive tests to a legacy codebase' requires mapping structure and prioritizing high-impact areas first. Why?",options:["Without prioritization, the task balloons — by focusing on high-impact areas first, the workflow delivers the most critical coverage before context limits are reached","Mapping structure is required by Claude Code's CLI before any file writes can proceed — no such requirement exists; Claude Code can write files without a prior mapping phase","Tests cannot be written for a module until its full call tree has been traced as a sitemap — tests can be written for individual functions without a complete call-tree map","Legacy codebases apply validation at commit time that rejects test files not placed in a pre-registered test directory — this is a hypothetical constraint; the real reason to map first is prioritization, not directory registration"],correct:0,explanation:"Prioritizing high-impact areas ensures the most critical coverage is produced before context or iteration limits are reached — alphabetical or arbitrary test generation can exhaust resources on low-priority files. The CLI doesn't require mapping, tests don't require full call trees, and legacy codebases don't reject tests by directory registration."},
{domain:"d1",task:"1.6 Task decomposition",question:"Your pipeline translates natural language business requirements into SQL queries, validates the SQL against a schema, then generates an explanation for non-technical stakeholders. Each step's output is the next step's input, the order is fixed, and the steps are known before execution starts. A teammate proposes adding an orchestrator-coordinator to manage this dynamically. What is the simpler correct pattern and why?",options:["Prompt chaining — a fixed sequential pipeline where each step is focused and feeds directly into the next","Dynamic decomposition — the model may discover during SQL generation that the requirement is ambiguous and need to clarify","A multi-agent orchestration pipeline — separating translation, validation, and explanation into specialist subagents improves quality","A single comprehensive prompt that asks the model to produce all three outputs simultaneously"],correct:0,explanation:"Prompt chaining is correct for predictable sequential workflows where steps are known, ordered, and each output feeds the next. An orchestrator adds coordination overhead with no benefit when there's nothing to coordinate dynamically. Dynamic decomposition is for tasks whose structure is unknown at start — this workflow's structure is fully known. A single comprehensive prompt dilutes attention across all three concerns simultaneously."},
{domain:"d1",task:"1.6 Task decomposition",question:"You are architecting a Claude-powered system to modernize a 15-year-old e-commerce platform's database layer. The codebase has no architecture diagram, inconsistent naming, and several layers of legacy abstractions. A junior engineer suggests starting with the most-changed files from git history. A senior engineer insists on an upfront analysis phase. Which decomposition approach applies here?",options:["Tasks where the structure is unknown until exploration is done benefit from an upfront analysis phase before adaptive subtask generation","Fixed-sequence pipelines are more appropriate — database migrations always follow the same schema → data → index pattern regardless of codebase state","Parallel decomposition is preferable — each database table can be migrated independently once the schema is mapped, so upfront analysis is unnecessary overhead","The junior engineer's git history approach is correct — code change frequency is a reliable proxy for both business criticality and coupling, making it sufficient as a decomposition basis"],correct:0,explanation:"Unknown-structure exploration tasks require an upfront analysis phase before decomposing into subtasks — without mapping the legacy abstractions and dependencies, any implementation plan risks breaking hidden couplings. Fixed-sequence pipelines assume structural knowledge that doesn't exist. Parallel table migration is possible eventually but requires discovery first. Git history frequency is a heuristic, not a complete decomposition basis for an unknown codebase."},

/* --- 1.7 Session state, resume, fork --- */
{domain:"d1",task:"1.7 Session management",question:"Your team runs multi-day security audits using Claude Code. Each audit takes 3-4 days and builds on prior findings — discovered vulnerabilities inform which attack surfaces to investigate next. At the start of day 2, the engineer needs to continue exactly where day 1 left off, with full access to the prior conversation context, tool results, and investigative thread. Which Claude Code mechanism is designed for this?",options:["<code>claude --resume &lt;session-name&gt;</code> continues a specific named prior session with its full conversation history","<code>fork_session</code> from the last checkpoint, branching the investigation into a new named session each day","<code>--continue</code>, which resumes the most recently active session regardless of name","<code>/memory</code>, which persists the session context to a durable file that can be reloaded at the start of each day"],correct:0,explanation:"<code>--resume &lt;session-name&gt;</code> is the documented mechanism for continuing a specific named conversation across separate CLI invocations — exactly what multi-day investigations need. <code>fork_session</code> creates branches for divergent exploration, not for continuing a single linear investigation. <code>--continue</code> is not the mechanism for resuming named sessions. <code>/memory</code> manages instruction files, not conversation state."},
{domain:"d1",task:"1.7 Session management",question:"You want to explore two different testing strategies starting from the same shared codebase analysis. Which feature creates parallel exploration branches?",options:["<code>fork_session</code>","<code>--resume</code>","<code>--print</code>","<code>/compact</code>"],correct:0,explanation:"<code>fork_session</code> creates independent branches from a shared baseline — exactly the pattern for parallel exploration of divergent approaches."},
{domain:"d1",task:"1.7 Session management",question:"You resumed a prior session, but several files have been modified since then. The agent gives stale advice. What should you do?",options:["Inform the resumed session about the specific file changes for targeted re-analysis, or start fresh with an injected summary","Continue without telling the agent — it will figure out the changes itself","Re-run the entire investigation from scratch every time","Disable session resumption permanently"],correct:0,explanation:"When files change, you must tell the resumed session what changed (for targeted re-analysis) or start fresh with a summary. Stale tool results in the resumed context will mislead the agent."},
{domain:"d1",task:"1.7 Session management",question:"A Claude Code security audit session accumulated findings over two days. On day 3, the codebase was refactored significantly: modules renamed, file structures changed, two components replaced entirely. The engineer plans to resume the session and continue. A teammate warns this could produce unreliable results. What is the specific risk?",options:["Prior tool results referencing the old file structure are now stale — the model may reason from outdated facts; start a new session with a structured summary of valid prior findings","The session's token count is likely near the context limit after two days — resuming will cause context overflow on the next tool call","Resuming a session after a git branch switch causes Claude Code to lose access to the new file system state","The prior session's system prompt will conflict with any new CLAUDE.md rules added during the refactor"],correct:0,explanation:"The key risk is stale context: the prior session contains tool results referencing file paths, module names, and component structures that no longer exist. The model reasoning over that stale context can produce confusing or incorrect findings. The right approach is a fresh session with a structured summary of valid prior findings — confirmed vulnerabilities, attack surfaces explored, etc. Token limits (B) are real but secondary. C and D describe non-issues."},
{domain:"d1",task:"1.7 Session management",question:"After completing a 90-minute codebase analysis, your team wants to compare two architectural approaches: Approach A (event-driven microservices) and Approach B (CQRS with shared database). Both must build from the same analysis baseline — you don't want to re-run the 90-minute analysis for each. Which is the right mechanism?",options:["Use <code>fork_session</code> so both approach evaluations start from the same analysis baseline and diverge independently","Start two independent sessions from scratch, each re-running the analysis phase before evaluating its approach","Use <code>--resume</code> twice in parallel, evaluating both approaches within the same resumed session sequentially","Run <code>/compact</code> on the analysis session, then use the summary as a shared starting prompt for two fresh sessions"],correct:0,explanation:"<code>fork_session</code> is designed for exactly this: branching two independent exploration paths from a shared baseline. Both approaches inherit the full analysis context without re-running it, and changes in one branch don't affect the other. Starting from scratch (B) wastes 90 minutes twice. Using <code>--resume</code> sequentially (C) means Approach A's findings pollute Approach B's evaluation. Compacting (D) loses precision and requires manually threading the summary into two new sessions."},
{domain:"d1",task:"1.7 Session management",question:"Your team uses Claude Code for week-long performance profiling investigations. Each investigation is named (e.g., 'q4-payment-latency'). The engineer returns after a weekend. Which is the correct approach to resume the investigation?",options:["<code>claude --resume q4-payment-latency</code> — resumes the specific named session, restoring its full conversation history and accumulated tool results","Save the final assistant message to a Markdown file and paste it as context at the start of a new session — this preserves only the last response, losing all intermediate tool calls and the investigative thread","Start a fresh session each time — stale tool results from a week-old session may reference code that has since changed","Use <code>fork_session</code> from the most recent checkpoint to create a named branch for the new week — fork_session is for divergent exploration, not for simply continuing a linear investigation after a break"],correct:0,explanation:"--resume <session-name> continues a specific named session across separate CLI invocations, restoring full context. Pasting the last message loses all prior tool results. Starting fresh discards valid prior findings. fork_session creates branches for divergent paths, not for resuming a single linear investigation."},
{domain:"d1",task:"1.7 Session management",question:"A developer is deciding whether to resume a Claude Code session or start fresh with an injected summary. The session was started 6 days ago to debug a caching bug. Since then, the cache layer was completely replaced with a different library, the original bug was fixed, and a new performance regression appeared in a different module. What should guide the decision?",options:["Whether prior tool results and context are mostly still valid (resume) or stale (start fresh with a summary of what still applies)","Whether the session's token count exceeds a threshold — sessions above 50K tokens should always start fresh to avoid context overflow","Whether the task has advanced to a new phase — a new regression is a new task and always warrants a fresh session","Available context budget — if more than 30% of budget remains, resume; if less, start fresh to avoid truncation"],correct:0,explanation:"Context validity is the correct criterion. The prior session's tool results reference a cache library that no longer exists and a bug that's already fixed — that context is stale and would confuse the model. Starting fresh with a structured summary ('cache layer replaced, original bug fixed, investigating new regression in module X') gives accurate context without obsolete noise. Token thresholds (B), phase transitions (C), and budget percentages (D) are mechanical heuristics that don't capture whether prior context is actually useful."},
{domain:"d1",task:"1.7 Session management",question:"Your team completed a 2-hour static analysis of a large codebase to understand module dependencies and architecture. Now you want to explore two different database migration strategies: Strategy A uses in-place schema migration; Strategy B uses a dual-write pattern with gradual cutover. Both must build from the same analysis baseline and be evaluated independently. What is the recommended session management pattern?",options:["Fork the session — both branches share the analysis baseline but evolve independently so each strategy's exploration doesn't contaminate the other","Resume the session and instruct it to explore Strategy A fully, then restart the resumed session for Strategy B","Use <code>--resume</code> to start each strategy in a separate session, accepting some baseline re-analysis to ensure clean context","Spawn two subagents in parallel, each receiving a copy of the baseline summary as context in their prompt"],correct:0,explanation:"<code>fork_session</code> is purpose-built for exploring divergent approaches from a shared baseline — both branches start from the same 2-hour analysis and diverge independently, with neither contaminating the other. Resuming sequentially (B) means Strategy A's exploration colors Strategy B's evaluation. Starting separate sessions (C) re-runs the expensive analysis unnecessarily. Subagents (D) receive a summary copy, not the full analysis context, losing precision."},

/* ============================================================
   DOMAIN 2 — Tool Design & MCP Integration (18%)
   ============================================================ */

/* --- 2.1 Tool descriptions and boundaries --- */
{domain:"d2",task:"2.1 Tool descriptions",question:"Production logs show the agent frequently calls <code>get_customer</code> when users ask about orders (e.g., 'check my order #12345'), instead of calling <code>lookup_order</code>. Both tools have minimal descriptions ('Retrieves customer information' / 'Retrieves order details') and accept similar identifier formats. What's the most effective first step to improve tool selection reliability?",options:["Expand each tool's description to include input formats it handles, example queries, edge cases, and boundaries explaining when to use it versus similar tools","Add few-shot examples to the system prompt demonstrating correct tool selection patterns, with 5-8 examples showing order-related queries routing to <code>lookup_order</code>","Implement a routing layer that parses user input before each turn and pre-selects the appropriate tool based on detected keywords and identifier patterns","Consolidate both tools into a single <code>lookup_entity</code> tool that accepts any identifier and internally determines which backend to query"],correct:0,explanation:"This is question 2 from the official guide. Tool descriptions are the primary mechanism LLMs use for tool selection. When descriptions are minimal, models lack the context to differentiate similar tools. Option B adds token overhead without fixing the underlying issue. A routing layer over-engineers and bypasses the LLM's natural language understanding. Consolidation is a valid choice but more effort than a 'first step' warrants."},
{domain:"d2",task:"2.1 Tool descriptions",question:"What is the primary mechanism LLMs use to decide which tool to call?",options:["The tool's description — the model reads each tool's description to understand its purpose, when to use it, and what it accepts; this is the primary signal for tool selection","The tool's parameter schema — while parameter types and names provide structural hints, the natural language description is the primary selection signal","The order of tools in the tools array — research shows no reliable positional bias in tool selection; descriptions dominate over position","The tool's name alone — names provide a coarse signal but are insufficient when multiple tools have semantically similar names; descriptions carry the deciding weight"],correct:0,explanation:"Tool descriptions are the primary mechanism LLMs use for selection — they provide the semantic context for when, why, and how to use each tool. Parameter schemas provide structural constraints. Tool position in the array has no reliable effect. Names are helpful but insufficient when tools are semantically similar."},
{domain:"d2",task:"2.1 Tool descriptions",question:"You have <code>analyze_content</code> and <code>analyze_document</code> tools with near-identical descriptions. The agent misroutes between them. Best fix?",options:["Rename one (e.g., to <code>extract_web_results</code>) and rewrite its description to make purpose, inputs, and boundaries unambiguous","Add a routing-rules section to the system prompt listing which query patterns map to each tool, overriding the model's natural selection logic","Add few-shot examples in the system prompt demonstrating the correct tool selection for representative inputs of each type","Consolidate them into a single <code>analyze</code> tool with a <code>mode</code> parameter, letting the model specify the analysis type in the call"],correct:0,explanation:"Renaming and rewriting descriptions to eliminate overlap is the recommended fix. Distinct names plus distinct boundaries lead to reliable selection."},
{domain:"d2",task:"2.1 Tool descriptions",question:"When you want to enforce 'always call a tool, never reply with conversational text', use:",options:["<code>tool_choice: \"any\"</code> — forces the model to invoke at least one tool every turn, preventing plain-text responses entirely","<code>tool_choice: \"auto\"</code> — the model evaluates whether a tool call is needed and may return conversational text when it decides no tool is relevant","Omitting <code>tool_choice</code> — without the parameter, the model defaults to <code>\"auto\"</code> behavior, making text replies equally valid responses","A prominent system prompt rule like 'ALWAYS invoke a tool before responding; never reply with text' — this provides guidance but does not deterministically prevent text-only responses"],correct:0,explanation:"tool_choice: \"any\" deterministically enforces tool calls on every turn. \"auto\" and the default both permit text-only responses when the model judges them appropriate. Prompt rules are probabilistic — the model may still return text under certain conditions."},
{domain:"d2",task:"2.1 Tool descriptions",question:"A generic <code>analyze_document</code> handles too many things and the model keeps using it inconsistently. Best architectural fix?",options:["Split it into purpose-specific tools (e.g., <code>extract_data_points</code>, <code>summarize_content</code>, <code>verify_claim_against_source</code>) with defined contracts","Enrich <code>analyze_document</code>'s description with more concrete examples and stricter boundary language — without splitting the tool","Rename <code>analyze_document</code> to <code>process_document</code> to reduce naming ambiguity, while keeping the same broad functionality","Add a dispatcher tool (<code>route_analysis</code>) the model calls first to identify the analysis type, then passes control to <code>analyze_document</code>"],correct:0,explanation:"Splitting overloaded generic tools into purpose-specific tools with clear contracts gives the model unambiguous choices and reduces misuse."},
{domain:"d2",task:"2.1 Tool descriptions",question:"Even with well-written tool descriptions, your model still sometimes ignores the right tool. The next most common cause is:",options:["Keyword-sensitive instructions in the system prompt that bias the model toward general or conversational responses over tool calls","The order of tools in the tools array — the model systematically prefers tools listed first and ignores later entries","Token budget limits in the API request — when max_tokens is low, the model skips tool calls to conserve output space","A mismatch between the tool's parameter types and the model's internal type inference for the request"],correct:0,explanation:"System prompt keyword sensitivity is the next most common cause after description quality — instructions like 'be helpful and conversational' can bias the model away from structured tool calls. Tool order in the array has no reliable effect on selection. Token budget constraints don't cause tool selection failures. Parameter type mismatches cause tool call errors, not tool selection failures."},
{domain:"d2",task:"2.1 Tool descriptions",question:"Which is NOT a good practice for tool description authoring?",options:["Encoding format hints in parameter names (e.g., <code>account_number_integer_eight_digits</code>)","Documenting example queries that show what user intent the tool is designed to handle, such as 'When a user asks about orders, call this tool'","Documenting edge cases like behavior on empty input, maximum identifier lengths, or what happens when no matching record exists","Explicitly stating when to use this tool instead of a semantically similar tool, identifying the specific conditions that differentiate them"],correct:0,explanation:"Encoding format hints in parameter names is less effective than concise descriptions. The model reads descriptions; it doesn't parse semantics out of camelCase."},
{domain:"d2",task:"2.1 Tool descriptions",question:"Why do agents sometimes prefer a built-in tool (like <code>Grep</code>) over a more capable MCP tool?",options:["The MCP tool's description is too vague, so the model can't tell when it's better than the familiar built-in","MCP tools require an extra protocol round-trip to the server, so the model prefers built-ins when it perceives the latency trade-off isn't worth the capability gain","The model gives higher implicit trust to built-in tools because they appear in the base tool registry, while MCP tools require a separate connection to activate","Built-in tools like <code>Grep</code> are pre-authorized in all contexts; MCP tools may require per-session capability grants that aren't consistently configured across environments"],correct:0,explanation:"Non-adoption of MCP tools is almost always a description problem. Expand the description with capabilities, inputs, outputs, and when to prefer this tool."},
{domain:"d2",task:"2.1 Tool descriptions",question:"You want the model to reliably differentiate <code>fetch_url</code> (generic) from a constrained <code>fetch_internal_api</code>. What solves selection unreliability?",options:["Replace generic tools with constrained alternatives whose descriptions clearly specify their scope, data source, and when to prefer them over semantically overlapping tools","Add <code>tool_choice: \"any\"</code> — this forces the model to call some tool but does not help it choose between two semantically similar tools with overlapping names","Rename both tools to start with 'fetch_' to make their shared domain explicit — a shared prefix adds visual consistency but doesn't resolve ambiguity about which tool handles which source","Wrap both into a single unified <code>fetch</code> tool with a <code>source</code> parameter — merging them removes the selection problem but loses the separate validation and access-control properties of each tool"],correct:0,explanation:"Description quality is the primary mechanism for tool selection — replacing vague descriptions with scope-specific ones that explain exactly when to use each tool is the correct fix. tool_choice: \"any\" doesn't guide selection. Shared prefixes add visual structure but not semantic clarity. Merging tools eliminates the selection problem but creates a different design problem."},
{domain:"d2",task:"2.1 Tool descriptions",question:"Which of the following is the most effective single intervention to improve tool selection reliability when two tools have overlapping purposes?",options:["Rewrite descriptions to make purposes, inputs, outputs, and boundaries distinct","Set <code>tool_choice: \"auto\"</code>","Use more few-shot examples","Add the tool's signature to the system prompt"],correct:0,explanation:"Description rewriting is the highest-leverage fix. Few-shot examples help but add tokens; <code>tool_choice: \"auto\"</code> is the default and doesn't change selection."},

/* --- 2.2 Structured errors --- */
{domain:"d2",task:"2.2 Structured errors",question:"Which MCP flag is used to signal that a tool result represents a failure?",options:["<code>isError: true</code>","<code>status: \"failed\"</code>","<code>error_code: -1</code>","<code>retry: true</code>"],correct:0,explanation:"The MCP spec uses <code>isError</code> to signal failure in application-level tool results."},
{domain:"d2",task:"2.2 Structured errors",question:"Your tool returns the same generic <code>{\"error\": \"Operation failed\"}</code> for all failures. What problem does this cause for the agent?",options:["The agent cannot make appropriate recovery decisions because it can't distinguish transient, validation, business, or permission errors","The agent's retry budget is exhausted even on permanent errors like permission denials, because a uniform error format hides which failures are retryable","The model defaults to escalating every failure to the human operator, since it cannot determine whether to retry, adapt its approach, or give up","The agent enters an infinite retry loop for permanent failures, since the generic error provides no signal that retrying is futile"],correct:0,explanation:"Uniform errors prevent intelligent recovery. The agent needs structured metadata (category, retryability, message) to choose the right next action."},
{domain:"d2",task:"2.2 Structured errors",question:"Which of these error categories should NOT be retried automatically?",options:["A business rule violation (e.g., 'insufficient funds for transfer')","A transient network timeout that resolves within the retry window","A <code>429 Too Many Requests</code> response from an external API, indicating a rate limit that clears after the backoff period","A <code>503 Service Unavailable</code> from a downstream dependency experiencing a temporary outage"],correct:0,explanation:"Business rule violations are permanent — 'insufficient funds' won't change on retry. The other options (network timeout, rate limit, 503 unavailable) are all transient errors that are appropriate to retry with backoff. The exam guide distinguishes permanent semantic failures from transient infrastructure failures."},
{domain:"d2",task:"2.2 Structured errors",question:"You return structured error metadata. Which three fields are recommended by the exam guide?",options:["<code>errorCategory</code> (transient/validation/permission/business), <code>isRetryable</code> boolean, and a human-readable description","<code>http_status</code>, <code>retry_count</code>, and <code>stack_trace</code> — transport-level fields sufficient for infrastructure-layer debugging","<code>severity</code>, <code>timestamp</code>, and <code>request_id</code> — operational fields designed for monitoring dashboards and log correlation","<code>language</code>, <code>region</code>, and <code>customer_id</code> — contextual fields required for localized, multi-tenant error message generation"],correct:0,explanation:"The exam guide specifies these three fields: an error category, an explicit retryability flag, and a human-readable description."},
{domain:"d2",task:"2.2 Structured errors",question:"For a business rule violation (policy says no refunds after 90 days), the structured error response should include:",options:["<code>retriable: false</code> and a customer-friendly explanation so the agent communicates appropriately","<code>retriable: true</code> with exponential backoff hints — the refund window may re-open if a support agent applies a manual policy override","A terse machine-readable error code only, trusting the calling application's error registry to supply the appropriate customer-facing message","The HTTP 422 status code and a timestamp, delegating message composition entirely to the front-end presentation layer"],correct:0,explanation:"Business rule violations are non-retryable and need customer-friendly explanations so the agent can communicate clearly."},
{domain:"d2",task:"2.2 Structured errors",question:"A subagent encounters a transient network timeout. Best practice is to:",options:["Implement local recovery (retry within the subagent); only propagate to the coordinator if it can't be resolved locally, along with partial results and what was attempted","Propagate the error to the coordinator immediately with full error details so it can decide whether to re-queue the task with different parameters","Suppress the timeout internally and return the last cached result as a successful response, logging the timeout for later reconciliation","Signal the coordinator to restart the entire pipeline from its last checkpoint, treating the subagent as permanently failed"],correct:0,explanation:"Local recovery within subagents avoids unnecessary coordinator escalation for transient failures. Propagate only what can't be resolved, with partial results and context."},
{domain:"d2",task:"2.2 Structured errors",question:"A search tool returns no matches. The agent retries 5 times because the response looks like a failure. What's the design fix?",options:["Distinguish a valid empty result (successful query with no matches) from an access failure (timeout, error) in the response structure","Always return <code>retriable: true</code> with a zero-length result list, letting the caller's retry policy decide whether to try alternative search terms","Return incrementing status codes on each retry so the model can detect the pattern and infer that repeated retries haven't improved the result","Wrap the empty result in a generic error object, so the model's standard error-recovery path handles it rather than treating it as a valid zero-result response"],correct:0,explanation:"Empty results are not failures. Distinguishing them from access failures prevents wasted retries and gives the agent appropriate context."},
{domain:"d2",task:"2.2 Structured errors",question:"For uncertain state — e.g., a write operation that timed out and you don't know if it succeeded — the structured error should:",options:["Communicate uncertainty explicitly and recommend NOT retrying (to avoid duplicate side effects)","Mark it as <code>retriable: true</code> with exponential backoff — retrying a write with an idempotency key is safe in most database systems","Return a success response with a <code>status: 'pending'</code> field, allowing the caller to poll a separate endpoint asynchronously for the final outcome","Throw an exception and let the application framework's global error handler classify the failure and determine the appropriate retry policy"],correct:0,explanation:"Uncertain-state write timeouts must communicate the uncertainty and prevent automatic retry to avoid double charges or duplicate notifications."},
{domain:"d2",task:"2.2 Structured errors",question:"Why is throwing exceptions in tool implementations worse than returning structured errors as tool content?",options:["Framework-level exception handling strips the error metadata the model needs to make recovery decisions","Exceptions bubble up to the application's top-level handler, which may restart the entire agentic loop rather than allowing the model to attempt a recovery action","Throwing an exception forces the model to see a raw stack trace in the tool result, consuming tokens without providing actionable recovery context","Exceptions prevent the <code>PostToolUse</code> hook from running, bypassing any normalization or audit logging configured for that tool"],correct:0,explanation:"Framework-level exception handling strips the structured error metadata the model needs to decide recovery actions. Returning <code>isError: true</code> content lets the model see error type, a human-readable message, retry guidance, and suggested alternatives. B is a real consequence of unhandled exceptions but not the primary reason. C confuses stack traces with structured errors. D describes a real hook concern but is secondary."},
{domain:"d2",task:"2.2 Structured errors",question:"Which is true about transient vs. permanent errors?",options:["Transient errors should be retried (often within the tool itself); permanent errors should be reported with enough detail for the agent to explain and adapt","Both categories benefit from retry with exponential jitter — transient errors typically resolve within 3 attempts, while permanent errors are identified by consistent failure after the retry budget is exhausted","Permanent errors like 'record not found' should be retried once with a refreshed identifier before being reported, in case the identifier was fetched from a stale cache","Transient errors should immediately route to a human operator rather than being retried, since automated retries can mask infrastructure instability that requires manual investigation"],correct:0,explanation:"Tool-level retry for transient errors keeps the model focused on user communication; permanent errors should be reported with structured detail for adaptation."},

/* --- 2.3 Tool distribution and tool_choice --- */
{domain:"d2",task:"2.3 Tool distribution",question:"Your agent has access to 18 tools and tool selection has become unreliable. What is the recommended approach?",options:["Restrict each agent's tool set to ~4-5 tools relevant to its role, preventing cross-specialization misuse","Give every agent access to every tool to maximize flexibility — well-written descriptions compensate for selection complexity regardless of tool count","Switch to a larger model with more parameters, since frontier models handle longer tool lists with better description-following reliability","Disable all tools temporarily, reintroduce them one by one, and measure selection accuracy at each step to identify which tools are causing conflicts"],correct:0,explanation:"Too many tools degrades selection reliability by increasing decision complexity. Scoping each agent to ~4-5 role-relevant tools is the recommended pattern."},
{domain:"d2",task:"2.3 Tool distribution",question:"Which <code>tool_choice</code> value guarantees the model will call SOME tool but lets it pick which?",options:["<code>\"any\"</code> — forces the model to call at least one tool every turn, but does not constrain which tool it chooses","<code>\"auto\"</code> — allows the model to call any available tool or respond with text if no tool call is warranted; does not guarantee a tool will be called","<code>{\"type\": \"tool\", \"name\": \"search\"}</code> — forces a specific named tool call, satisfying the 'must call a tool' requirement but removing the model's freedom to choose which tool","Omitting <code>tool_choice</code> entirely — without the parameter, the model defaults to <code>\"auto\"</code> behavior, making text responses equally likely"],correct:0,explanation:"\"any\" forces the model to call at least one tool every turn while still letting it select from the available set. \"auto\" allows text-only responses when the model decides no tool is needed. A named tool override forces a specific tool, removing the model's flexibility. Omitting tool_choice defaults to \"auto\" behavior."},
{domain:"d2",task:"2.3 Tool distribution",question:"To ensure <code>extract_metadata</code> is the FIRST tool called in a workflow, which <code>tool_choice</code> setting guarantees this on the first API call?",options:["<code>{\"type\": \"tool\", \"name\": \"extract_metadata\"}</code> — forces the model to call this specific tool and no other on this request, guaranteeing it runs first","<code>\"any\"</code> — forces a tool call but lets the model choose which tool; it might call an enrichment tool instead of <code>extract_metadata</code>","<code>\"auto\"</code> — the model decides which tool to call based on context; it may or may not call <code>extract_metadata</code> first","A system prompt instruction: 'Always call <code>extract_metadata</code> before any other tool' — probabilistic compliance; the model may deviate under complex inputs"],correct:0,explanation:"{\"type\": \"tool\", \"name\": \"extract_metadata\"} forces exactly that tool on that request — guaranteed first call. \"any\" forces some tool but doesn't specify which. \"auto\" gives the model full discretion. System prompt instructions are probabilistic."},
{domain:"d2",task:"2.3 Tool distribution",question:"A user wants to verify which memory files are loaded for their current session. The command is:",options:["<code>/memory</code> — displays the list of CLAUDE.md files and memory files currently loaded in the session context","<code>/compact</code> — compresses the current conversation to save context space, but does not display loaded memory files","<code>/clear</code> — resets the current conversation history and unloads all context, rather than displaying what's loaded","<code>/reload</code> — forces the session to re-read CLAUDE.md files from disk to pick up recent changes, but does not display the loaded file list"],correct:0,explanation:"/memory shows which instruction and memory files are currently in the model's context — the correct command for diagnosing what's loaded. /compact summarizes history to save tokens. /clear wipes the conversation. /reload refreshes file-based instructions from disk."},
{domain:"d2",task:"2.3 Tool distribution",question:"For a synthesis agent that occasionally needs to verify a fact but should not perform full web searches, the recommended pattern is:",options:["Give it a scoped <code>verify_fact</code> tool for high-frequency simple lookups; route complex verifications through the coordinator","Give the synthesis agent full access to the web search tool but add system prompt instructions limiting usage to single-fact lookups of no more than three queries per synthesis task","Remove all verification capability from the synthesis agent and have it flag uncertain claims with a low-confidence marker for the coordinator to resolve","Disable all tool use in the synthesis agent and precompute verified supporting evidence in the coordinator, injecting it into each synthesis prompt"],correct:0,explanation:"Scoped cross-role tools handle high-frequency needs without giving the synthesis agent broader capabilities it would misuse."},
{domain:"d2",task:"2.3 Tool distribution",question:"You want to guarantee the model calls a tool rather than returning a free-form text reply (e.g., when every request must produce structured output). Which <code>tool_choice</code> setting achieves this?",options:["<code>\"any\"</code> — guarantees the model calls at least one tool on every turn, preventing conversational text responses","<code>\"auto\"</code> — the model evaluates whether a tool call is warranted; structured output is not guaranteed because the model may return plain text when it judges no tool call is needed","Omitting <code>tool_choice</code> entirely — the model defaults to <code>\"auto\"</code> behavior, calling a tool only when it believes it appropriate; text-only responses remain possible","<code>{\"type\": \"text\"}</code> — this setting forces a plain-text response, which can be schema-shaped via prompt but does not use tool calling"],correct:0,explanation:"\"any\" is the correct setting when every request must produce a tool call — it prevents plain-text responses entirely. \"auto\" and the default both allow the model to reply with text when it decides no tool is needed. {\"type\": \"text\"} forces text and prohibits tool calls."},
{domain:"d2",task:"2.3 Tool distribution",question:"You give an agent access to <code>verify_fact</code> as a scoped cross-role tool. What is the trade-off vs. routing every verification through the coordinator?",options:["Scoped tools reduce coordinator round-trips for the common case but require careful description to prevent over-use","There is no meaningful trade-off — a scoped tool call has the same latency as a coordinator round-trip, so the choice is purely architectural preference","Giving the synthesis agent direct verification access removes the coordinator from the common verification path, potentially making the coordinator role redundant over time","Scoped tool calls increase per-turn token cost because the synthesis agent's context must include the tool schema, even on turns where no verification is needed"],correct:0,explanation:"Scoped cross-role tools improve efficiency on the common path but require good descriptions and clear boundaries to prevent the agent from over-relying on them for complex cases."},
{domain:"d2",task:"2.3 Tool distribution",question:"During testing, you observe that the synthesis agent frequently needs to verify specific claims while combining findings. Currently, when verification is needed, the synthesis agent returns control to the coordinator, which invokes the web search agent, then re-invokes synthesis with results. This adds 2-3 round trips per task and increases latency by 40%. Your evaluation shows that 85% of these verifications are simple fact-checks (dates, names, statistics) while 15% require deeper investigation. What's the most effective approach to reduce overhead while maintaining system reliability?",options:["Give the synthesis agent a scoped <code>verify_fact</code> tool for simple lookups, while complex verifications continue delegating to the web search agent through the coordinator","Have the synthesis agent accumulate all verification needs and return them as a batch to the coordinator at the end of its pass","Give the synthesis agent access to all web search tools so it can handle any verification need directly","Have the web search agent proactively cache extra context around each source during initial research, anticipating what the synthesis agent might need to verify"],correct:0,explanation:"This is question 9 from the official guide. Option A applies least privilege: scoped tool for the 85% common case, coordinator path preserved for 15% complex cases. Batching creates blocking dependencies; full access violates separation of concerns; speculative caching can't reliably predict needs."},
{domain:"d2",task:"2.3 Tool distribution",question:"The number of tools that degrades selection reliability often kicks in around:",options:["A dozen or more tools — as the decision space grows, the model's ability to consistently select the right tool degrades; beyond ~10-12 tools, reliability drops noticeably","Two or three tools — selection is reliable with any number above two; the issue only appears when tools have identical names","Exactly 20 tools — selection reliability is a step function: perfect below 20, unreliable above it","The number of tools has no effect on selection reliability — the model's tool-selection quality depends entirely on description quality, not on the count"],correct:0,explanation:"Tool selection reliability degrades as count grows beyond roughly 10-12 — the model's decision space becomes too large for consistent disambiguation. Two or three tools don't degrade selection. There's no exact 20-tool cliff. Count matters; description quality alone can't compensate for an extremely large tool set."},
{domain:"d2",task:"2.3 Tool distribution",question:"When you want to enforce 'always call a tool, never reply with conversational text', use:",options:["<code>tool_choice: \"any\"</code> — forces the model to invoke at least one tool every turn, preventing plain-text responses entirely","<code>tool_choice: \"auto\"</code> — the model evaluates whether a tool call is needed and may return conversational text when it decides no tool is relevant","Omitting <code>tool_choice</code> — without the parameter, the model defaults to <code>\"auto\"</code> behavior, making text replies equally valid responses","A prominent system prompt rule like 'ALWAYS invoke a tool before responding; never reply with text' — this provides guidance but does not deterministically prevent text-only responses"],correct:0,explanation:"tool_choice: \"any\" deterministically enforces tool calls on every turn. \"auto\" and the default both permit text-only responses when the model judges them appropriate. Prompt rules are probabilistic — the model may still return text under certain conditions."},

/* --- 2.4 MCP server integration --- */
{domain:"d2",task:"2.4 MCP integration",question:"You want to share an MCP server configuration with your entire team via version control. Where should the config go?",options:["Project-level <code>.mcp.json</code> — committed to the repository root, this file is shared with all team members who clone the project","<code>~/.claude.json</code> on the lead engineer's machine, with a note in the README — this is user-level config that exists only on one machine and requires teammates to manually replicate it","<code>.claude/rules/</code> with a YAML frontmatter <code>mcp: true</code> flag — rules files manage instruction scoping and path-based context loading, not MCP server registration","An environment variable <code>CLAUDE_MCP_SERVER_URL</code> set in the team's CI/CD platform — environment variables are not the mechanism for registering MCP tools in Claude Code"],correct:0,explanation:"Project-level .mcp.json lives in the repository root and is committed to version control, making it automatically available to every team member who clones the project. ~/.claude.json is user-level and not shared. Rules files handle instruction scoping. Environment variables are not the MCP registration mechanism."},
{domain:"d2",task:"2.4 MCP integration",question:"For a personal experimental MCP server you don't want to share with teammates, the config goes in:",options:["<code>~/.claude.json</code> — the user-level configuration file; settings here are local to your machine and never committed to version control or shared with teammates","<code>.mcp.json</code> in the project root, under a personal namespace key — this file is version-controlled and visible to all teammates who pull the repository","<code>.claude/rules/</code> with a YAML frontmatter flag to activate the server only when specified — a rules file manages instruction scoping, not MCP server registration","The project-level <code>CLAUDE.md</code>, since CLAUDE.md configuration and MCP server settings share the same hierarchical lookup — CLAUDE.md manages instructions, not MCP server registration"],correct:0,explanation:"~/.claude.json is the user-level config that lives on your machine and is never version-controlled — the right place for personal MCP servers you don't want shared. .mcp.json in the project root is committed to git and visible to all collaborators. Rules files and CLAUDE.md manage instruction scoping, not MCP server configuration."},
{domain:"d2",task:"2.4 MCP integration",question:"You need a GitHub MCP server to authenticate with a token. How do you keep secrets out of <code>.mcp.json</code>?",options:["Use environment variable expansion (e.g., <code>${GITHUB_TOKEN}</code>) in the config so the secret comes from the env at runtime","Commit the token directly to <code>.mcp.json</code> and rely on <code>.gitignore</code> to prevent it from being pushed to the remote — acceptable for solo projects","Hardcode the token in the system prompt under an <code>auth</code> header, noting that Claude Code strips recognized credentials from conversation logs before storage","Store credentials in a plaintext <code>.mcp-credentials</code> file referenced by path from <code>.mcp.json</code>, with the credentials file excluded via <code>.gitignore</code>"],correct:0,explanation:"Environment variable expansion lets you commit the config without committing the secret. The secret stays in the env."},
{domain:"d2",task:"2.4 MCP integration",question:"When an agent connects to multiple MCP servers (CRM, Slack, metrics), which is true about tool availability?",options:["Tools from all connected servers are discovered at connection time and available simultaneously to the agent","Only one server's tools are available at a time; you switch by API call","Servers are queried sequentially when the agent calls a tool","Each conversational turn limits the agent to tools from a single MCP server; it cycles through configured servers in round-robin order across turns"],correct:0,explanation:"All MCP tools across all configured servers are discoverable and available simultaneously. There is no per-turn server selection."},
{domain:"d2",task:"2.4 MCP integration",question:"What is the primary value MCP provides?",options:["Reusability — one MCP server exposes tools to many AI applications via a common protocol","Built-in retry logic, rate-limit management, and automatic circuit breaking — so every agent connecting to an MCP server gets infrastructure resilience for free","Cost reduction through server-side result caching and deduplication of identical tool calls made within the same context window","Faster inference by offloading computation-heavy tool execution to dedicated server-side workers, so Claude's context window focuses on reasoning"],correct:0,explanation:"MCP's main value is reusability. It does NOT provide retries, rate limiting, or performance optimization by itself."},
{domain:"d2",task:"2.4 MCP integration",question:"Which of these is NOT something MCP provides automatically?",options:["Authentication, retries, rate-limiting, performance optimization","A standard protocol for tool discovery and capability advertisement at session connection time, enabling dynamic tool registration","A standardized application-level error reporting convention using the <code>isError</code> flag in tool result content","A standard message format for tool invocation including the tool name, input parameters, and expected result schema"],correct:0,explanation:"MCP is a protocol for discovery, invocation, and error reporting; it does not provide infrastructure features like auth, retries, or rate limiting."},
{domain:"d2",task:"2.4 MCP integration",question:"Your agent has access to a powerful MCP tool but keeps using built-in <code>Grep</code> instead. What is the most effective fix?",options:["Expand the MCP tool's description to clearly explain capabilities and outputs so the model prefers it over the familiar built-in","Remove <code>Grep</code> from the agent's <code>allowedTools</code> list entirely, forcing it to use the MCP tool for all search operations","Add a high-priority system prompt rule: 'Always prefer the <code>code_search</code> MCP tool over <code>Grep</code> for all code navigation tasks'","Allow the model to continue using <code>Grep</code> in early sessions; usage patterns will shift toward the MCP tool as the model gains familiarity with it"],correct:0,explanation:"Non-adoption of MCP tools is almost always a description problem. Expanding capabilities, inputs, outputs, and explicit comparisons to built-ins fixes it."},
{domain:"d2",task:"2.4 MCP integration",question:"MCP resources are best used to:",options:["Expose content catalogs (issue summaries, doc hierarchies, DB schemas) so agents have visibility without exploratory tool calls","Replace parameterized tool calls for static or versioned content — resources expose named snapshots that agents can download without constructing tool invocations","Provide server-side rendered UI components that Claude Code displays in its left panel as a supplementary reference alongside the conversation","Encrypt sensitive tool input parameters before transmission to the MCP server, ensuring API keys and tokens are not visible in conversation history"],correct:0,explanation:"MCP resources are the discovery mechanism for content catalogs — useful for giving the agent visibility into what's available without speculative tool calls."},
{domain:"d2",task:"2.4 MCP integration",question:"A community MCP server already exists for Jira integration. Your team needs Jira tools. What's the recommended approach?",options:["Prefer the community server over building a custom one; reserve custom servers for team-specific workflows","Always write a custom server — community MCP servers are typically research-quality and lack the access control and audit logging required for production workflows","Evaluate the community server by reviewing its source code and issue tracker; if it lacks permission scoping or input validation, build a hardened wrapper around it","Build a custom server first to fully understand your requirements, then migrate to the community server once you've validated that its interface meets your needs"],correct:0,explanation:"For standard integrations, community MCP servers are usually the right choice. Custom servers are justified when generic tools don't cover your workflow."},
{domain:"d2",task:"2.4 MCP integration",question:"An MCP tool is annotated with <code>readOnlyHint: true</code>. Is it safe to bypass user confirmation prompts based on this annotation?",options:["No — annotations are self-reported by the server and are not security guarantees; base trust on your assessment of the server","Yes — the MCP specification requires servers to honor annotation contracts; a tool marked <code>readOnlyHint: true</code> cannot perform writes at the protocol enforcement layer","Yes, if the server's TLS certificate is signed by a certificate authority in Claude Code's trusted root store, indicating it has passed a security review","Only when both <code>readOnlyHint: true</code> and <code>destructiveHint: false</code> are explicitly set — a single annotation is insufficient evidence of read-only behavior"],correct:0,explanation:"Tool annotations are self-reported metadata, not security boundaries. Trust decisions should be based on the server's overall trustworthiness."},

/* --- 2.5 Built-in tools --- */
{domain:"d2",task:"2.5 Built-in tools",question:"To search file CONTENTS for all callers of a function, the right built-in tool is:",options:["<code>Grep</code> — searches file contents by text or regex pattern; the correct choice for locating all references to a function name across a codebase","<code>Glob</code> — pattern-matches file paths by name, useful when the function name corresponds to a file name pattern, but does not search file contents","<code>Read</code> — loads complete file contents so the model can scan the text manually, but requires reading every file individually rather than searching across all at once","<code>Edit</code> — searches for a target string as part of its anchor-based replacement mechanism, not as a standalone content search tool"],correct:0,explanation:"Grep searches file contents by pattern — it's the correct tool for finding all callsites of a function across a codebase. Glob matches file names, not contents. Read loads individual files; it can't search across the codebase in one operation. Edit uses text matching as part of a replacement workflow, not as a search operation."},
{domain:"d2",task:"2.5 Built-in tools",question:"To find all files matching a NAME pattern like <code>**/*.test.tsx</code>, you use:",options:["<code>Glob</code> — matches files by path/name pattern using glob syntax; the right tool when you need a file list by name rather than by content","<code>Grep</code> — while primarily a content-search tool, it can filter by filename extension with <code>--include</code> flags, but returns matching lines rather than a clean file list","<code>Read</code> on the directory — this lists immediate children but does not recursively match glob patterns across subdirectories","<code>Bash</code> with shell glob expansion — can enumerate files recursively but requires constructing the correct find/ls command rather than using a dedicated path-pattern tool"],correct:0,explanation:"Glob is purpose-built for matching file paths by name pattern — the right tool for \"give me all files matching **/*.test.tsx\". Grep searches contents and produces line matches, not a file list. Read on a directory lists only immediate children. Bash can do this with find/ls but requires manual shell command construction."},
{domain:"d2",task:"2.5 Built-in tools",question:"Your <code>Edit</code> call fails because the target text appears multiple times in the file. What is the recommended fallback?",options:["<code>Read</code> to load the full file, then <code>Write</code> the modified contents","Retry <code>Edit</code> with additional surrounding context lines to make the anchor text unique within the file","Switch to <code>Bash sed -i</code> with an explicit line-number range to scope the replacement to the correct occurrence","Use <code>Glob</code> to find all files containing similar code, then batch the edit across all matching files rather than targeting one"],correct:0,explanation:"When <code>Edit</code> can't find a unique anchor, the reliable fallback is Read + Write."},
{domain:"d2",task:"2.5 Built-in tools",question:"You want to map an unfamiliar codebase efficiently. The recommended starting approach is:",options:["Use <code>Grep</code> to find entry points, then <code>Read</code> to follow imports — build understanding incrementally","Use <code>Read</code> on every directory starting from the root, loading all file listings before making any <code>Grep</code> calls to trace relationships","Run <code>Bash find . -type f -name '*.py' | sort</code> to enumerate all source files, then read each one alphabetically for systematic coverage","Skip codebase exploration entirely and begin directly with <code>Edit</code> calls — the model's training on common project patterns fills in missing context adequately"],correct:0,explanation:"Incremental exploration with Grep + Read is the recommended pattern. Reading everything upfront wastes context and isn't necessary."},
{domain:"d2",task:"2.5 Built-in tools",question:"For targeted modifications when the surrounding text is unique, the right tool is:",options:["<code>Edit</code> — performs exact string replacement anchored to unique surrounding context; the right choice for isolated changes when the target text appears only once","<code>Write</code> — replaces entire file contents; appropriate for complete rewrites, but unnecessarily destructive for small targeted changes","<code>Bash</code> with <code>sed</code> — supports in-place substitution, but requires careful regex escaping and doesn't provide the same precision as anchor-based replacement for unique context","<code>Read</code> the file first to verify the surrounding text is actually unique, then write a separate Edit call — correct approach but describes the verification step, not the modification tool itself"],correct:0,explanation:"Edit is the correct tool for targeted modifications — it matches the exact old string (using surrounding context as an anchor) and replaces it precisely. Write replaces the whole file, which is heavy-handed for a small change. Sed requires regex escaping and lacks the anchor-based uniqueness guarantee. Reading first is good practice but describes verification, not the modification tool."},
{domain:"d2",task:"2.5 Built-in tools",question:"You want to trace usage of a function across wrapper modules. Best approach?",options:["Identify all exported names with <code>Grep</code> (or read the module exports), then <code>Grep</code> for each name across the codebase","Use <code>Read</code> on every source file to load all content into context, then ask the model to identify call sites in the accumulated text","Use <code>Glob</code> with a filename regex to find files likely to contain the function by name, then manually inspect each identified file","Run <code>Bash find . -name '*.py' | xargs grep -n 'function_name'</code> to get line-numbered results for immediate navigation"],correct:0,explanation:"Wrapper modules re-export names, so you first identify the exports, then trace each across the codebase with <code>Grep</code>."},
{domain:"d2",task:"2.5 Built-in tools",question:"Which built-in tool is appropriate for full-file replacement?",options:["<code>Write</code> (often used together with <code>Read</code> as a fallback when <code>Edit</code> can't find unique anchor text)","<code>Edit</code> — when the replacement spans the entire file content, <code>Edit</code>'s anchor matching covers the full file in a single targeted call","<code>Grep</code> — it identifies all occurrences that need updating, after which a follow-up script handles the actual file modification","<code>Glob</code> — it finds files by name pattern and can be combined with a write callback to update all matching files in a single batch operation"],correct:0,explanation:"<code>Write</code> replaces a file's contents. Combined with <code>Read</code>, it's the standard fallback when <code>Edit</code> isn't viable."},
{domain:"d2",task:"2.5 Built-in tools",question:"Which sequence is INCORRECT?",options:["<code>Glob</code> for file contents; <code>Grep</code> for file names — this reversal is wrong: Grep searches file CONTENTS by pattern; Glob matches file NAMES/paths","<code>Grep</code> searches within file contents using regex patterns; <code>Glob</code> matches files by name pattern — correct assignment of each tool's purpose","<code>Edit</code> performs anchor-based targeted replacement within a file; <code>Write</code> replaces the entire file — correct distinction between targeted and full-file operations","<code>Read</code> loads the complete contents of a file into the model's context for inspection — correct description of the Read tool's function"],correct:0,explanation:"The incorrect sequence is using Glob for content search and Grep for filename matching — these are reversed. Grep searches file contents by text/regex pattern (the right tool for finding all callers of a function). Glob matches file paths by name pattern (the right tool for finding files by extension or path structure). Options B, C, and D correctly describe each tool's purpose."},
{domain:"d2",task:"2.5 Built-in tools",question:"You need to run shell commands (e.g., build, test). The built-in tool is:",options:["<code>Bash</code> — executes arbitrary shell commands, build scripts, test runners, and CLI tools; the right tool when you need to interact with the system environment","<code>Edit</code> — modifies shell scripts or Makefiles in the file system, but does not execute them; changes take effect only when the scripts are run separately","<code>Glob</code> — enumerates build targets or test runner scripts by filename pattern, but provides only a file list rather than executing any commands","<code>Read</code> — loads build configuration files like <code>package.json</code> or <code>Makefile</code> so the model can inspect their contents, but does not run any of the commands they define"],correct:0,explanation:"Bash is the tool for executing shell commands — it invokes the system shell, runs build scripts, test commands, and CLIs. Edit modifies files but doesn't execute them. Glob finds files by name pattern but doesn't run anything. Read loads file contents for inspection only."},
{domain:"d2",task:"2.5 Built-in tools",question:"A junior engineer reads every file in a 5000-file repo before writing code. What's the correct critique?",options:["This is inefficient — incremental exploration (Grep for entry points, then Read to follow imports) is the recommended pattern","Exhaustive upfront reading is the correct pattern for unfamiliar codebases — full context enables better design decisions than incremental discovery","They should have used <code>Glob</code> to list all files first, then used <code>Read</code> on each one in batches to remain within context window limits","They should have used the Task tool to spawn a reading subagent"],correct:0,explanation:"Exhaustive upfront reading wastes context and time. Incremental exploration is the recommended approach."},

/* ============================================================
   DOMAIN 3 — Claude Code Configuration & Workflows (20%)
   ============================================================ */

/* --- 3.1 CLAUDE.md hierarchy --- */
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"A new team member doesn't get the team's coding standards even after cloning the repo. You discover the standards are in <code>~/.claude/CLAUDE.md</code>. What is the fix?",options:["Move team-shared instructions to project-level (<code>.claude/CLAUDE.md</code> or root <code>CLAUDE.md</code>) so they're version-controlled and shared","Document the steps for new team members to copy the user-level file from a canonical location in the team wiki during onboarding","Move the standards to a user-level configuration canonical across the organization by enforcing it in the developer environment setup script","Embed the standards in a hidden shell rc file (<code>.zshrc</code> or <code>.bashrc</code>) that Claude Code reads automatically at startup"],correct:0,explanation:"User-level <code>~/.claude/CLAUDE.md</code> applies only to that user. Team-shared instructions must live in project-level config so they're available to anyone who clones the repo."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"Which CLAUDE.md scope is shared via version control with all team members?",options:["Project-level (<code>.claude/CLAUDE.md</code> or root <code>CLAUDE.md</code>) — these files live in the repository and are committed to version control, making them automatically available to every contributor","User-level (<code>~/.claude/CLAUDE.md</code>) — this file lives on each developer's local machine and is never committed; changes are not propagated to teammates","Subdirectory-level (<code>backend/CLAUDE.md</code>) — while these are in the repository, they apply only to sessions started within that subdirectory rather than to the whole team's workflow","Enterprise-level CLAUDE.md configured in the organization's admin console — no such scope exists; Claude Code CLAUDE.md scopes are project, user, and subdirectory"],correct:0,explanation:"Project-level CLAUDE.md (at the repo root or in .claude/) is version-controlled with the source code and shared automatically with all team members. User-level is local-only. Subdirectory files are version-controlled but scoped to specific paths. There is no enterprise admin console scope."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"Your monolithic <code>CLAUDE.md</code> has grown unmanageable. What is the recommended way to break it up while keeping rules organized?",options:["Use <code>@import</code> syntax to reference external files, or split into topic-specific files in <code>.claude/rules/</code>","Comment out sections that apply to less common tasks, toggling them back in when needed — effectively making CLAUDE.md a self-documenting toggle file","Compress the file and reference the compressed version from a root-level CLAUDE.md stub, relying on Claude Code's decompression support","Move all conventions to user-level <code>~/.claude/CLAUDE.md</code> so the project file stays clean and personal standards live in the personal scope"],correct:0,explanation:"<code>@import</code> and <code>.claude/rules/</code> are the recommended ways to modularize. <code>.claude/rules/</code> supports YAML frontmatter for path-scoped activation."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"A user wants to verify which memory files are loaded for their current session. The command is:",options:["<code>/memory</code> — displays the list of CLAUDE.md files and memory files currently loaded in the session context","<code>/compact</code> — compresses the current conversation to save context space, but does not display loaded memory files","<code>/clear</code> — resets the current conversation history and unloads all context, rather than displaying what's loaded","<code>/reload</code> — forces the session to re-read CLAUDE.md files from disk to pick up recent changes, but does not display the loaded file list"],correct:0,explanation:"/memory shows which instruction and memory files are currently in the model's context — the correct command for diagnosing what's loaded. /compact summarizes history to save tokens. /clear wipes the conversation. /reload refreshes file-based instructions from disk."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"You want to apply different conventions to your <code>terraform/</code> and <code>src/api/</code> areas without bloating the main CLAUDE.md. Best mechanism?",options:["Create files in <code>.claude/rules/</code> with YAML frontmatter <code>paths</code> globs (e.g., <code>paths: [\"terraform/**/*\"]</code>) so they load only when editing matching files","Add conditional sections to the root CLAUDE.md using comment delimiters, trusting the model to apply the relevant section based on which file is being edited","Place a separate CLAUDE.md file directly inside each subdirectory so conventions are co-located with the code they govern","Create a dedicated MCP server for each code area that exposes conventions as resource endpoints, enabling dynamic convention retrieval"],correct:0,explanation:"Path-scoped rules in <code>.claude/rules/</code> load only when matching files are being edited — reducing irrelevant context."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"Which is true about <code>@import</code> in CLAUDE.md?",options:["@import references an external file — the referenced content is loaded into context; it keeps the main CLAUDE.md concise while allowing modular organization of instructions","@import embeds the content of the referenced file inline at the import statement location during authoring — @import is a runtime reference, not a build-time embedding","@import only works for files within the same directory as the CLAUDE.md that contains the import statement — @import can reference files at any path within the repository","@import is processed by the Claude API during request handling, adding the referenced file's tokens to the system prompt — @import is processed by Claude Code, not the Anthropic API"],correct:0,explanation:"@import is a runtime reference: when Claude Code loads the CLAUDE.md, it reads the referenced file and includes its content in context. It's not inline embedding at authoring time. @import can reference files at paths beyond the current directory. Processing happens in Claude Code, not in the Anthropic API."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"For consistent behavior across team sessions, the safest place for shared coding standards is:",options:["Project-level <code>CLAUDE.md</code> in version control — committed alongside the source code, it's automatically picked up by every team member's session and changes are tracked via git","User-level <code>~/.claude/CLAUDE.md</code> on each developer's machine — this requires every team member to manually keep their personal file in sync with the shared standards","A shared network drive that Claude Code reads at startup — Claude Code doesn't read from external network paths; context comes from the local filesystem hierarchy","The team's CI/CD environment variables — environment variables provide execution context but are not the mechanism for delivering coding standards to Claude Code sessions"],correct:0,explanation:"Project-level CLAUDE.md is version-controlled with the codebase — every team member automatically inherits updates when they pull, and changes are tracked in git history. User-level files require manual synchronization per developer. Claude Code doesn't read from network drives or use environment variables for coding standards."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"You're seeing inconsistent code behavior across sessions, suspecting different memory files are loaded. First step:",options:["Run <code>/memory</code> to see which CLAUDE.md files and memory files are currently loaded in the session context","Delete all CLAUDE.md files in the project and recreate them — destructive; if the issue is a stale or conflicting file, this removes all project context, not just the problem","Check <code>~/.claude/CLAUDE.md</code> first — while user-level config can cause cross-session inconsistencies, starting with /memory shows ALL loaded files, not just user-level ones","Open a fresh session and compare behavior — useful for confirming the problem is session-specific, but doesn't reveal which files are causing the inconsistency"],correct:0,explanation:"/memory shows the complete list of loaded instruction files for the current session — it's the diagnostic first step. Deleting files is destructive without knowing which one is the problem. Checking only user-level config misses project and subdirectory files. A fresh session confirms the symptom but doesn't diagnose the cause."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"For conventions that span MANY directories (e.g., all test files <code>**/*.test.tsx</code>), the right CLAUDE.md approach is:",options:["<code>.claude/rules/</code> with YAML frontmatter <code>paths</code> glob — a dedicated rules file with a glob pattern loads only when the agent touches matching files, keeping other contexts clean","A single root-level <code>CLAUDE.md</code> with all conventions inline — this always loads into every session regardless of which files are being edited, consuming context even when the conventions are irrelevant","Separate <code>CLAUDE.md</code> files in each test directory — requires duplicating the same conventions N times, creating drift when standards change","A user-level <code>~/.claude/CLAUDE.md</code> with the test conventions — user-level config applies globally to all projects, polluting sessions for projects that don't share these conventions"],correct:0,explanation:".claude/rules/ with a paths glob targets the convention precisely — it loads only when matching files are in context, avoiding unnecessary token usage on unrelated tasks. Root-level CLAUDE.md always loads. Per-directory duplication causes drift. User-level config bleeds across all projects."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"What's the practical difference between using @import vs. .claude/rules/ to organize CLAUDE.md content?",options:["@import always loads its referenced content; .claude/rules/ can be path-scoped via YAML frontmatter to load conditionally","They are functionally identical — both load the referenced file's content into the active context; the choice between them is purely organizational preference","<code>@import</code> is a planned future feature; currently, all modular CLAUDE.md organization must be done using <code>.claude/rules/</code> directory structure","<code>@import</code> is evaluated at parse time (static inclusion), while <code>.claude/rules/</code> files are evaluated at prompt construction time (dynamic loading)"],correct:0,explanation:"@import always loads its referenced content unconditionally; .claude/rules/ files can be conditionally activated via YAML frontmatter path globs — loading only when editing matching files. B claims they are identical, which misses the conditional-loading advantage. C incorrectly states @import is a future feature. D's parse-time vs. prompt-time distinction is not how either mechanism works."},

/* --- 3.2 Slash commands & skills --- */
{domain:"d3",task:"3.2 Slash commands & skills",question:"You want to create a <code>/review</code> slash command that runs your team's standard code review checklist. Where does the command file go?",options:["In the <code>.claude/commands/</code> directory in the project repository — project-scoped commands are version-controlled and automatically available to every team member","In <code>~/.claude/commands/</code> on your personal machine — this creates a user-scoped command visible only to you; teammates must create their own copies","In a <code>commands:</code> block within the root <code>CLAUDE.md</code> file — CLAUDE.md defines instructions and @imports, not executable slash command definitions","As a YAML entry in <code>.mcp.json</code> under a <code>commands</code> key — .mcp.json registers MCP server configurations, not Claude Code slash commands"],correct:0,explanation:".claude/commands/ is the project-level commands directory — files here are committed to version control and shared across the team. ~/.claude/commands/ is personal-only. CLAUDE.md manages instructions, not command files. .mcp.json registers MCP servers, not slash commands."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"A personal slash command that you don't want to share belongs in:",options:["<code>~/.claude/commands/</code> — the user-level commands directory; files here are local to your machine and never version-controlled","<code>.claude/commands/</code> in the project root — this creates a project-scoped command that is committed to version control and shared with all teammates who clone the repository","<code>CLAUDE.md</code> at the project root — custom slash commands can be referenced in CLAUDE.md but the command file itself must live in a commands directory, not in an instruction file","An environment variable like <code>CLAUDE_COMMAND_REVIEW</code> — Claude Code reads commands from directory-based files, not from environment variables"],correct:0,explanation:"~/.claude/commands/ is the user-level directory for personal slash commands — it's local to your machine and never committed. .claude/commands/ in the project root is version-controlled and shared with teammates. CLAUDE.md manages instructions, not command file storage. Environment variables don't register slash commands."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"You're writing a Claude Code skill that produces verbose codebase analysis. To prevent the analysis from polluting the main session context, use:",options:["<code>context: fork</code> in the SKILL.md frontmatter — runs the skill in an isolated sub-agent","<code>context: main</code> in the frontmatter — runs the skill inline in the current session, retaining all verbose output in the main conversation context","A separate MCP server that the skill delegates to, isolating the verbose analysis behind a tool boundary that returns only structured results","Run the skill during plan mode, which automatically compacts verbose discovery output before it enters the main session context"],correct:0,explanation:"<code>context: fork</code> runs a skill in an isolated sub-agent, returning a summary rather than dumping verbose output into the main session."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"To restrict which tools a skill can use during execution, configure:",options:["<code>allowed-tools</code> in the SKILL.md frontmatter — this explicitly lists which tools the skill is permitted to invoke, enforcing least privilege during execution","<code>tool_choice: \"none\"</code> in the skill's execution context — this would prevent all tool calls, not selectively restrict to a specific subset","A guard prompt at the start of the skill body listing forbidden tools — prompt-based tool restrictions are probabilistic; the model may still call unlisted tools","The project-level <code>.mcp.json</code> by omitting the tools you want to exclude — this controls MCP server registration, not per-skill tool access within Claude Code"],correct:0,explanation:"allowed-tools in SKILL.md frontmatter is the supported mechanism for scoping a skill's tool access — it deterministically restricts which tools the skill can invoke. tool_choice: \"none\" disables all tools. Prompt-based restrictions are probabilistic. .mcp.json manages MCP server registration, not skill-level tool permissions."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"To prompt the developer for parameters when invoking a skill without arguments, configure:",options:["<code>argument-hint</code> in the SKILL.md frontmatter — this string is displayed to the developer as a prompt when the skill is invoked without arguments","A required parameter in the skill's YAML schema with <code>required: true</code> — skill frontmatter uses <code>argument-hint</code> for prompting, not a required-field schema","A guard clause at the top of the skill body that checks for missing arguments and outputs usage instructions — this is an output-based pattern, not the frontmatter mechanism for prompting","The <code>prompt: interactive</code> frontmatter flag, which activates an interactive argument-gathering dialog before running the skill body"],correct:0,explanation:"argument-hint in the SKILL.md frontmatter is the supported mechanism for displaying a prompt to the developer when a skill is invoked without arguments. A required: true schema field describes parameter requirements but doesn't display a prompt. Guard clauses produce output after invocation, not before. prompt: interactive is not a valid frontmatter flag."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"Which is the right choice between skills and CLAUDE.md for an universally applicable coding standard?",options:["CLAUDE.md — for always-loaded universal standards that should apply to every session without explicit invocation","A skill with <code>context: main</code> — skills require explicit invocation via slash command; a coding standard that must always apply cannot rely on developers remembering to invoke it","A skill with <code>context: fork</code> — forked skills run in isolated sessions, which means the standard wouldn't affect the main working session at all","An MCP server that responds to a <code>get_standards</code> tool call — this makes the standard opt-in per tool call rather than passively enforced across all sessions"],correct:0,explanation:"CLAUDE.md is the right vehicle for universal standards — it loads automatically on every session without requiring explicit invocation. Skills must be triggered by slash command, so critical standards that must always apply shouldn't depend on developers remembering to run them. Forked skills execute in isolation. MCP tools are opt-in per call."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"A teammate creates a personal variant of a project skill. To avoid affecting other contributors, where should it go?",options:["<code>~/.claude/skills/</code> with a different name — the user-level skills directory is local to their machine and never committed, so it won't affect other team members","<code>.claude/skills/</code> in the project root with a <code>personal: true</code> frontmatter flag — project-level skills are committed to version control regardless of frontmatter flags","A new branch of the repository — while branching isolates changes during development, merging or PR review exposes the personal variant to the whole team","The same <code>.claude/skills/</code> location with a modified name like <code>my-review.skill.md</code> — project-level skills are version-controlled, so any file in this directory will be shared with all contributors"],correct:0,explanation:"~/.claude/skills/ is the user-level directory that exists only on the developer's machine and is never committed to version control. Any file in .claude/skills/ (project-level) is committed and shared, regardless of name or frontmatter. Branches isolate during development but not permanently."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"A skill that brainstorms multiple architectural alternatives should run with which frontmatter setting?",options:["<code>context: fork</code> — runs the skill in an isolated copy of the session so brainstorm output and generated alternatives don't pollute the main conversation context","<code>context: main</code> — runs the brainstorm inline in the current session, with all generated alternatives and reasoning added directly to the main context window","No frontmatter context setting — skills default to <code>context: main</code>, so brainstorm results automatically accumulate in the current session unless overridden","An external MCP server that runs brainstorming as a separate service, returning only the final recommendation through a tool call — a valid architecture but heavier than using a forked skill"],correct:0,explanation:"context: fork isolates the brainstorming session so that exploratory output — intermediate alternatives, discarded ideas, verbose reasoning — doesn't contaminate the main session's context. context: main adds all that noise directly to the working conversation. The default is main, not fork."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"Which best describes when to use a skill vs. CLAUDE.md?",options:["Skills = on-demand task-specific workflows; CLAUDE.md = always-loaded universal standards","Skills define persistent project-wide conventions loaded in every session; CLAUDE.md defines on-demand task execution templates invoked by the developer","Use CLAUDE.md for repeatable developer workflows like code review or deployment; use skills to define coding standards that should apply in every context window","Skills and CLAUDE.md serve the same purpose — the choice depends on whether you prefer YAML frontmatter (skills) or plain Markdown (CLAUDE.md)"],correct:0,explanation:"Skills are on-demand workflows invoked explicitly (e.g., <code>/security-review</code>); CLAUDE.md is always-loaded universal context providing persistent standards and conventions. B and C invert the roles. D incorrectly treats them as interchangeable with a format preference."},
{domain:"d3",task:"3.2 Slash commands & skills",question:"You want a skill to write files but never run destructive commands. The right approach is:",options:["Restrict <code>allowed-tools</code> in SKILL.md frontmatter to file operations only","Add a system prompt rule inside the skill's SKILL.md body that instructs the model to avoid <code>Bash</code> calls that could delete or overwrite files","Run the skill under a separate OS user account with filesystem permissions that prevent writes to protected directories","Disable the <code>Bash</code> tool globally in the project's CLAUDE.md so no skill or session can run shell commands that might cause destructive side effects"],correct:0,explanation:"<code>allowed-tools</code> in frontmatter explicitly restricts tool access during skill execution — the right control for limiting destructive operations."},

/* --- 3.3 Path-specific rules --- */
{domain:"d3",task:"3.3 Path-specific rules",question:"Your codebase has distinct areas with different coding conventions: React components use functional style with hooks, API handlers use async/await with specific error handling, and database models follow a repository pattern. Test files are spread throughout the codebase alongside the code they test (e.g., <code>Button.test.tsx</code> next to <code>Button.tsx</code>), and you want all tests to follow the same conventions regardless of location. What's the most maintainable way to ensure Claude automatically applies the correct conventions when generating code?",options:["Create rule files in <code>.claude/rules/</code> with YAML frontmatter specifying glob patterns to conditionally apply conventions based on file paths","Consolidate all conventions in the root <code>CLAUDE.md</code> file under headers for each area, relying on Claude to infer which section applies","Create skills in <code>.claude/skills/</code> for each code type that include the relevant conventions in their SKILL.md files","Place a separate <code>CLAUDE.md</code> file in each subdirectory containing that area's specific conventions"],correct:0,explanation:"This is question 6 from the official guide. <code>.claude/rules/</code> with glob patterns automatically applies conventions by file path regardless of directory location — essential for test files spread throughout. Inference is unreliable; skills require manual invocation; subdirectory CLAUDE.md can't handle files spread across many directories."},
{domain:"d3",task:"3.3 Path-specific rules",question:"For files spread across many directories that share conventions (e.g., all test files <code>**/*.test.tsx</code>), the recommended approach is:",options:["<code>.claude/rules/</code> with YAML frontmatter <code>paths</code> globs — a single rule file targets all matching files without duplicating content or loading the convention when unrelated files are in context","Embed the convention in the root <code>CLAUDE.md</code> — this works but loads the test convention into every session, even when no test files are being touched","Create a <code>CLAUDE.md</code> in each directory that contains test files — duplicates the same content in multiple places, creating maintenance drift when the convention changes","Use user-level <code>~/.claude/CLAUDE.md</code> to make the convention global — applies it across all projects on the machine, not just this one repository"],correct:0,explanation:".claude/rules/ with a paths glob is path-scoped loading: the convention loads only when matching files are in context, avoiding unnecessary context consumption. Root CLAUDE.md always loads regardless. Per-directory files duplicate content. User-level config is too broad."},
{domain:"d3",task:"3.3 Path-specific rules",question:"A rule file in <code>.claude/rules/</code> uses YAML frontmatter <code>paths: [\"terraform/**/*\"]</code>. This means:",options:["The rule loads only when editing files matching the glob, reducing irrelevant context","The rule is loaded for every session but the model applies it conditionally, checking which file is open before following the convention","The rule locks those files against editing — Claude Code enforces read-only access to files matching the glob as a safety mechanism","The YAML frontmatter <code>paths</code> key is not a recognized field; the rule loads unconditionally and the glob is ignored"],correct:0,explanation:"Path-scoped rules activate only on matching files, keeping context lean and avoiding irrelevant convention loads."},
{domain:"d3",task:"3.3 Path-specific rules",question:"Why is <code>.claude/rules/</code> preferred over subdirectory CLAUDE.md for conventions like 'all <code>.test.tsx</code> files use Jest with React Testing Library'?",options:["Glob-pattern rules can apply to files by type regardless of where they live; subdirectory CLAUDE.md is directory-bound","Subdirectory CLAUDE.md requires an explicit <code>@import</code> statement in the root CLAUDE.md; <code>.claude/rules/</code> files are auto-discovered at session startup","Both mechanisms are equivalent for file-type conventions — the decision is stylistic and depends on whether the team prefers co-location or centralized rule management","Subdirectory CLAUDE.md files load for all files in the directory regardless of extension; <code>.claude/rules/</code> can additionally filter by file type using <code>paths</code> globs"],correct:0,explanation:"Glob-pattern rules in <code>.claude/rules/</code> can match files by type regardless of directory depth, while subdirectory CLAUDE.md applies to all files within that directory. D is the closest distractor — it's partially correct in saying subdirectory CLAUDE.md applies to the whole directory, but it misses that the key advantage is cross-directory type-matching, not just directory scope. B incorrectly implies <code>@import</code> is required."},
{domain:"d3",task:"3.3 Path-specific rules",question:"Which YAML frontmatter <code>paths</code> pattern would match all TypeScript source files under <code>src/</code> but exclude test files?",options:["<code>paths: [\"src/**/*.ts\", \"!src/**/*.test.ts\"]</code> — the glob matches all .ts files under src/ and the negation pattern excludes test files","<code>paths: [\"src/**/*.ts\"]</code> — this matches all .ts files including test files; there is no negation to exclude the test file pattern","<code>paths: [\"src/\"]</code> — a bare directory path is not a valid glob pattern for matching files; it would match the directory name, not files within it","<code>paths: [\"**/*.ts\", \"src/\"]</code> — combining a broad glob with a directory pattern creates an overly broad match that includes .ts files outside src/ and duplicates src/ matching"],correct:0,explanation:"The correct pattern uses a glob for source files combined with a negation pattern to exclude test files. A glob without the negation includes test files. A bare directory path doesn't match files. Combining an unrestricted glob with a directory entry is overly broad and doesn't achieve the exclusion."},
{domain:"d3",task:"3.3 Path-specific rules",question:"You want a single rule file to apply only when editing files in <code>src/api/</code>. The correct frontmatter is:",options:["<code>paths: [\"src/api/**/*\"]</code>","<code>paths: [\"src/api\"]</code>","<code>paths: [\"src/api/*\"]</code>","No <code>paths</code> field is needed"],correct:0,explanation:"<code>src/api/**/*</code> matches any file under <code>src/api/</code> at any depth. <code>src/api/*</code> only matches direct children."},
{domain:"d3",task:"3.3 Path-specific rules",scenario:"Your team uses a monorepo with a React frontend (<code>src/frontend/</code>), a Node.js API (<code>src/api/</code>), Terraform infrastructure code (<code>infrastructure/</code>), and database migration scripts (<code>db/migrations/</code>). Each area has different code style conventions and review standards. Multiple engineers, each with personal preferences, use Claude Code.",question:"What's a major drawback of putting conventions in a single huge root CLAUDE.md instead of <code>.claude/rules/</code>?",options:["The model carries irrelevant rules into every context, wasting tokens and diluting attention to what matters","Project-level CLAUDE.md has a size limit; files over 100 KB are silently truncated, causing conventions near the end of the file to be ignored","All conventions in one file are applied with equal precedence, making it impossible to override subdirectory-specific standards with more targeted rules","A monolithic CLAUDE.md prevents the team from using <code>@import</code> to split content across modular files, since <code>@import</code> only works in the top-level file"],correct:0,explanation:"A monolithic CLAUDE.md loads every rule into every context window, wasting tokens and diluting the model's attention to relevant conventions. Path-scoped rules in <code>.claude/rules/</code> load only when editing matching files. B describes a size limit that doesn't exist. C is incorrect — subdirectory CLAUDE.md files do override root rules. D is incorrect — @import works regardless of file structure."},
{domain:"d3",task:"3.3 Path-specific rules",question:"For Terraform files: which is the recommended frontmatter?",options:["<code>paths: [\"**/*.tf\", \"**/*.tfvars\"]</code> — matches both Terraform configuration files and variable files at any directory depth","<code>paths: [\"*.tf\"]</code> — matches only .tf files in the root directory; doesn't recurse into subdirectories like <code>modules/</code> or <code>environments/</code>","<code>paths: [\"terraform/\"]</code> — a bare directory name is not a valid file-matching glob; it would match the directory itself, not the files inside it","<code>paths: [\"**/*\"]</code> — matches every file in the repository, making this rule apply regardless of file type rather than targeting Terraform files specifically"],correct:0,explanation:"The recommended pattern covers both .tf and .tfvars extensions with recursive matching (**) to handle typical Terraform directory structures. *.tf without ** misses nested modules. A bare directory path doesn't match files. **/* is too broad and applies the rule to all files."},
{domain:"d3",task:"3.3 Path-specific rules",question:"Why is path-scoped loading more efficient than always-loaded rules?",options:["Rules load only when matching files are in context — irrelevant conventions don't consume token budget when the agent is working on unrelated files","Parse time is reduced because Claude Code skips path-scoped files that don't match — the efficiency gain is context token savings, not reduced parse time; all rule files are still discovered","The CLAUDE.md hierarchy check is bypassed for path-scoped rules — all CLAUDE.md files go through the same hierarchy discovery regardless of path scoping","Priority ordering gives path-scoped rules precedence over always-loaded rules — rule priority is determined by scope specificity in the hierarchy, not by whether a rule is path-scoped"],correct:0,explanation:"Path-scoped rules are efficient because they load only when relevant files are in context — they don't consume token budget on sessions where the agent is working outside the scope. The efficiency is about context token savings, not parse time, hierarchy bypassing, or priority ordering."},
{domain:"d3",task:"3.3 Path-specific rules",question:"In <code>.claude/rules/</code>, what does the YAML frontmatter <code>paths</code> field control?",options:["The glob patterns that determine which file edits trigger loading of the rule","The access control list for the rule file — specifying which team members or roles are permitted to have the rule loaded in their sessions","The encryption key used to protect sensitive conventions from being read by unauthorized Claude Code sessions","The rule's priority in the loading order — higher index values take precedence when multiple rules provide conflicting guidance for the same file"],correct:0,explanation:"The <code>paths</code> field is a list of glob patterns that determine when the rule is active."},

/* --- 3.4 Plan mode vs direct execution --- */
{domain:"d3",task:"3.4 Plan vs direct",question:"You've been assigned to restructure the team's monolithic application into microservices. This will involve changes across dozens of files and requires decisions about service boundaries and module dependencies. Which approach should you take?",options:["Enter plan mode to explore the codebase, understand dependencies, and design an implementation approach before making changes","Start with direct execution and make changes incrementally, letting the implementation reveal the natural service boundaries","Use direct execution with comprehensive upfront instructions detailing exactly how each service should be structured","Begin in direct execution mode and only switch to plan mode if you encounter unexpected complexity during implementation"],correct:0,explanation:"This is question 5 from the official guide. Plan mode is designed for complex tasks involving large-scale changes, multiple valid approaches, and architectural decisions. Direct execution risks costly rework. Comprehensive upfront instructions assume you already know the right structure, which isn't true for unfamiliar codebases."},
{domain:"d3",task:"3.4 Plan vs direct",question:"For a single-file bug fix with a clear stack trace, the recommended Claude Code mode is:",options:["Direct execution — the approach is already clear from the stack trace; no investigation phase is needed before making the targeted change","Plan mode — to verify the proposed fix doesn't introduce regressions in other files or violate architectural constraints before any code changes","A forked session — to preserve the current main session state while testing the fix in isolation before merging changes back","Plan mode followed immediately by direct execution — the two-phase investigation and implementation workflow for well-scoped tasks"],correct:0,explanation:"Direct execution is correct for single-file, well-scoped changes where the stack trace provides a clear diagnosis — no planning phase is needed. Plan mode is valuable when the approach is uncertain or the change touches many files. A forked session is for divergent exploration, not simple bug fixes. Plan→direct together is appropriate for more complex changes, not a clear single-file fix."},
{domain:"d3",task:"3.4 Plan vs direct",question:"Which task is best suited for plan mode?",options:["Microservice restructuring across 45+ files — high complexity, broad impact, and multiple viable approaches make plan mode essential before committing to changes","Adding a null check to a single function — well-scoped, unambiguous, and confined to one location; direct execution is appropriate","Renaming a local variable within a method — single-file, atomic, no architectural decisions needed; no planning value","Fixing a typo in a string constant — trivial scope with zero ambiguity; plan mode would add unnecessary overhead with no benefit"],correct:0,explanation:"Microservice restructuring benefits from plan mode: the scope is broad, the impact is high, and the approach may not be obvious upfront. Null checks, variable renames, and typo fixes are all bounded, unambiguous tasks where plan mode adds overhead without benefit."},
{domain:"d3",task:"3.4 Plan vs direct",question:"During plan mode, what is the purpose of the Explore subagent?",options:["To isolate verbose discovery output and return summaries to preserve main conversation context","To draft and validate the proposed implementation plan in detail before the developer approves it for direct execution","To run the test suite in the background while plan mode generates the architectural overview, reducing the total round-trip time for the planning phase","To search the internet for documentation about unfamiliar APIs or libraries referenced in the codebase being analyzed"],correct:0,explanation:"The Explore subagent isolates verbose discovery output (file reads, dependency tracing, codebase scanning) and returns summaries to the main session. This preserves main context for high-level design reasoning. B describes a code-generation phase that happens after plan mode, not within it. C incorrectly combines Explore with test execution. D misidentifies Explore as a documentation search tool."},
{domain:"d3",task:"3.4 Plan vs direct",question:"A library migration touches 45 files and has two viable integration strategies. The first step is:",options:["Plan mode — to investigate the codebase, evaluate both strategies, and decide on the approach before any files are modified","Direct execution on a sample file — start with one file to see which approach works before committing to a strategy across all 45","Fork the session and run both strategies in parallel to compare outcomes — fork_session is for exploring divergent paths, not for deciding between strategies before acting","Ask the model to estimate which strategy is better from the description alone, then directly execute that strategy across all 45 files"],correct:0,explanation:"Plan mode is correct when you need to investigate the codebase and decide between strategies before committing to changes across 45 files — it prevents costly rework. Direct execution on a sample is reasonable but skips the strategy-evaluation phase. Forking is for parallel exploration, not upfront decision-making. Asking for an estimate without investigation risks choosing the wrong strategy."},
{domain:"d3",task:"3.4 Plan vs direct",question:"Best practice for plan-mode-to-direct-execution workflow:",options:["Use plan mode to investigate and decide; then switch to direct execution to implement the planned approach","Use a single mode for the entire task — switching between modes mid-task introduces context fragmentation that reduces the quality of the final implementation","Begin with direct execution and switch to plan mode only if you encounter unexpected complexity that requires stepping back to redesign the approach","Use plan mode for all tasks regardless of scope — the investigation overhead is always worthwhile to avoid rework on any code change"],correct:0,explanation:"Plan for investigation/design + direct execution for implementation is the recommended combined pattern."},
{domain:"d3",task:"3.4 Plan vs direct",question:"Plan mode primarily prevents which class of problem?",options:["Costly rework from committing to changes before the codebase structure and implications are understood","API timeout errors from long-running direct execution sessions that exceed request limits","Context window overflow from accumulating too many tool results during implementation","Model hallucinations about code that doesn't exist — plan mode verifies assumptions before any file reads occur"],correct:0,explanation:"Plan mode's primary purpose is preventing costly rework — it allows the agent to map the codebase and evaluate approaches before modifying any files. API timeouts are unrelated to mode. Context accumulation is managed by other mechanisms. Plan mode doesn't prevent hallucinations; it prevents premature changes."},
{domain:"d3",task:"3.4 Plan vs direct",question:"For a date validation conditional being added to one function, the right mode is:",options:["Direct execution — the scope is limited to one function in one file and the implementation is straightforward; no planning phase is needed","Plan mode — to confirm the validation logic is consistent with other date validations in the codebase before implementing","Plan mode followed by direct execution — use the planning phase to draft the conditional logic, then execute to insert it into the function","A forked session — to test the conditional logic without affecting the main session's file state"],correct:0,explanation:"Direct execution is correct for small, well-scoped additions like a single function's validation logic — the task is clear and bounded. Plan mode is valuable for changes with uncertain scope or multiple viable approaches, not routine additions. Plan→direct adds unnecessary overhead for a known simple change. Forking is for divergent exploration."},
{domain:"d3",task:"3.4 Plan vs direct",question:"You're choosing between integration approaches with different infrastructure requirements. Why is plan mode valuable here?",options:["It enables safe exploration of both approaches and comparison of trade-offs before any infrastructure changes are made or code is modified","It prevents Claude from consuming API tokens on implementation details before the approach is confirmed — plan mode uses fewer tokens than direct execution","It forces the model to produce a spec document before writing code, ensuring downstream engineers understand the rationale","It automatically forks the session so both approaches can be evaluated in parallel without affecting the main context"],correct:0,explanation:"Plan mode's core value is enabling exploration and comparison before commitment — especially important when approaches have different infrastructure requirements and the wrong choice is costly to reverse. Plan mode doesn't inherently use fewer tokens. It doesn't produce spec documents unless instructed. It doesn't automatically fork sessions."},
{domain:"d3",task:"3.4 Plan vs direct",question:"In a multi-phase task, how can you avoid filling the main context with verbose codebase exploration output?",options:["Spawn an Explore subagent that returns concise summaries to the main session — the subagent's verbose exploration stays in its isolated context; only the summary enters the main session","Use <code>/compact</code> after each exploration phase to summarize and compress accumulated context before starting the next phase","Write all exploration findings to a file and instruct the main session to read only the file rather than the raw tool output","Increase <code>max_tokens</code> on each exploration call so the model finishes exploration before the main session fills"],correct:0,explanation:"Spawning an Explore subagent isolates verbose exploration output in the subagent's context — only the summary it returns enters the main session. /compact summarizes after the fact but loses precision. Writing to files adds round-trips and doesn't prevent the main session from processing the exploration output. max_tokens controls output length, not context accumulation."},

/* --- 3.5 Iterative refinement --- */
{domain:"d3",task:"3.5 Iterative refinement",question:"Prose descriptions of a desired transformation produce inconsistent results across runs. What is usually the most effective improvement?",options:["Provide 2-3 concrete input/output examples in the prompt","Prepend a chain-of-thought reasoning section so the model explains its approach before transforming","Increase temperature to encourage broader exploration of possible output formats","Add a post-processing regex to normalize the output into the expected format"],correct:0,explanation:"Concrete I/O examples are the most reliable signal for transformations. Chain-of-thought (B) helps reasoning but doesn't fix format consistency. Higher temperature (C) increases variance, worsening consistency. Regex post-processing (D) is brittle and treats the symptom, not the cause."},
{domain:"d3",task:"3.5 Iterative refinement",question:"For improving generated code quality, the test-driven iteration pattern is:",options:["Write tests covering expected behavior, edge cases, and performance; share test failures with the model to guide improvement","Have the model write tests after implementation is complete — tests serve as a verification layer rather than a specification that guides iterative improvement","Skip tests during rapid iteration cycles and add them in a final consolidation pass once the implementation stabilizes","Always discard the current implementation on test failure and ask the model to regenerate from scratch with the test failures as additional context"],correct:0,explanation:"Test-driven iteration: tests first, then iterate by sharing failures. This gives the model concrete signals to correct against."},
{domain:"d3",task:"3.5 Iterative refinement",question:"The 'interview pattern' for unfamiliar domains means:",options:["Having Claude ask questions to surface design considerations (cache invalidation, failure modes) before implementing","Conducting a structured interview with a domain expert teammate to capture their knowledge before beginning the implementation session","Reading published developer interviews about the domain to gather implicit knowledge not captured in official documentation","Asking the model to generate a Frequently Asked Questions document about the domain before beginning, using that FAQ as context for implementation"],correct:0,explanation:"The interview pattern uses Claude's questions to surface design considerations the developer may not have anticipated."},
{domain:"d3",task:"3.5 Iterative refinement",question:"For multiple INTERACTING issues, the recommended approach is:",options:["Provide all issues in a single detailed message so the model can resolve them coherently","Address each issue in its own separate iteration, accepting that fixing one may require revisiting an earlier fix in the next turn","Ignore the interactions between issues on the first pass and apply a reconciliation prompt at the end to resolve any conflicts introduced","Assign a different Claude Code session to each issue, then merge the independent solutions and resolve conflicts in a final integration pass"],correct:0,explanation:"Interacting issues should be addressed together so fixes for one don't break another. Independent issues can be handled sequentially."},
{domain:"d3",task:"3.5 Iterative refinement",question:"For multiple INDEPENDENT issues, the recommended approach is:",options:["Address each in a separate, focused iteration — independent issues can cause cross-contamination when combined; isolating them also makes it easier to attribute regressions","Combine all issues into one comprehensive prompt and fix everything in a single pass — mixing independent issues increases context complexity and makes it harder to verify each fix individually","Fix the highest-severity issue first, then re-evaluate whether the others still exist — useful for dependent issues, but independent ones don't cascade and don't require re-evaluation after each fix","Open parallel sessions for each issue and merge the changes — parallel sessions risk conflicting edits to shared files; sequential isolation is the safer pattern"],correct:0,explanation:"Separate iterations for independent issues prevent cross-contamination and make each fix verifiable in isolation. Combining them into one prompt increases complexity and ambiguity. Re-evaluating after each fix makes sense for dependent issues, not independent ones. Parallel sessions risk merge conflicts."},
{domain:"d3",task:"3.5 Iterative refinement",question:"You're fixing edge case handling (e.g., null values in migration scripts). What's a high-leverage improvement?",options:["Provide specific test cases with example input and expected output for the edge case","Add a general instruction to 'be careful about edge cases' at the start of the prompt, reinforcing caution without specifying which cases to handle","Increase the model's temperature setting to encourage it to generate a broader range of solutions, including ones that handle unusual input combinations","Switch to a different Claude model tier with stronger code generation capabilities to handle the edge case logic more reliably"],correct:0,explanation:"Specific test cases with example I/O for the edge case are the most direct signal for correction."},
{domain:"d3",task:"3.5 Iterative refinement",question:"Why are concrete examples more effective than long prose for transformation tasks?",options:["Examples demonstrate the exact desired behavior; prose is interpreted inconsistently across runs","Long prose activates the model's general language reasoning pathway, while examples activate its pattern-matching capability — pattern matching produces more consistent transformations","Examples can be automatically validated by running the transformation and comparing output to the expected result, while prose correctness cannot be verified programmatically","The model interprets prose instructions through its training distribution, which may differ from your intended meaning; examples bypass this interpretation step entirely"],correct:0,explanation:"Examples demonstrate the exact desired behavior — they show the model what 'correct' looks like for specific inputs. Long prose is interpreted inconsistently because different phrasings carry different implications. B has a partial truth (pattern matching vs. reasoning) but mischaracterizes why. C describes a real engineering benefit of examples but not why they produce more consistent outputs. D is plausible but overstated — prose can be interpreted correctly."},
{domain:"d3",task:"3.5 Iterative refinement",question:"The recommended number of input/output examples for clarifying a transformation is typically:",options:["2–3 examples — enough to demonstrate the pattern without consuming excessive context tokens or over-constraining the model to specific formats","30 or more examples — the model needs a statistically significant sample to generalize correctly; fewer examples produce inconsistent transformations","A single carefully chosen example — one precise, representative example conveys the pattern efficiently and leaves the model flexibility to handle variation","No examples — clear prose instructions are sufficient; adding examples increases prompt size and may cause the model to overfit to example formats"],correct:0,explanation:"2–3 targeted examples strikes the right balance: enough to communicate the pattern clearly without wasting context or causing the model to overfit. 30+ examples are excessive for transformation guidance. A single example may be ambiguous about edge cases. No examples forces the model to rely entirely on prose, which is often less effective than showing the pattern."},

/* --- 3.6 CI/CD --- */
{domain:"d3",task:"3.6 CI/CD integration",scenario:"You are setting up an automated PR review pipeline. Every pull request triggers a Claude Code job that performs security and quality analysis. The CI runner has no terminal and must not hang waiting for user input. Findings should be posted as inline PR comments with location, severity, and suggested fix.",question:"Your pipeline script runs <code>claude \"Analyze this pull request for security issues\"</code> but the job hangs indefinitely. Logs indicate Claude Code is waiting for interactive input. What's the correct approach to run Claude Code in an automated pipeline?",options:["Add the <code>-p</code> flag: <code>claude -p \"Analyze this pull request for security issues\"</code>","Set the environment variable <code>CLAUDE_HEADLESS=true</code> before running","Redirect stdin from <code>/dev/null</code> — this prevents the process from reading interactive input but doesn't tell Claude Code to produce non-interactive output","Add the <code>--batch</code> flag — this flag is documented in some Claude Code versions but has different behavior from <code>-p</code> and does not guarantee non-interactive operation"],correct:0,explanation:"This is question 10 from the official guide. The <code>-p</code> (or <code>--print</code>) flag is the documented way to run Claude Code in non-interactive mode. It processes the prompt, outputs the result to stdout, and exits without waiting for user input. The other options reference non-existent features or Unix workarounds."},
{domain:"d3",task:"3.6 CI/CD integration",scenario:"You are setting up an automated PR review pipeline. Every pull request triggers a Claude Code job that performs security and quality analysis. The CI runner has no terminal and must not hang waiting for user input. Findings should be posted as inline PR comments with location, severity, and suggested fix.",question:"To enforce machine-parseable structured output for automated PR comments, the right CLI flag combination is:",options:["<code>--output-format json</code> together with <code>--json-schema</code> — these flags together instruct Claude Code to output a JSON object conforming to the provided schema","<code>--json</code> alone — this flag exists but only requests JSON formatting without a schema, so the output structure is not enforced","<code>--format structured --schema pr-comments.json</code> — not valid Claude Code CLI flags; the correct flags are <code>--output-format</code> and <code>--json-schema</code>","<code>--print</code> combined with a system prompt instruction to output JSON — the system prompt can request JSON but doesn't enforce schema compliance the way the CLI schema flag does"],correct:0,explanation:"--output-format json combined with --json-schema enforces schema-conformant structured output. --json alone requests JSON without schema enforcement. --format and --schema are not valid Claude Code flags. System prompt JSON requests are probabilistic; schema flags are enforced."},
{domain:"d3",task:"3.6 CI/CD integration",question:"Why is the same Claude session that generated code less effective at reviewing it?",options:["Session context isolation: a session that retains reasoning context from generation is less likely to question its own decisions","The generator session's sampling temperature is implicitly optimized for creation; a dedicated reviewer session uses a more analytical temperature for precise judgment","The generator session accumulates implicit reward signal from successful tool calls, making it systematically less likely to flag its own outputs as problematic","Claude Code's internal deduplication filter suppresses repeated observations across turns, preventing the generator from surfacing issues it already reasoned through during generation"],correct:0,explanation:"The generator session retains the reasoning context that produced the code — including the implicit justifications for every design decision. This makes it less likely to question its own choices. Independent review instances start without that anchoring context. B incorrectly implies temperature settings differ per use. C describes reward signals that don't apply to inference-time behavior. D describes a deduplication filter that doesn't exist."},
{domain:"d3",task:"3.6 CI/CD integration",question:"You re-run a CI review after new commits and want to avoid duplicating earlier findings as comments. Best approach?",options:["Include prior review findings in context and instruct Claude to report only new or still-unaddressed issues","Disable review comment creation after the initial PR scan and rely on the developer to manually check for new findings on subsequent runs","Manually audit the comment thread after each re-run and delete any comments that duplicate findings from the previous review pass","Reset the session context for each re-run so the model starts fresh, accepting duplicate comments as an acceptable trade-off for stateless simplicity"],correct:0,explanation:"Passing prior findings as context with explicit instructions to dedupe is the recommended pattern for incremental reviews."},
{domain:"d3",task:"3.6 CI/CD integration",question:"Test generation in CI keeps suggesting scenarios already covered. The recommended fix is:",options:["Provide existing test files in context so the agent can identify covered scenarios before generating new ones","Set the temperature to 0 to make generation more deterministic and reduce the chance of suggesting duplicate scenarios","Use a post-processing deduplication step to filter out scenarios that match existing test descriptions","Regenerate the test suite from scratch to ensure a coherent, non-redundant set of scenarios"],correct:0,explanation:"Providing existing test files in context gives the agent the information it needs to avoid duplication — it can see what's covered before generating. Temperature 0 reduces variance but doesn't prevent duplication if the model lacks context about existing tests. Post-processing deduplication is a fallback, not a root-cause fix. Regenerating from scratch discards valid existing tests."},
{domain:"d3",task:"3.6 CI/CD integration",question:"How does CLAUDE.md improve test generation quality in CI?",options:["By documenting testing standards, fixtures, and review criteria so the model can produce relevant, valuable tests","By disabling low-quality test suggestions — CLAUDE.md includes a test generation suppression flag that prevents Claude Code from producing tests below a confidence threshold","By storing the CI API keys in CLAUDE.md so Claude Code can authenticate with testing infrastructure without requiring environment variable configuration","By tracking which test scenarios were generated in previous CI runs and filtering them out of subsequent generation requests"],correct:0,explanation:"CLAUDE.md provides project context — standards, fixtures, criteria — that grounds CI-invoked Claude Code in your project conventions."},
{domain:"d3",task:"3.6 CI/CD integration",question:"For a non-interactive CI invocation, the most important CLI flag is:",options:["<code>-p</code> / <code>--print</code> — bypasses the interactive TUI and prints the model's response to stdout, which is required for CI pipelines that capture output programmatically","<code>--quiet</code> — reduces log verbosity and suppresses progress messages, but the process still requires a terminal for its interactive TUI","<code>--debug</code> — increases output verbosity with diagnostic information, making it unsuitable for clean CI log capture","<code>--no-input</code> — while semantically intuitive, this flag does not exist in Claude Code's CLI; non-interactive operation is controlled by <code>--print</code>"],correct:0,explanation:"--print / -p is the key flag for CI: it disables the interactive terminal UI and writes output to stdout, enabling programmatic output capture in pipelines. --quiet changes verbosity but doesn't switch to non-interactive mode. --debug adds noise unsuitable for CI logs. --no-input doesn't exist."},
{domain:"d3",task:"3.6 CI/CD integration",question:"For producing PR comments that include a location, issue, severity, and suggested fix, what's the right output strategy?",options:["Use <code>--output-format json</code> with <code>--json-schema</code> defining the fields; parse and post inline","Use plain text output, then post the entire response as a monolithic comment on the pull request","Use markdown output with a consistent heading structure, and rely on regex heuristics to extract field values","Stream text output and parse each token as it arrives to incrementally build the structured comment"],correct:0,explanation:"Structured JSON output with an enforced schema gives reliable, machine-parseable comments suitable for inline posting. Plain text (B) can't be parsed reliably. Regex heuristics (C) are brittle. Streaming token-by-token parsing (D) doesn't produce structured output."},

/* ============================================================
   DOMAIN 4 — Prompt Engineering & Structured Output (20%)
   ============================================================ */

/* --- 4.1 Explicit criteria --- */
{domain:"d4",task:"4.1 Explicit criteria",question:"Your review prompt says 'be conservative — only report high-confidence findings'. False positives remain high. What change will most effectively reduce false positives?",options:["Replace vague instructions with explicit categorical criteria defining which issues to report (bugs, security) and which to skip (minor style, local patterns)","Reinforce the instruction with emphasis: 'CRITICAL: be VERY conservative and only report the highest-confidence issues'","Add a few-shot example showing a conservative review to calibrate the level of caution expected","Lower the model's temperature to reduce variation in which issues get flagged"],correct:0,explanation:"Vague language like 'be conservative' doesn't define what to flag. Specific categorical criteria do. Emphasis (B) just amplifies vagueness. Few-shot examples of caution (C) still don't define categories. Temperature (D) controls randomness, not classification thresholds."},
{domain:"d4",task:"4.1 Explicit criteria",question:"Which review instruction is more likely to produce precise, actionable findings?",options:["'Flag comments only when the claimed behavior contradicts the actual code behavior' — a specific, testable criterion the model can apply consistently","'Check that comments are accurate' — too vague; 'accurate' is open to interpretation and produces inconsistent results depending on how the model interprets the standard","'Be thorough about comments' — a behavioral instruction without a specific criterion; thoroughness varies per model interpretation and produces no consistent standard","'Identify any comment-related issues' — the broadest possible framing; without defining what constitutes an issue, the model applies its own judgment inconsistently"],correct:0,explanation:"'Flag when the claimed behavior contradicts the actual code' is precise and testable — the model has a specific criterion to apply. 'Check for accuracy', 'be thorough', and 'identify issues' are increasingly vague formulations that rely on the model's interpretation of what counts as a problem, producing inconsistent results across runs."},
{domain:"d4",task:"4.1 Explicit criteria",question:"High false-positive rates in one category undermine trust in:",options:["All categories — high FP in one category erodes developer confidence in the whole review pipeline, causing them to dismiss even accurate findings from other categories","Just that one category — developers are highly calibrated reviewers who apply skepticism selectively and continue trusting well-performing categories independently","Exclusively the severity classification for that category — high FP suggests severity labeling is miscalibrated, not that the entire pipeline loses developer trust","Categories tuned in the same prompt-improvement session — co-tuned categories share a prompt history, but changes to one don't directly affect detection logic in others"],correct:0,explanation:"Trust erosion from high-FP categories is not contained — developers who encounter frequent false alarms apply skepticism to all findings, not just the noisy category. 'If the tool cried wolf on security, why trust the performance findings?' Options B, C, D all propose that skepticism stays isolated, which contradicts how developer trust degrades in practice."},
{domain:"d4",task:"4.1 Explicit criteria",question:"You have a category with persistently high false positives. While improving prompt specificity, what is the most important near-term action?",options:["Temporarily disable that category to stop eroding developer trust while you work on improving its precision","Raise the minimum confidence threshold for only that category, routing its findings to a 'pending' queue rather than disabling it entirely — reduces noise without removing the category, but adds queue management complexity","Merge it with the nearest adjacent category so its FP rate is diluted by the combined category's overall accuracy — obscures the FP problem rather than solving it, and contaminates a functioning category","Add a counter-example bank to the prompt showing common FP patterns so the model avoids similar false positives — the correct long-term prompt improvement strategy, but doesn't address the trust damage in the interim"],correct:0,explanation:"Temporarily disabling the high-FP category is the most important near-term action because developer trust erodes with each false alarm — even accurate alerts from other categories get ignored once the review pipeline feels unreliable. Raising thresholds and adding counter-examples are valid long-term improvements, but the immediate priority is stopping the trust damage."},
{domain:"d4",task:"4.1 Explicit criteria",question:"To achieve consistent severity classification, the recommended approach is:",options:["Define explicit severity levels with concrete code examples for each level","Require the model to output a free-form severity rationale and use a post-processing step to normalize the rationale text into standard levels","Define severity levels in natural language in the prompt — descriptive prose definitions produce more consistent classification than code examples","Derive severity levels from historical ticket resolution priority so the labels reflect actual business impact rather than abstract code quality judgments"],correct:0,explanation:"Severity levels backed by concrete code examples produce consistent classification. Letting the model invent a rubric per run leads to drift."},
{domain:"d4",task:"4.1 Explicit criteria",question:"Why do general instructions like 'only report high-confidence findings' fail to improve precision?",options:["They don't tell the model what 'high-confidence' means in this context — without specific criteria, the model can't calibrate","They constrain output volume but not classification thresholds — the model reports fewer findings but applies the same underlying decision criteria to each one","They suppress accurate low-confidence findings alongside inaccurate ones, reducing both false positives and true positives proportionally","LLMs interpret 'confidence' as frequency — high-confidence findings are those that appear in most training examples, biasing the model toward common issues over critical ones"],correct:0,explanation:"'High confidence' is not a calibrated quantity for an LLM. Specific categorical criteria are what actually reduce FPs."},
{domain:"d4",task:"4.1 Explicit criteria",question:"You want to reduce false positives in a code review tool. The most direct improvement is:",options:["Specify the exact categories of issues to report (with examples), and the categories to skip","Require the model to quote supporting evidence from the code for every finding — findings without direct code citations are rejected","Lower the model's temperature to 0 to reduce variance — deterministic outputs mean the model consistently applies the same (possibly imprecise) criteria","Use a best-of-3 sampling strategy and keep only findings that appear in at least two of three runs, filtering out low-confidence inconsistent outputs"],correct:0,explanation:"Explicit inclusion/exclusion criteria with examples is the direct mechanism. Generic admonitions don't help."},
{domain:"d4",task:"4.1 Explicit criteria",question:"Which is the strongest indicator that a prompt's specificity is insufficient?",options:["Inconsistent classification on identical inputs across separate runs — when the same input produces different outputs, it signals the prompt leaves too much room for model interpretation","The model produces longer outputs than expected — verbosity is often a sign of over-specification (too many instructions) rather than under-specification","Claude asks clarifying questions before answering — questions typically indicate genuine ambiguity in the input, not a prompt design defect","The model occasionally declines to answer — refusals are driven by safety guidelines or missing context, not by prompt specificity gaps"],correct:0,explanation:"Inconsistent outputs on identical inputs is the clearest signal that a prompt lacks specificity — the model fills the ambiguous space differently each run. Longer outputs suggest over-instruction. Clarifying questions reflect input ambiguity, not prompt design. Refusals are safety-driven, not specificity-driven."},
{domain:"d4",task:"4.1 Explicit criteria",question:"For high-FP categories, what's a useful diagnostic field to include in structured findings?",options:["A <code>detected_pattern</code> field describing the code construct that triggered the finding, enabling analysis of dismissal patterns","A <code>model_version</code> field so teams can attribute FP changes to specific model deployments rather than prompt changes","A <code>file_path</code> field so reviewers can navigate directly to the flagged location without reading the full finding context","A <code>rule_id</code> field that maps each finding to the prompt rule it triggered, enabling targeted rule-level FP analysis"],correct:0,explanation:"<code>detected_pattern</code> lets the team analyze which constructs trigger false positives and target prompt improvements."},
{domain:"d4",task:"4.1 Explicit criteria",question:"You're reviewing a prompt that says 'flag suspicious imports.' What's wrong?",options:["'Suspicious' is undefined — replace with specific patterns (unused, dynamic, network)","The scope is too narrow — 'imports' should be broadened to cover all dependency declarations including <code>require()</code> and dynamic <code>import()</code> calls","The instruction conflates detection with severity — split it into separate detection and severity-classification steps for cleaner output","The prompt omits context — add the project's approved import allowlist so the model can flag only deviations from sanctioned patterns"],correct:0,explanation:"'Suspicious' is a vague catch-all. Replace it with categorical, concrete patterns the model can check directly."},

/* --- 4.2 Few-shot prompting --- */
{domain:"d4",task:"4.2 Few-shot",question:"For an ambiguous scenario where the model picks inconsistent actions, what's the most effective improvement?",options:["Provide 2-4 targeted few-shot examples that show the reasoning for choosing one action over plausible alternatives","Extend the prose description to cover all edge cases explicitly, detailing the decision criteria for each possible situation","Add a <code>tool_choice: \"any\"</code> constraint to force the model to commit to a structured action rather than returning ambiguous free-form reasoning","Include a chain-of-thought prompt prefix that instructs the model to reason through the options before committing — better reasoning produces more consistent action selection"],correct:0,explanation:"Few-shot examples that demonstrate REASONING for ambiguous cases are the most effective. Pure prose explanations are less reliable; tool_choice doesn't help with selection between options."},
{domain:"d4",task:"4.2 Few-shot",question:"To get consistent output format (location, issue, severity, suggested fix), the recommended technique is:",options:["Include few-shot examples that demonstrate the exact desired output format","Add an 'IMPORTANT: format your output exactly as follows' instruction with a text description of the expected format","Set <code>temperature: 0</code> so that the model always generates the same token sequence once the format is established in the first run","Provide an explicit JSON schema alongside the prompt, relying on schema validation to reject any response that deviates from the expected structure"],correct:0,explanation:"Few-shot examples are the most reliable way to lock in specific output formats."},
{domain:"d4",task:"4.2 Few-shot",question:"Few-shot examples are particularly useful for which problem?",options:["Hallucination in extraction tasks (e.g., informal measurements, varied document structures)","Enforcing strict JSON output format — without examples, the model uses inconsistent field ordering and capitalization","Grounding the model on domain-specific terminology that was underrepresented in its training data","Reducing output verbosity — examples showing concise responses teach the model to be more succinct"],correct:0,explanation:"Few-shot examples are most powerful for extraction tasks prone to hallucination — they demonstrate correct handling of varied, ambiguous real-world inputs (informal measurements, unusual layouts, missing fields). B and D are real uses of few-shot but secondary. C describes fine-tuning territory; few-shot helps at inference time but doesn't permanently ground terminology."},
{domain:"d4",task:"4.2 Few-shot",question:"You're providing few-shot examples to reduce false positives. What should the examples include?",options:["Both acceptable patterns AND genuine issues, with reasoning — so the model learns the distinction, not just one side of the boundary","Only confirmed flagged issues — the model learns what to flag but not what to accept; it will over-flag because it has no representation of acceptable patterns","Only acceptable patterns — the model learns what to pass through but not what to flag; it will under-flag because it has no representation of genuine issues","A random sample from the dataset regardless of label — random samples may not include examples near the decision boundary where the model needs the most guidance"],correct:0,explanation:"Both positive (genuine issues) and negative (acceptable patterns) examples with reasoning teach the model the decision boundary — not just one side. Only flagged examples produce over-flagging. Only acceptable examples produce under-flagging. Random samples may not include the boundary cases that most need illustration."},
{domain:"d4",task:"4.2 Few-shot",question:"How do few-shot examples enable the model to generalize?",options:["They demonstrate the reasoning behind a class of decisions, helping the model apply the same logic to novel inputs that share the same underlying pattern","Enumerating the complete decision boundary — the model pattern-matches new inputs against examples without reasoning; generalization comes from boundary coverage, not reasoning","Encoding the desired behavior as implicit constraints that override contradictory instructions — examples demonstrate behavior but don't override explicit instructions; explicit rules take precedence","Showing exactly which tools to call in the illustrated scenarios — few-shot examples influence output style and reasoning patterns, not specific tool selections in unrelated scenarios"],correct:0,explanation:"Few-shot examples work by demonstrating the reasoning pattern behind a category of decisions — the model infers the principle and applies it to novel inputs. They don't work by exhaustive boundary coverage, implicit constraint override, or tool-call prescription."},
{domain:"d4",task:"4.2 Few-shot",question:"For documents with varied structure (inline citations vs. bibliographies, narrative vs. tables), the recommended pattern is:",options:["Add few-shot examples demonstrating correct extraction from each structural variant","Use <code>tool_choice: \"any\"</code> with multiple extraction tools, one per document structure, and let the model pick the right schema per document","Write a detailed prose description of how to handle each structural variant in the prompt — examples are only needed when prose fails to achieve consistency","Validate the output structure after extraction and retry with an error message describing which structural variant the document uses"],correct:0,explanation:"Examples per structural variant teach the model to handle the variety without inventing patterns."},
{domain:"d4",task:"4.2 Few-shot",question:"You see empty/null extraction for required fields when the source format varies. The fix is:",options:["Add few-shot examples demonstrating correct extraction from documents with varied formats — shows the model how to handle each format variation rather than leaving it to guess","Make all fields required in the schema — required fields enforce that the model outputs something, but force it to hallucinate values when the source doesn't contain the data","Require uniform input formatting before extraction — normalizing inputs reduces variance but adds preprocessing complexity and doesn't help with inherently varied source documents","Drop the failing fields from the schema — removing fields avoids the extraction problem but also loses the data those fields were designed to capture"],correct:0,explanation:"Few-shot examples with varied source formats teach the model how to handle each variation explicitly. Making fields required forces hallucination for missing data. Requiring uniform input adds preprocessing complexity. Dropping fields loses the data entirely — none of these address the root cause."},
{domain:"d4",task:"4.2 Few-shot",question:"The recommended number of targeted few-shot examples for an ambiguous scenario is roughly:",options:["2–4 examples — enough to establish the boundary condition with representative positive and negative cases without overwhelming the prompt","5–8 examples — fewer than 5 examples leaves too much ambiguity for boundary cases; more than 8 is still manageable and improves precision","1–2 examples — a minimal example set is sufficient to demonstrate the pattern while preserving context budget for other instructions","10 or more examples — the more examples, the more reliably the model generalizes the desired behavior and avoids edge cases"],correct:0,explanation:"2–4 targeted examples is the recommended range for clarifying ambiguous scenarios — enough to show positive and negative cases without consuming excessive context. 5–8 is more than needed for targeted boundary clarification. 1–2 may be insufficient to define boundaries. 10+ is overkill for targeted clarification and risks context waste."},
{domain:"d4",task:"4.2 Few-shot",question:"Few-shot examples are MORE effective than detailed prose when:",options:["Prose produces inconsistent results across runs and the desired transformation is hard to articulate in words","The task is well-defined with a small number of possible output patterns — examples exhaustively cover all valid outputs without needing the model to reason about edge cases","The prompt already contains a detailed chain-of-thought specification, so examples serve as a redundant consistency check rather than the primary signal","The system prompt is long and the examples can be placed near the end where they receive more attention than mid-prompt prose instructions"],correct:0,explanation:"When prose is interpreted inconsistently, concrete examples carry the signal more reliably."},
{domain:"d4",task:"4.2 Few-shot",question:"In a code review tool, you want the model to recognize a specific anti-pattern. The most effective addition is:",options:["A few-shot example showing the anti-pattern alongside its correct alternative, with the reasoning for the distinction","A general 'flag all anti-patterns' instruction — explicit enumeration creates a brittle list that misses novel instances of the same underlying problem","A detailed prose description of the anti-pattern's definition, characteristics, and why it's problematic — enough context for the model to generalize","A system prompt rule that names the pattern and declares it out of bounds, with a severity level indicating how urgently it should be flagged"],correct:0,explanation:"Showing the anti-pattern, the correct alternative, and the reasoning gives the model the concrete signal needed to apply judgment."},

/* --- 4.3 Structured output via tool_use --- */
{domain:"d4",task:"4.3 Structured output",question:"For guaranteed schema-compliant structured output that eliminates JSON syntax errors, use:",options:["Tool use (<code>tool_use</code>) with a JSON schema defined as the input parameter — the API enforces schema conformance on tool call arguments, making syntax errors impossible","A system prompt instruction: 'Always output valid JSON conforming to this schema: ...' — provides guidance but doesn't enforce conformance; the model can still produce malformed JSON","Setting <code>response_format: {\"type\": \"json_object\"}</code> in the request — requests JSON format but does not validate the output against a specific schema","Post-processing the model's output with a JSON parser and retrying on parse failures — correct as a fallback but adds latency and doesn't prevent syntax errors at generation time"],correct:0,explanation:"Tool use with a JSON schema enforces schema compliance at the API level — tool call arguments are validated against the schema, making JSON syntax errors impossible. System prompt instructions are probabilistic. response_format: json_object requests JSON without schema validation. Post-processing is a fallback, not a prevention mechanism."},
{domain:"d4",task:"4.3 Structured output",question:"Tool use with strict schemas eliminates syntax errors. Does it prevent semantic errors (e.g., line items not summing to the total)?",options:["No — strict schemas eliminate syntax errors but not semantic ones; design validation separately","Yes — the model is constrained to produce only values that satisfy field-level type and format constraints, which transitively ensures cross-field consistency","Yes, but only when using <code>tool_choice: \"any\"</code> — this strict mode enables cross-field constraint checking that <code>\"auto\"</code> does not activate","Yes if the schema includes cross-field <code>allOf</code> or <code>dependentSchemas</code> constraints — JSON Schema supports these, and the tool use framework enforces them"],correct:0,explanation:"Schemas guarantee syntactic correctness, not semantic correctness. You still need validation for cross-field constraints."},
{domain:"d4",task:"4.3 Structured output",question:"You have multiple extraction schemas and don't know the document type in advance. Which <code>tool_choice</code> setting guarantees structured output?",options:["<code>tool_choice: \"any\"</code> — model must call a tool but can pick which schema","<code>tool_choice: \"auto\"</code> — the model will select the appropriate schema based on the document content and return structured output","Forced selection with the most general schema — the model will fill only the applicable fields and leave the rest null","Omitting <code>tool_choice</code> — the model's default behavior is to call a tool when tools are defined, ensuring structured output"],correct:0,explanation:"<code>\"any\"</code> forces a tool call but lets the model select which schema fits the document. <code>\"auto\"</code> (B) allows a text response if the model decides no tool is needed — no guarantee of structured output. Forced selection (C) locks in a schema regardless of fit. Omitting tool_choice (D) defaults to <code>\"auto\"</code> behavior, which also permits text responses."},
{domain:"d4",task:"4.3 Structured output",question:"You want <code>extract_metadata</code> to run BEFORE any enrichment tools, every time. Best mechanism?",options:["<code>tool_choice: {\"type\": \"tool\", \"name\": \"extract_metadata\"}</code> on the first API call — this forces the specific tool on that turn, guaranteeing it runs first before giving the model tool-selection freedom","Rely on the model to infer the intended order from the tool descriptions and system prompt — probabilistic; the model may choose an enrichment tool first under complex or ambiguous inputs","Add a system prompt rule: 'Always call <code>extract_metadata</code> before any enrichment tool' — prompt-based ordering is probabilistic and has been shown to fail under high-context scenarios","Remove enrichment tools from the tool list for the first API call, then add them back on subsequent turns — this works but is more complex than using tool_choice on the first call"],correct:0,explanation:"Named tool_choice on the first call is the deterministic mechanism — it guarantees extract_metadata runs before the model gets discretion over enrichment tools. Prompt rules and description-based ordering are probabilistic. Removing/restoring tools works but adds unnecessary complexity compared to tool_choice."},
{domain:"d4",task:"4.3 Structured output",question:"For source documents where a field may not be present, what's the recommended schema design?",options:["Make the field optional/nullable to prevent the model from fabricating values","Mark it required and let the model fill in defaults","Always require all fields","Use a separate tool for missing fields"],correct:0,explanation:"Required fields with missing data tempt the model to fabricate. Nullable fields are explicit and preserve truthful extraction."},
{domain:"d4",task:"4.3 Structured output",question:"Your enum has a fixed set of categories, but new ones occasionally arise. Best schema pattern?",options:["Add an <code>\"other\"</code> enum value with an associated detail/free-text field for extensibility","Make the enum field free-text","Force the model to pick the closest existing value","Add a new field for each unknown"],correct:0,explanation:"<code>\"other\"</code> + a detail string captures cases outside the fixed set without losing structure."},
{domain:"d4",task:"4.3 Structured output",question:"For ambiguous extraction cases, what enum value pattern helps?",options:["An <code>\"unclear\"</code> enum value so the model can mark ambiguity instead of guessing","Removing enums entirely","Adding more required fields","Increasing max_tokens"],correct:0,explanation:"<code>\"unclear\"</code> as an explicit option lets the model represent ambiguity instead of fabricating a confident answer."},
{domain:"d4",task:"4.3 Structured output",question:"To prevent the model from inventing data to satisfy a required schema field that isn't present in the source, you should:",options:["Use nullable fields and let the model report missing data explicitly","Always make fields required","Lower temperature","Switch to a smaller model"],correct:0,explanation:"Nullable/optional fields tell the model that 'missing' is a valid answer — fabrication is what required fields with missing data invite."},
{domain:"d4",task:"4.3 Structured output",question:"Strict JSON schemas via tool use prevent:",options:["Syntax errors in the JSON output — missing braces, wrong value types, malformed strings, and structural issues are prevented at the API level by schema enforcement","Semantic errors where field values are logically incorrect — a schema can enforce that a field is a number, but not that it equals the sum of other fields","Model hallucinations in extracted content — schema enforcement validates structure, not factual accuracy; the model can still return a valid-schema response with fabricated values","Downstream application crashes from unexpected field names — schema enforcement at generation time prevents undefined fields, but this is a consequence of preventing syntax/structure errors, not a distinct category"],correct:0,explanation:"Strict schemas via tool use prevent syntax and structural errors — the API validates that the model's output matches the defined schema. They don't prevent semantic errors (logically wrong values), factual hallucinations (structurally valid but invented content), or network failures."},
{domain:"d4",task:"4.3 Structured output",question:"For an extraction tool whose JSON schema has many optional fields, what's the value of marking them optional rather than required-with-empty-default?",options:["Optional/nullable lets the model represent 'not present' truthfully; required-with-default may produce fabricated empty values that look real","Required fields with empty defaults are semantically equivalent to optional fields — both allow the model to omit values it can't find","Optional fields cause the model to skip extraction for those fields entirely, reducing hallucination at the cost of lower recall","Required fields with a null default cause the model to report uncertainty, which is preferable to omitting the field silently"],correct:0,explanation:"Optional/nullable lets the model represent 'not present in source' truthfully. Required-with-empty-default (e.g., required with default '') may cause the model to fabricate a plausible value to satisfy the constraint, producing data that looks real but isn't. B incorrectly equates the two — they have different model behaviors. C mischaracterizes how optional fields affect extraction. D suggests null default achieves the same goal, but required-null still signals to the model that a value should exist."},
{domain:"d4",task:"4.3 Structured output",question:"Which <code>tool_choice</code> option might let the model return conversational text instead of calling a tool?",options:["<code>\"auto\"</code> — the model decides on each turn whether to call a tool or return a text response; a tool call is not guaranteed","<code>\"any\"</code> — forces the model to call at least one tool every turn, making a text-only response impossible","<code>{\"type\": \"tool\", \"name\": \"summarize\"}</code> — forces a specific tool call, guaranteeing a tool invocation rather than a text response","Omitting <code>tool_choice</code> is equivalent to <code>\"any\"</code> — the model always calls a tool when the parameter is absent"],correct:0,explanation:"\"auto\" (and the equivalent default of omitting tool_choice) gives the model discretion to return text when it judges no tool is needed. \"any\" and named tool overrides both guarantee a tool call occurs. The distractors' claim that omitting tool_choice equals \"any\" is incorrect — omitting it defaults to \"auto\" behavior."},
{domain:"d4",task:"4.3 Structured output",question:"What additional element should accompany strict output schemas to handle inconsistent source formatting?",options:["Format normalization rules in the prompt (e.g., 'convert all dates to ISO 8601 before output')","A nullable fallback field in the JSON schema for each field that may vary in format, letting the model pick the appropriate representation per document","Post-extraction normalization logic in the application layer that canonicalizes values after they are extracted, keeping the schema stable across document variations","Pre-processing each document through a separate normalization step before extraction, standardizing field formats so the extraction schema can be simpler"],correct:0,explanation:"Schemas don't normalize input. Prompts should include normalization rules alongside output schemas to handle varied source formatting."},

/* --- 4.4 Validation, retry --- */
{domain:"d4",task:"4.4 Validation & retry",question:"A semantic validation error (e.g., line items don't sum to total) occurred during extraction. What's the recommended retry strategy?",options:["Send a follow-up request that includes the original document, the failed extraction, and the specific validation error","Retry with the same prompt and document — transient generation errors often self-correct on a second attempt without any additional context","Send only the validation error without the original document — the model already has the extraction in context and the error alone is sufficient to trigger self-correction","Route directly to human review without retrying — semantic validation failures usually indicate the source document is ambiguous and automation cannot resolve them reliably"],correct:0,explanation:"Retry-with-error-feedback is the recommended pattern: include the document, the failed extraction, and the specific error so the model can self-correct."},
{domain:"d4",task:"4.4 Validation & retry",question:"For calibrating field-level confidence scores, you should:",options:["Map model confidence to actual accuracy using a labeled validation set — raw model confidence is uncalibrated; calibration reveals the true accuracy at each confidence level","Use the model's raw confidence output directly as a routing threshold — raw confidence is systematically biased and doesn't reliably reflect actual accuracy without calibration","Apply a fixed threshold of 0.5 across all field types — a universal threshold ignores that different fields have different base rates and error distributions","Set the threshold based on business risk tolerance alone without measuring empirical accuracy — risk tolerance informs the decision but must be applied to calibrated accuracy curves, not raw scores"],correct:0,explanation:"Calibration against a labeled validation set converts raw model confidence into meaningful accuracy estimates — without calibration, 0.9 confidence might correspond to 70% actual accuracy. Raw confidence is unreliable. A fixed 0.5 threshold ignores field-level accuracy variance. Risk tolerance sets the policy but requires calibrated accuracy data to apply correctly."},
{domain:"d4",task:"4.4 Validation & retry",question:"To enable systematic analysis of false-positive triggers, the structured findings should include:",options:["A <code>detected_pattern</code> field describing the code construct that triggered the finding — this enables engineers to aggregate and analyze which patterns drive false positives","A <code>confidence_score</code> field from 0.0 to 1.0 — numeric confidence helps route findings for review but doesn't explain which patterns are causing misclassifications","A <code>suggested_fix</code> field with an auto-generated remediation — actionable but doesn't help diagnose why the finding was triggered in the first place","A <code>line_count</code> field for the affected code block — contextual metadata but doesn't capture the semantic construct that caused the false positive"],correct:0,explanation:"A detected_pattern field records what the model observed that triggered the finding — this is the key to systematic false-positive analysis. Without knowing what code construct fired, you can't identify which patterns produce noise. Confidence scores route findings but don't explain triggers. Suggested fixes and line counts are supplementary, not diagnostic."},
{domain:"d4",task:"4.4 Validation & retry",question:"A document has a stated total of $1000 but line items sum to $950. The model fills in both fields. What's the right extraction approach?",options:["Extract both <code>calculated_total</code> and <code>stated_total</code>, and flag the discrepancy — preserves both values and surfaces the inconsistency for downstream reconciliation","Force the model to recompute and resolve the discrepancy before returning — the model performing arithmetic on source data introduces the risk of choosing the wrong value or hallucinating a reconciliation","Return only the stated total, treating the document's explicit value as authoritative — discards the discrepancy signal and the calculated figure, which may be the correct one","Return only the calculated total as the more reliable value — the stated total is explicit source data; discarding it loses potentially important provenance information"],correct:0,explanation:"Extracting both values and flagging the discrepancy preserves all source data and enables human reconciliation. Forcing the model to resolve the discrepancy risks choosing the wrong value. Returning only the stated or only the calculated total discards a value that may be correct and loses the inconsistency signal."},
{domain:"d4",task:"4.4 Validation & retry",question:"For inconsistent source data, a useful schema addition is:",options:["A <code>conflict_detected: boolean</code> field that the model sets when it finds inconsistencies in the source — enables automated routing and downstream reconciliation without losing either conflicting value","A free-text <code>notes</code> field where the model describes the inconsistency — captures the issue but in an unstructured form that's hard to process programmatically","A <code>confidence: float</code> field without a conflict flag — numeric confidence indicates uncertainty but doesn't explicitly signal that two conflicting values exist in the source","A <code>source_count: int</code> field recording how many sources were consulted — quantifies coverage but doesn't indicate whether sources agree or conflict"],correct:0,explanation:"A machine-actionable conflict_detected boolean enables automated routing of inconsistent records for human review without losing either conflicting value. Free-text notes are unstructured and hard to process programmatically. A confidence float indicates uncertainty but doesn't explicitly flag contradictions. Source count measures coverage, not consistency."},
{domain:"d4",task:"4.4 Validation & retry",question:"Synthesis reports should:",options:["Distinguish well-established findings from contested ones, preserving original source context and confidence levels rather than presenting all findings as equally certain","Present all findings as equally certain to avoid confusing readers with uncertainty qualifications — flattening confidence differences hides important reliability distinctions","Drop contested findings to maintain a coherent, definitive narrative — contested findings often represent the most important areas of uncertainty and should be surfaced, not suppressed","Drop well-established findings to focus the report on unresolved questions — established findings provide the baseline context needed to interpret contested ones"],correct:0,explanation:"Synthesis reports should preserve the confidence hierarchy from the source data — well-established findings versus contested ones carry different weights. Presenting all findings as equally certain flattens important distinctions. Dropping contested or established findings both lose critical information: contested findings reveal uncertainty, established findings provide context."},
{domain:"d4",task:"4.4 Validation & retry",question:"Semantic validation errors (e.g., wrong field placement, totals not summing) differ from syntax errors in that:",options:["They require business-logic validation; strict JSON schemas via tool use eliminate syntax errors but cannot verify that a total field actually equals the sum of its line items","Detection is easier for semantic errors — the opposite is true; syntax errors are caught by a parser in milliseconds, while semantic errors require domain-aware validation logic","Retrying with a corrected prompt cannot fix them — incorrect; targeted retry with specific error context (e.g., 'the total field should equal the sum of line items') can address semantic errors","Exceeding token limits is the primary cause — token limits cause truncation of outputs, not semantic mismatches between related fields within the response"],correct:0,explanation:"Semantic errors require business-logic validation beyond what JSON schema enforcement provides — a structurally valid JSON document can still have totals that don't sum or fields in wrong positions. Semantic errors are harder to detect than syntax errors, not easier. Both can be addressed by retry. Token limits cause truncation, not semantic inconsistencies."},
{domain:"d4",task:"4.4 Validation & retry",question:"Why is including the original document in a retry request important?",options:["The model needs the source to re-extract correctly — a retry with only the failed extraction result lacks the ground truth the model needs to identify and correct the error","The batch API requires the original document in retry requests — the API doesn't enforce this; it's a best practice, not a technical requirement","Retrying without the original document counts against rate limits differently — rate limits are based on tokens and request counts, not on whether source documents are included","Including the document provides the model with more tokens to work with, improving extraction quality — it's not the token increase but the source reference that matters"],correct:0,explanation:"The model needs the original source document to re-extract correctly — the failed extraction result alone doesn't contain the source text the model needs to correct the error. The API doesn't technically require the original document. Rate limits are not affected by document inclusion. It's the source reference, not the added tokens, that improves retry quality."},

/* --- 4.5 Batch processing --- */
{domain:"d4",task:"4.5 Batch processing",question:"Your team wants to reduce API costs for automated analysis. Currently, real-time Claude calls power two workflows: (1) a blocking pre-merge check that must complete before developers can merge, and (2) a technical debt report generated overnight. Your manager proposes switching both to the Message Batches API for its 50% cost savings. How should you evaluate this proposal?",options:["Use batch processing for the technical debt reports only; keep real-time calls for pre-merge checks","Switch both workflows to batch processing with status polling to check for completion","Keep real-time calls for both workflows to avoid batch result ordering issues","Switch both to batch processing with a timeout fallback to real-time if batches take too long"],correct:0,explanation:"This is question 11 from the official guide. The Batches API offers 50% savings but processes up to 24 hours with no SLA — unsuitable for blocking workflows like pre-merge checks. Overnight reports are ideal batch candidates. Option B accepts unbounded latency for blocking work. Option C reflects a misconception (batches use custom_id for correlation). Option D adds unnecessary complexity."},
{domain:"d4",task:"4.5 Batch processing",question:"The Message Batches API offers:",options:["50% cost savings with up to a 24-hour processing window and no guaranteed latency SLA","75% cost savings with 1-hour SLA","Same cost as real-time but lower latency","Free processing for the first 1M tokens"],correct:0,explanation:"50% cost savings, up to 24h processing, no SLA — the exam guide's specific figures."},
{domain:"d4",task:"4.5 Batch processing",question:"Which is INAPPROPRIATE for batch processing?",options:["Blocking pre-merge code review checks","Overnight technical debt report","Weekly extraction audits","Nightly test generation"],correct:0,explanation:"Blocking workflows can't accept unbounded latency. Overnight/weekly/nightly jobs are ideal batch candidates."},
{domain:"d4",task:"4.5 Batch processing",question:"A limitation of the Batches API important to remember is:",options:["It does not support multi-turn tool calling within a single batch request","Batch requests have a 24-hour TTL — if not claimed within 24 hours, results are deleted and the batch must be resubmitted","The Batches API does not support extended thinking mode, limiting its use for complex multi-step reasoning tasks","Batch requests are rate-limited to 100 concurrent items, making it less efficient than the synchronous API for large-scale processing"],correct:0,explanation:"The Batches API does not support mid-request tool execution — you cannot have Claude call a tool, get a result, and continue in the same batch item. Agentic loops that require tool turn-around must use the synchronous API. B describes a real constraint (batches do expire) but it's not the key architectural limitation. C (extended thinking) may be a limitation but not the primary one. D (100-item limit) is factually incorrect — batch limits are much higher."},
{domain:"d4",task:"4.5 Batch processing",question:"How do you correlate a batch response back to its original request?",options:["By the <code>custom_id</code> field — included in each request and returned with each response, enabling reliable mapping between submissions and results","By response order — batch results are not guaranteed to return in submission order; relying on position would silently misassign responses when the order shifts","By response timestamp — multiple requests in a batch may complete at the same millisecond, making timestamp-based correlation ambiguous","By content similarity — matching responses to requests by semantic similarity introduces errors when requests have similar content and is not a reliable programmatic approach"],correct:0,explanation:"custom_id is the supported correlation mechanism: you assign a unique ID to each request when submitting, and the batch API returns that same ID with the corresponding result. Response order is not guaranteed. Timestamps are not unique within a batch. Content similarity is error-prone and non-deterministic."},
{domain:"d4",task:"4.5 Batch processing",question:"You have an SLA requiring 30-hour processing for a large batch. How should you schedule submissions?",options:["Use a staggered submission schedule (e.g., every 4 hours) so each batch's 24-hour processing window completes before its downstream deadline","Sending everything in a single batch at the start maximizes throughput but risks missing the SLA if the 24-hour window starts late or the batch is large enough to run long","Restricting submissions to business hours is tempting for operational simplicity but wastes the available processing window and reduces effective throughput","Hourly micro-batches fragment the workload unnecessarily, add overhead from frequent submissions, and don't leverage the batch API's cost efficiency for large volumes"],correct:0,explanation:"Staggered submission ensures each batch's worst-case 24-hour processing window fits within the 30-hour SLA. A single large batch risks SLA failure if processing starts near the deadline. Business-hours-only submission wastes the available window. Hourly micro-batches add overhead without improving latency."},
{domain:"d4",task:"4.5 Batch processing",question:"Some batch requests fail. What's the right recovery?",options:["Identify failed items by <code>custom_id</code> and resubmit only those, with appropriate modifications if the failure was due to invalid inputs","Resubmit the entire batch — re-running successful requests wastes API budget and potentially inflates costs for results you already have","Drop failed items and proceed with partial results — acceptable for non-critical pipelines but not for cases where complete coverage is required","Switch the failed items to synchronous API calls for immediate retry — synchronous calls have different rate limits and higher per-token cost, making this inefficient for batch-scale recovery"],correct:0,explanation:"Targeted resubmission by custom_id is the correct recovery pattern — retry only what failed, preserving the work already completed. Resubmitting the full batch wastes budget on successes. Dropping failed items produces incomplete results. Switching to synchronous for recovery loses batch pricing advantages."},
{domain:"d4",task:"4.5 Batch processing",question:"Before batch-processing large volumes, what's the recommended preparation step?",options:["Refine prompts on a small sample first — maximizing first-pass success rates avoids costly iterative resubmissions across the full volume","Submit the full volume immediately — faster time-to-start, but prompt issues discovered mid-batch require reprocessing large numbers of items","Run a synthetic load test against the batch endpoint — load testing verifies throughput but doesn't validate prompt quality or output correctness on real inputs","Disable retries to measure the raw first-pass success rate — disabling retries produces a metric but at the cost of failing items that would otherwise recover"],correct:0,explanation:"Prompt refinement on a small sample catches issues before they compound across large volumes. Submitting immediately risks discovering prompt problems after many items have been processed incorrectly. Load testing validates infrastructure, not prompt quality. Disabling retries provides a quality metric but unnecessarily fails recoverable items."},
{domain:"d4",task:"4.5 Batch processing",question:"For a workflow that must complete within seconds and blocks the user, the right API choice is:",options:["Synchronous API — batch does not meet the latency requirement","Batch API — for cost savings","Either — they perform similarly","Neither — use a different model"],correct:0,explanation:"Blocking workflows require synchronous API. Batch API saves cost but has no latency SLA."},
{domain:"d4",task:"4.5 Batch processing",question:"Which scenario MOST clearly fits the Batches API?",options:["Weekly compliance audits run overnight","Live customer chat","Pre-deploy security review","Real-time fraud detection"],correct:0,explanation:"Latency-tolerant, large-volume scheduled workloads are the textbook batch case."},

/* --- 4.6 Multi-instance review --- */
{domain:"d4",task:"4.6 Multi-pass review",question:"A model retains its reasoning context from generation, making it less likely to question its own decisions in the same session. The recommended pattern for high-quality review is:",options:["Use a second, independent Claude instance (without the generator's reasoning context) to review the code","Have the same session re-review its own work after a full <code>/compact</code> to clear the reasoning context before beginning the review pass","Provide the reviewing session with the original task specification but not the generated code, so it reasons about expected behavior independently before comparing","Run extended thinking mode on the generator session's last turn, which forces deeper self-critique that corrects generation-phase blind spots"],correct:0,explanation:"Independent review instances catch issues the generator missed. Self-review in the same session is biased by the generator's prior reasoning."},
{domain:"d4",task:"4.6 Multi-pass review",question:"A large multi-file review producing inconsistent depth across files should be restructured by:",options:["Splitting into per-file local-issue passes plus a separate integration pass for cross-file analysis","Running the review in one pass but with an explicit instruction to allocate equal token budget per file, ensuring uniform depth","Using a dedicated reviewer model with a larger context window so all files receive equal attention in a single pass","Providing a structured review template with one section per file so the model allocates a fixed portion of its response to each file"],correct:0,explanation:"Per-file passes give consistent depth; a separate integration pass handles cross-file concerns. Larger context doesn't solve attention dilution."},
{domain:"d4",task:"4.6 Multi-pass review",question:"To enable calibrated review routing, what should each finding include?",options:["A model-reported confidence score alongside the finding so downstream code can route by confidence","Only the finding text and file location — adding confidence scores introduces model self-reporting noise that degrades routing accuracy","A severity level plus an <code>evidence_quote</code> field — these give reviewers enough context to decide review priority without raw confidence numbers","An <code>is_auto_fixable</code> boolean so routing can separate findings that need human review from those the pipeline can auto-patch"],correct:0,explanation:"Per-finding confidence scores enable downstream routing — though they must be calibrated against labeled data to be useful."},
{domain:"d4",task:"4.6 Multi-pass review",question:"Self-review in the same session tends to be less effective because:",options:["The model retains its prior reasoning context and is less likely to question its own decisions","The same session applies a stricter review pass because it already knows which decisions were made with low confidence, making it over-report issues on uncertain code","The generator session's context is flushed after generation, so the reviewer starts fresh — but without the original task specification it has insufficient context to evaluate correctness","Session state is shared across generation and review, causing the model to use its cached tool results from generation instead of re-evaluating the code from scratch"],correct:0,explanation:"Reasoning anchoring is the issue. Independent instances aren't anchored, so they can catch issues the generator missed."},
{domain:"d4",task:"4.6 Multi-pass review",question:"Why doesn't a larger context window solve attention dilution in a multi-file review?",options:["Context window size doesn't equate to attention quality — large inputs can still suffer from 'lost in the middle' effects","Larger context windows raise the per-token cost significantly, making them uneconomical for review tasks that could be split into smaller focused passes","The Claude API enforces a maximum context size for review tool calls, preventing large-context review from being used in production CI pipelines","Larger context windows require the model to use extended thinking mode, which changes review behavior in ways that reduce false-positive suppression"],correct:0,explanation:"Capacity and attention are different. Larger windows don't guarantee equal depth across all content."},
{domain:"d4",task:"4.6 Multi-pass review",question:"Which is true about multi-pass review architectures?",options:["Splitting large reviews into per-file passes plus a separate cross-file integration pass produces more consistent depth than reviewing the full changeset in one pass","Running a single comprehensive pass always produces higher quality than splitting — a single pass maintains context about the full changeset, whereas splits lose cross-file relationships","Running identical passes five times and majority-voting the results is more accurate than a targeted multi-pass approach — repetition without structure amplifies noise rather than improving precision","Static analysis tools make multi-pass review redundant — static analysis handles syntax and pattern detection; multi-pass review adds semantic and architectural review that static tools can't perform"],correct:0,explanation:"Per-file passes plus a cross-file integration pass consistently outperform single-pass review because each pass can focus attention fully — local issues don't compete with cross-file concerns. A single pass suffers from attention dilution across large changesets. Majority voting on identical passes doesn't fix the dilution problem. Static analysis complements but doesn't replace LLM review for semantic issues."},
{domain:"d4",task:"4.6 Multi-pass review",question:"Requiring developers to manually split their PRs into smaller submissions to fit review context limits is:",options:["An architectural anti-pattern — it shifts review-system complexity onto developers who must manually restructure their work; a well-designed review pipeline should handle decomposition internally","The recommended approach — smaller PRs improve review quality and are a standard software engineering best practice regardless of the reviewing system","Required by the Claude Code review API — the API doesn't mandate PR size; context limits can be managed through multi-pass review architecture without developer intervention","Equivalent to a multi-pass review — multi-pass review splits analysis across multiple API calls; requiring developers to split PRs is a workflow burden, not an equivalent technical solution"],correct:0,explanation:"Manual PR splitting is an anti-pattern because it imposes architectural constraints on developers rather than solving the problem at the review system level. Multi-pass review handles large PRs internally through per-file passes plus integration passes. Smaller PRs are a general best practice but not the right solution to review system context limits."},
{domain:"d4",task:"4.6 Multi-pass review",question:"Running three independent review passes and only flagging issues that appear in all three:",options:["Suppresses real bugs that may only be caught intermittently — not recommended","Is the recommended pattern","Increases recall","Is required for compliance"],correct:0,explanation:"Triple-pass consensus filtering suppresses legitimate findings caught by only one pass. It's an anti-pattern."},

/* ============================================================
   DOMAIN 5 — Context Management & Reliability (15%)
   ============================================================ */

/* --- 5.1 Conversation context --- */
{domain:"d5",task:"5.1 Conversation context",question:"In a long support conversation, the agent starts giving inaccurate quotes for amounts and dates discussed earlier. What's the most reliable mitigation?",options:["Extract transactional facts (amounts, dates, order numbers, statuses) into a persistent 'case facts' block included in every prompt, outside the summarized history","Use progressive summarization that compresses earlier turns — the summary preserves the gist of past exchanges and is sufficient for coherent conversation","Increase the <code>max_tokens</code> limit so the full conversation history fits in context without truncation, preserving all earlier amounts and dates verbatim","Add a system prompt reminder at every turn: 'Recall and reference exact amounts and dates from earlier in the conversation' — this anchors the model's attention to precision"],correct:0,explanation:"Precise facts should be persisted in a dedicated case-facts block outside summarized history, so progressive summarization doesn't degrade them."},
{domain:"d5",task:"5.1 Conversation context",question:"The 'lost in the middle' effect means:",options:["Models reliably process information at the beginning and end of long inputs but may under-attend to content in the middle of the context window","Uniform attention across all positions — this describes the opposite of lost-in-the-middle; research shows attention is NOT uniform and drops in the middle of long contexts","Performance degradation only at the end of long contexts due to recency bias — recency bias describes the opposite pattern (favoring recent content); lost-in-the-middle specifically affects middle positions","Degradation only on inputs longer than the model's training corpus average — lost-in-the-middle is an attention distribution phenomenon, not a training-length comparison effect"],correct:0,explanation:"Lost-in-the-middle describes the empirical finding that models are more reliable at the beginning and end of long inputs than in the middle. It is not uniform attention, not an end-only effect, and not a training-length comparison — it's specifically about attention distribution across position within a single context."},
{domain:"d5",task:"5.1 Conversation context",question:"How should you organize long aggregated inputs to mitigate position effects?",options:["Place key findings summaries at the beginning, use explicit section headers, and trim verbose tool outputs","Place key findings summaries at the end with a brief executive summary at the start, relying on the model's strong recency and primacy effects to ensure both ends get attention","Interleave source material with analysis throughout the document so supporting evidence appears immediately after each claim, reducing navigation distance","Consolidate all source material into a single dense section without headers so the model treats it as continuous context rather than siloed chunks"],correct:0,explanation:"Beginning placement + section headers + trimming addresses position effects. Middle placement is worst."},
{domain:"d5",task:"5.1 Conversation context",question:"A tool returns 40+ fields per order lookup, but only 5 are relevant. What should the architect do?",options:["Trim the tool output to the 5 relevant fields before returning results to the model — prevents the irrelevant data from accumulating in context across repeated calls","Increase the context window by upgrading to a higher-tier model — more context capacity allows the verbose output to fit, but doesn't prevent context degradation from irrelevant tokens","Ask the model to ignore the irrelevant fields in its reasoning — the model still processes all 40 fields even when told to ignore them; the tokens still consume context","Cache the tool result and reuse it across turns to avoid repeated context accumulation from multiple lookup calls"],correct:0,explanation:"Trimming at the tool layer removes irrelevant data before it enters context — the correct architectural fix. A larger context window accommodates the noise but doesn't eliminate it. Instructing the model to ignore fields doesn't prevent those tokens from consuming context. Caching reduces the number of calls but doesn't fix the per-call context overhead."},
{domain:"d5",task:"5.1 Conversation context",question:"For a multi-issue session, what's the recommended state-management pattern?",options:["Extract structured issue data (order IDs, amounts, statuses) into a separate context layer that persists across turns","Append a running issues log at the end of each turn so the model can scan it for open items, relying on in-context proximity for recall","Use progressive summarization of earlier turns, compressing older issue details into brief references and keeping only recent exchanges verbatim","Store each issue's state in a structured JSON object and inject the whole object into every prompt, letting the model read and update fields as it resolves each item"],correct:0,explanation:"Structured issue extraction into a persistent layer is the recommended pattern. Re-reading messages doesn't scale; discarding loses context."},
{domain:"d5",task:"5.1 Conversation context",scenario:"You are deploying an AI-powered customer support agent for a telecommunications company. The agent handles multi-turn conversations about billing disputes, plan changes, and device issues. Conversations can span 20+ turns and reference order numbers, amounts, and dates discussed earlier.",question:"Why is the Claude API described as 'stateless'?",options:["Claude does not retain memory between API calls — the application must send the full conversation history each request","Each API request creates a new model instance from scratch — there is no persistent server-side session binding requests together","Claude's attention mechanism treats each token equally regardless of turn order, so earlier messages don't influence later responses unless emphasized","The API uses HTTP/1.1 rather than WebSockets, so each request-response cycle is independent with no persistent connection state"],correct:0,explanation:"Stateless means Claude retains no memory between API calls — the application must send the full conversation history on every request. B conflates statelessness with instance allocation (different concept). C mischaracterizes how attention mechanisms work. D confuses network protocol with application state management."},
{domain:"d5",task:"5.1 Conversation context",question:"Why is progressive summarization risky for transactional conversations?",options:["It tends to condense numerical values, dates, and customer-stated expectations into vague summaries, losing precision","Summarization prompts introduce extra latency on every turn, slowing the customer experience","It requires a dedicated summarization API call that doubles token usage compared to simple truncation","Once summarized, the model can no longer be asked follow-up questions about earlier turns"],correct:0,explanation:"Summaries compress detail. Precise transactional facts (amounts, dates, order IDs) must be persisted separately in a stable facts block, outside the summarized history."},
{domain:"d5",task:"5.1 Conversation context",question:"To maintain conversational coherence across many turns, your application should:",options:["Send the complete (or appropriately curated) conversation history in subsequent API requests","Send only the latest user message; rely on the system prompt to maintain persona consistency","Use semantic similarity to retrieve the 3 most relevant earlier turns and inject them before the latest message","Cache the API response server-side and re-use it when the same user returns within 24 hours"],correct:0,explanation:"The API is stateless; coherence requires sending history in each request. Sending only the latest message loses all prior context. Semantic retrieval may miss important non-salient context. Server-side caching returns stale responses."},

/* --- 5.2 Escalation patterns --- */
{domain:"d5",task:"5.2 Escalation",question:"Which is an appropriate escalation trigger?",options:["The customer requests a human, or policy is silent/ambiguous on the request, or the agent cannot make meaningful progress","The customer's message contains negative sentiment words above a configurable threshold","The agent's self-reported confidence drops below 7 out of 10 on its most recent response","The conversation has exceeded 10 minutes of elapsed time"],correct:0,explanation:"Customer request, policy gap, and inability to progress are the recommended triggers. Sentiment and self-reported confidence are unreliable signals, and duration alone is not a meaningful indicator of escalation need."},
{domain:"d5",task:"5.2 Escalation",question:"A customer is frustrated about a return that the agent CAN process within policy. What's the right response?",options:["Acknowledge the frustration, offer to resolve the return since it's within policy, and escalate only the emotional component if the customer remains upset after resolution","Silently process the return without acknowledging the frustration — resolves the issue but ignores the customer's emotional state, which is an escalation trigger","Escalate immediately to a human agent — appropriate if the return were outside policy, but escalating a solvable in-policy request wastes human resources unnecessarily","Process the return and send a satisfaction survey afterward — deferring acknowledgment to a post-interaction survey doesn't address the frustration during the interaction"],correct:0,explanation:"The right response combines resolution (the return is solvable) with acknowledgment (the customer is frustrated). Silent processing ignores the emotional trigger. Immediate escalation is appropriate for out-of-policy requests, not solvable ones. Post-interaction surveys don't address frustration in the moment."},
{domain:"d5",task:"5.2 Escalation",question:"A tool returns multiple matching customers for an ambiguous identifier. The agent should:",options:["Ask the customer for additional identifiers rather than selecting heuristically","Pick the first match","Pick the most recent match","Always escalate to a human"],correct:0,explanation:"Multiple matches require clarification, not heuristic selection. Heuristics produce silent misidentification."},
{domain:"d5",task:"5.2 Escalation",question:"Why is sentiment-based escalation unreliable?",options:["Customer frustration doesn't correlate with case complexity — easy cases can have frustrated customers, hard cases can have calm customers","Sentiment analysis adds meaningful latency to every turn, making real-time escalation impractical","Sentiment scores are unreliable when the customer switches languages mid-conversation","Negative sentiment already triggers a refund, so escalating again would duplicate the action"],correct:0,explanation:"Sentiment is a poor proxy for case complexity. It produces too many false positives and false negatives."},
{domain:"d5",task:"5.2 Escalation",question:"Why is self-reported confidence a poor escalation signal?",options:["LLM confidence is poorly calibrated — the model is often confidently wrong on the hard cases","Confidence is computed after generation, making it inherently reactive rather than preventive","Confidence scores are highly variable across model versions, making thresholds hard to maintain","Self-reported confidence is reliable for factual queries but degrades on multi-turn policy interpretation"],correct:0,explanation:"LLM self-reported confidence is famously poorly calibrated. The model can be confidently wrong on exactly the cases where escalation is warranted."},
{domain:"d5",task:"5.2 Escalation",question:"Policy says nothing about competitor price matching. The customer asks for one. The recommended response is:",options:["Escalate — this is a policy gap, not a complex case","Decline the request and log it for monthly policy review","Apply a standard 5% discount as a reasonable proxy for a price-match outcome","Ask the customer for a screenshot of the competitor's price so you can validate the claim before deciding"],correct:0,explanation:"Policy gaps are escalation triggers. Don't invent policy; don't refuse silently; escalate so a human can decide."},
{domain:"d5",task:"5.2 Escalation",scenario:"You are deploying an AI-powered customer support agent for a telecommunications company. The agent handles multi-turn conversations about billing disputes, plan changes, and device issues. Conversations can span 20+ turns and reference order numbers, amounts, and dates discussed earlier.",question:"A customer explicitly demands a human. The recommended response is:",options:["Honor the request immediately without first attempting investigation","Investigate the issue first to save the human's time","Ignore the request and continue investigating","Send the customer a survey"],correct:0,explanation:"Explicit customer requests for human agents should be honored immediately. Investigation first is disrespectful and overrides their stated preference."},
{domain:"d5",task:"5.2 Escalation",question:"What is the most effective way to teach an agent WHEN to escalate?",options:["Explicit escalation criteria in the system prompt with few-shot examples demonstrating each trigger","Insert a secondary LLM call after every response to evaluate whether the answer warrants escalation","Use a sentiment classifier to detect frustration and route above a threshold score","Set a heuristic timer: escalate if the agent hasn't resolved the issue within 3 turns"],correct:0,explanation:"Explicit criteria + few-shot examples is the recommended pattern. The exam guide makes this explicit."},

/* --- 5.3 Error propagation --- */
{domain:"d5",task:"5.3 Error propagation",scenario:"Your multi-agent research system has three subagents: <strong>WebSearchAgent</strong> (web queries), <strong>DocumentParser</strong> (PDF extraction), and <strong>SynthesisAgent</strong> (report generation). The coordinator orchestrates them sequentially. Any subagent can fail independently due to network issues, rate limits, or document parsing errors.",question:"The web search subagent times out while researching a complex topic. You need to design how this failure information flows back to the coordinator agent. Which error propagation approach best enables intelligent recovery?",options:["Return structured error context to the coordinator including the failure type, the attempted query, any partial results, and potential alternative approaches","Implement automatic retry logic with exponential backoff within the subagent, returning a generic 'search unavailable' status only after all retries are exhausted","Catch the timeout within the subagent and return an empty result set marked as successful","Propagate the timeout exception directly to a top-level handler that terminates the entire research workflow"],correct:0,explanation:"This is question 8 from the official guide. Structured error context gives the coordinator what it needs to recover intelligently — retry with modified query, try an alternative approach, or proceed with partial results. Generic statuses hide context. Suppressing errors as success prevents recovery. Top-level termination overreacts to a recoverable failure."},
{domain:"d5",task:"5.3 Error propagation",scenario:"Your multi-agent research system has three subagents: <strong>WebSearchAgent</strong> (web queries), <strong>DocumentParser</strong> (PDF extraction), and <strong>SynthesisAgent</strong> (report generation). The coordinator orchestrates them sequentially. Any subagent can fail independently due to network issues, rate limits, or document parsing errors.",question:"Returning empty results as success on a subagent failure is an anti-pattern because:",options:["It silently suppresses errors, preventing the coordinator from recovering or annotating gaps in the final output","It causes the coordinator to retry the subagent infinitely, consuming unnecessary compute","The API treats empty tool results as a schema validation error and rejects subsequent calls","Downstream subagents interpret empty results as successful completion and over-report confidence"],correct:0,explanation:"Silent suppression prevents recovery and risks delivering incomplete output without acknowledgment."},
{domain:"d5",task:"5.3 Error propagation",question:"Why is a generic 'search unavailable' status worse than a structured error?",options:["It hides valuable context (what was attempted, partial results, failure type) that the coordinator needs to decide what to do next","It conflates transient failures (rate-limit) with permanent ones (invalid query), so the coordinator applies the same retry policy to both regardless of recoverability","It prevents the coordinator from surfacing a human-readable explanation to downstream users who are waiting for the research results","It cannot be cleanly serialized in the structured error schema, causing deserialization errors in the coordinator's error handler for multi-agent responses"],correct:0,explanation:"Generic statuses strip context. Structured errors preserve the information needed for recovery."},
{domain:"d5",task:"5.3 Error propagation",question:"Subagents should implement local recovery for transient failures and propagate only:",options:["What was attempted, partial results obtained, and potential alternative approaches — giving the coordinator the information it needs to decide next steps without re-running work already done","A generic error code and status field — minimal error payloads force the coordinator to re-run the subagent entirely without the context needed to adapt the approach","The full exception stack trace and raw tool output — useful for debugging but too verbose for coordinator decision-making; structured summaries are more actionable","Nothing — coordinators should implement their own health checks and detect subagent failures by timeout rather than waiting for subagent error reports"],correct:0,explanation:"Propagating what was attempted, partial results, and alternatives gives the coordinator actionable context for recovery decisions — it can retry differently, use the partial results, or skip the failed task. Generic error codes lose context. Raw stack traces are too verbose. Silent failures by timeout prevent informed recovery."},
{domain:"d5",task:"5.3 Error propagation",question:"How should synthesis output handle coverage gaps when some subagents failed?",options:["Annotate which findings are well-supported and which topic areas have gaps due to unavailable sources","Produce a complete synthesis using only the available findings, marking sections with limited source coverage as 'low-confidence' without specifying which sources failed","Add a standardized disclaimer at the top of the report noting that some sources were unavailable, without identifying which specific topic areas are incomplete","Replace failed subagents' sections with a placeholder message indicating the topic is outside scope, to avoid raising questions about data quality"],correct:0,explanation:"Coverage annotations preserve transparency about what's well-supported vs. what's missing — critical for downstream decisions."},
{domain:"d5",task:"5.3 Error propagation",question:"Distinguishing an access failure from a valid empty result in error reporting matters because:",options:["Access failures may need retry; valid empty results are successful queries with no matches and don't need retry","Both return the same data payload (nothing), so the distinction is only cosmetic — callers should treat both as 'no data' and proceed identically","Access failures require human escalation while empty results can be returned directly, so conflating them forces unnecessary escalations on every empty query","Empty results from one subagent can be used as a fallback signal to trigger a broader search, while access failures warrant an immediate coordinator-level abort"],correct:0,explanation:"Different failure semantics require different recovery actions. Conflating them wastes retries on empty results or skips real retries."},
{domain:"d5",task:"5.3 Error propagation",question:"Terminating an entire multi-agent workflow on a single subagent failure is:",options:["An anti-pattern — coordinators should attempt partial recovery, annotate gaps, and continue where possible rather than discarding all completed work","Best practice — a workflow with any failure is unreliable; it's better to restart from scratch with a clean state than to synthesize partial results","Required by the Agent SDK — the SDK automatically terminates a coordinator when any spawned subagent returns an error status","Appropriate only for financial workflows — non-critical pipelines should tolerate partial failures, but monetary operations must terminate on any subagent error"],correct:0,explanation:"Aborting the full workflow on a single failure discards all completed subagents' work unnecessarily. The recommended pattern is partial recovery: annotate which parts failed, use available results, and retry only the failed components. The SDK doesn't enforce termination. Blanket termination for financial workflows ignores the availability of partial recovery with appropriate safeguards."},
{domain:"d5",task:"5.3 Error propagation",question:"For subagent transient failures, the recommended pattern is:",options:["Local recovery first; propagate only if local recovery fails, with structured context","Propagate immediately on first failure so the coordinator can route to a backup subagent without delay — local recovery only adds latency","Apply a circuit breaker: after 3 consecutive failures disable the subagent and proceed with partial coverage, annotating the gap in the final output","Retry with exponential backoff up to a fixed budget; if the budget is exhausted, escalate to the coordinator with structured context rather than failing silently"],correct:0,explanation:"Local recovery reduces unnecessary coordinator load. Only propagate what can't be resolved locally, with full context."},

/* --- 5.4 Large codebase context --- */
{domain:"d5",task:"5.4 Large codebase context",question:"In an extended exploration session, the model starts giving inconsistent answers and referencing 'typical patterns' rather than the specific classes it discovered earlier. What's the most effective mitigation?",options:["Have the agent maintain scratchpad files recording key findings, referencing them in subsequent prompts to counteract context degradation","Use <code>/compact</code> to compress earlier turns so the model retains a summary of prior findings while freeing context budget for continued exploration","Inject a brief summary of the key findings at the top of each new turn to re-anchor the model on specific discovered details rather than general training knowledge","Store findings in a vector database and retrieve the most relevant ones before each query, replacing in-context storage with retrieval-augmented context"],correct:0,explanation:"Scratchpad files persist key findings across context boundaries, counteracting degradation. Restarting loses progress."},
{domain:"d5",task:"5.4 Large codebase context",question:"When context fills with verbose discovery output during extended exploration, the recommended command to reduce usage is:",options:["<code>/compact</code>","<code>/clear</code>","<code>/memory</code>","<code>/restart</code>"],correct:0,explanation:"<code>/compact</code> reduces context usage by summarizing prior turns while preserving key information."},
{domain:"d5",task:"5.4 Large codebase context",question:"To investigate 'find all test files' or 'trace refund flow dependencies' while keeping the main agent focused on high-level coordination, you should:",options:["Spawn subagents to investigate specific questions; the main agent stays focused on coordination","Run sequential tool calls within the main agent, using <code>/compact</code> between each investigation to keep context from overflowing during extended file reads","Share a scratchpad MCP resource that both the main agent and investigation tools write findings to, enabling coordination without direct context coupling","Include the full file tree in the main agent's initial context and rely on the model to selectively read only the files relevant to each investigation question"],correct:0,explanation:"Subagent delegation isolates verbose investigation, preserving the main agent's high-level context."},
{domain:"d5",task:"5.4 Large codebase context",question:"For crash recovery in extended sessions, what's the recommended pattern?",options:["Each agent exports state to a manifest file; the coordinator loads the manifest on resume and injects state into agent prompts","Rely on the model retaining the key details across a session restart if the system prompt is preserved","Checkpoint only the final output of each stage; re-run any incomplete stage from scratch on resume","Save the entire conversation transcript and replay it through the model to reconstruct agent state"],correct:0,explanation:"Structured state exports (manifests) enable robust recovery from interruption. The model has no persistent memory across restarts. Replaying full transcripts is expensive and may rerun completed work."},
{domain:"d5",task:"5.4 Large codebase context",question:"Before spawning sub-agents for a new exploration phase, what's the recommended preparation step?",options:["Summarize key findings from the prior phase and inject the summary into the sub-agents' initial context — prevents redundant re-exploration and gives sub-agents a coherent starting point","Start sub-agents with completely empty context — clean state avoids bias from prior findings, but also requires each sub-agent to re-discover baseline facts already established","Pass the entire prior session transcript as each sub-agent's context — comprehensive but likely to overflow context limits and dilute the sub-agent's focus with irrelevant intermediate steps","Let sub-agents inherit context automatically from the coordinator — sub-agents in Claude Code have isolated context; they do not automatically inherit the coordinator's conversation history"],correct:0,explanation:"Injecting a targeted summary of prior findings gives sub-agents the essential context they need without re-running previous work or overloading context with verbose exploration output. Empty context wastes exploration capacity on re-discovery. Full transcripts overflow context. Sub-agent context inheritance is not automatic."},
{domain:"d5",task:"5.4 Large codebase context",question:"Context degradation in long sessions manifests as:",options:["Inconsistent answers and the model defaulting to 'typical patterns' instead of specific discovered details","The model begins requesting <code>/compact</code> automatically when it detects that earlier findings are no longer in context","Tool calls start returning stale cached results because the context window no longer contains the original file contents","The model's responses become shorter and more terse as it prioritizes brevity to conserve remaining context budget"],correct:0,explanation:"Context degradation produces inconsistent, generalized answers — the model stops referencing specific classes, functions, or findings it discovered earlier and falls back to generic descriptions. The other options describe behaviors that don't occur — /compact is user-invoked, tool results are not cached from context, and response length is not automatically throttled."},
{domain:"d5",task:"5.4 Large codebase context",question:"For a long codebase exploration that exceeds your context budget, the right pattern is:",options:["Delegate verbose exploration to subagents that return summaries; preserve the main agent for coordination","Have the main agent read files incrementally and run <code>/compact</code> after each module to reclaim budget, continuing until the full codebase is covered","Use a retrieval-augmented approach — index the codebase in a vector database and inject only the most relevant code chunks per query","Expand the context budget by using a model tier with a larger context window, enabling the full codebase to fit in a single session"],correct:0,explanation:"Subagent delegation for verbose work + main-agent coordination keeps context manageable."},
{domain:"d5",task:"5.4 Large codebase context",question:"What's the relationship between scratchpad files and the <code>/compact</code> command?",options:["Scratchpad files persist findings across context boundaries; <code>/compact</code> reduces in-context tokens — they're complementary","They are interchangeable — both reduce the amount of information stored in the active context window; the choice between them is a matter of preference","Scratchpad files are only needed when <code>/compact</code> is unavailable — once compact is applied, the compacted summary is sufficient for preserving findings across phases","<code>/compact</code> automatically writes a scratchpad file after summarization so that the full prior context can be restored if the compacted summary proves insufficient"],correct:0,explanation:"Scratchpads persist data outside context; compact reduces in-context tokens. They serve complementary purposes."},

/* --- 5.5 Human review and confidence --- */
{domain:"d5",task:"5.5 Human review",question:"Your extraction pipeline shows 97% overall accuracy. Why might this aggregate metric be misleading?",options:["Aggregate accuracy can mask poor performance on specific document types or fields — you need stratified analysis","97% is a reasonable threshold but should be monitored for model drift over time","97% aggregate accuracy is sufficient to eliminate human review entirely for most enterprise deployments","Aggregate accuracy is only meaningful if the validation set had at least 10,000 documents"],correct:0,explanation:"Aggregate metrics can hide systematic failures in specific segments. Stratified analysis by document type and field is essential."},
{domain:"d5",task:"5.5 Human review",question:"To detect novel error patterns in high-confidence extractions, the recommended approach is:",options:["Stratified random sampling of high-confidence extractions for periodic human review — novel errors concentrate in this tier precisely because the model's confidence prevents them from reaching review through standard routing","Reviewing only low-confidence extractions — novel errors in high-confidence extractions are invisible to low-confidence routing; sampling only the uncertain tier misses the silent failure mode","Reviewing a fixed 1% random sample regardless of confidence level — flat random sampling can work but doesn't specifically target the high-confidence tier where novel errors hide","Skipping review of high-confidence extractions entirely — 'high confidence' reflects model self-assessment, not ground truth; systematic errors can persist undetected without any sampling"],correct:0,explanation:"Stratified sampling specifically covers the high-confidence tier where novel errors are most likely to go undetected — standard routing never sends these to human review. Low-confidence-only review creates a blind spot. Flat random sampling is better than nothing but less targeted. Skipping high-confidence review entirely allows novel systematic errors to compound undetected."},
{domain:"d5",task:"5.5 Human review",question:"For routing extractions to human review, the most reliable signal is:",options:["Field-level model confidence scores calibrated against a labeled validation set","The raw confidence score returned by the model without additional calibration","Document length in pages — longer documents correlate with harder extractions","The number of fields extracted — documents with more populated fields are easier and need less review"],correct:0,explanation:"Field-level confidence, calibrated against labeled data, is the recommended routing signal. Uncalibrated confidence is unreliable."},
{domain:"d5",task:"5.5 Human review",question:"Before reducing human review, what should you verify?",options:["That accuracy is consistent across document types and field segments — not just overall average accuracy, which can mask systematic failures in specific subcategories","Human reviewer approval for the reduction plan — reviewer buy-in matters operationally, but it doesn't validate whether the model's accuracy is high enough to safely reduce oversight","Cost savings from reduced review outweigh projected cost of residual errors — financial justification is important but secondary to verifying the accuracy baseline is actually trustworthy","Model version and prompt stability since the last accuracy baseline — important for reproducibility, but accuracy consistency across document types is the more fundamental check"],correct:0,explanation:"Consistency across document types and segments is the critical check — aggregate accuracy can look high while the model systematically fails on specific document formats or edge cases. Reviewer approval, cost justification, and prompt stability are all operationally relevant but don't substitute for confirming that accuracy holds across the full distribution."},
{domain:"d5",task:"5.5 Human review",question:"You want to route ambiguous or contradictory source documents to human review. What's the best signal?",options:["A model-emitted <code>conflict_detected</code> or <code>low_confidence</code> indicator at the field or document level — the model that processed the document is best positioned to flag its own uncertainty","A word-count threshold — routing all documents above a certain length assumes longer documents are more ambiguous, which is not reliably true","Presence of numeric data — flagging all documents with numbers for human review is too broad and would route most financial or scientific documents regardless of clarity","Processing timestamp — using time of day or document age as a routing signal has no relationship to document ambiguity or contradictions"],correct:0,explanation:"Model-emitted uncertainty flags (conflict_detected, low_confidence) are the most reliable routing signal — the model that processed the document is best positioned to report where it encountered contradictions or lacked confidence. Word count, numeric data presence, and timestamps are unrelated to actual document ambiguity."},
{domain:"d5",task:"5.5 Human review",question:"For calibrating field-level confidence scores, you should:",options:["Map model confidence to actual accuracy using a labeled validation set — raw model confidence is uncalibrated; calibration reveals the true accuracy at each confidence level","Use the model's raw confidence output directly as a routing threshold — raw confidence is systematically biased and doesn't reliably reflect actual accuracy without calibration","Apply a fixed threshold of 0.5 across all field types — a universal threshold ignores that different fields have different base rates and error distributions","Set the threshold based on business risk tolerance alone without measuring empirical accuracy — risk tolerance informs the decision but must be applied to calibrated accuracy curves, not raw scores"],correct:0,explanation:"Calibration against a labeled validation set converts raw model confidence into meaningful accuracy estimates — without calibration, 0.9 confidence might correspond to 70% actual accuracy. Raw confidence is unreliable. A fixed 0.5 threshold ignores field-level accuracy variance. Risk tolerance sets the policy but requires calibrated accuracy data to apply correctly."},

/* --- 5.6 Provenance --- */
{domain:"d5",task:"5.6 Provenance",scenario:"Your multi-source synthesis system aggregates findings from a web-search subagent, a document-analysis subagent, and a structured-data subagent. Each subagent finds different statistics about the same metric from different sources with different publication dates.",question:"After multi-source synthesis, the final report cites no sources even though subagents found them. What's the typical root cause?",options:["Source attribution is lost during summarization steps when findings are compressed without preserving claim-source mappings","The synthesis model lacks access to the URL field because it is stripped by the tool schema","The model redacts URLs by default to prevent prompt injection from malicious source content","Citations are included in tool call arguments but not in tool results, so the synthesizer never sees them"],correct:0,explanation:"Summarization without explicit claim-source mappings loses attribution. Require subagents to output structured mappings that downstream agents preserve."},
{domain:"d5",task:"5.6 Provenance",scenario:"Your multi-source synthesis system aggregates findings from a web-search subagent, a document-analysis subagent, and a structured-data subagent. Each subagent finds different statistics about the same metric from different sources with different publication dates.",question:"Two credible sources report different statistics for the same metric. Best practice is:",options:["Annotate both values with their sources rather than arbitrarily selecting one","Pick the higher value","Pick the lower value","Average them and report the mean"],correct:0,explanation:"Conflicting credible sources require annotation, not arbitrary selection. Lossy reconciliation hides important uncertainty."},
{domain:"d5",task:"5.6 Provenance",question:"To prevent temporal differences (e.g., 2023 vs 2024 data) from being misinterpreted in synthesis:",options:["Include publication or collection dates in structured outputs — downstream agents can then filter, compare, or contextualize findings by time period","Strip all dates from the output and present findings as timeless facts — removes temporal context needed for correct interpretation; a 2023 regulatory finding may no longer be valid","Filter to only the most recent data before synthesis — newer data is not always more relevant; older authoritative sources may carry more weight for certain analyses","Merge all sources into a single time-aggregated summary — aggregation without temporal tracking is precisely what causes temporal misinterpretation"],correct:0,explanation:"Including dates preserves temporal provenance so downstream agents can correctly interpret findings relative to time periods. Stripping dates removes the context needed to identify temporal conflicts. Filtering to only recent data discards potentially valid older sources. Time-aggregated summaries without date tracking cause the misinterpretation problem this pattern is designed to prevent."},
{domain:"d5",task:"5.6 Provenance",question:"Synthesis reports should:",options:["Distinguish well-established findings from contested ones, preserving original source context and confidence levels rather than presenting all findings as equally certain","Present all findings as equally certain to avoid confusing readers with uncertainty qualifications — flattening confidence differences hides important reliability distinctions","Drop contested findings to maintain a coherent, definitive narrative — contested findings often represent the most important areas of uncertainty and should be surfaced, not suppressed","Drop well-established findings to focus the report on unresolved questions — established findings provide the baseline context needed to interpret contested ones"],correct:0,explanation:"Synthesis reports should preserve the confidence hierarchy from the source data — well-established findings versus contested ones carry different weights. Presenting all findings as equally certain flattens important distinctions. Dropping contested or established findings both lose critical information: contested findings reveal uncertainty, established findings provide context."},
{domain:"d5",task:"5.6 Provenance",question:"For multi-source synthesis, subagents should be required to output:",options:["Structured claim-source mappings — each finding paired with its source document, URL, or excerpt so the synthesis agent can verify provenance and detect contradictions across sources","Plain prose summaries — human-readable but unstructured; the synthesis agent cannot programmatically match claims to sources or detect when two prose summaries contradict each other","Source URLs only without content — provides attribution but not the claim content or excerpt needed for the synthesis agent to evaluate relevance or identify conflicts","A single consolidated paragraph per subagent — discards the source-level structure needed for cross-source verification and makes contradiction detection impossible"],correct:0,explanation:"Structured claim-source mappings give the synthesis agent verifiable provenance: it can match claims to sources, detect contradictions between sources, and produce attributed outputs. Plain prose, URL-only lists, and consolidated paragraphs all lose the claim-level structure needed for reliable multi-source synthesis."},
{domain:"d5",task:"5.6 Provenance",question:"In a synthesis output, financial data and news content should be rendered:",options:["According to content type — financial data as structured tables (for comparison), news as prose narrative (for context), technical findings as code or structured lists","In a single prose narrative regardless of content type — simplifies downstream text processing but makes financial comparisons unreadable and obscures tabular relationships","As machine-readable JSON for all content — aids programmatic processing but produces unreadable synthesis for human reviewers and loses the narrative flow of news articles","As bullet lists for everything — loses the relational structure of financial tables (where row/column position carries meaning) and the narrative thread of news content"],correct:0,explanation:"Rendering by content type preserves each category's natural structure: financial data as tables for comparison, news as prose for narrative, technical findings as structured lists. A single format — prose, JSON, or bullets — either loses structure or produces unreadable output for at least one content type."},

/* ============================================================
   ADDITIONAL CROSS-DOMAIN QUESTIONS
   ============================================================ */
{domain:"d1",task:"1.1 Agentic loop",question:"In your agentic loop you receive <code>stop_reason: \"tool_use\"</code> with two tool calls in the same response. What's the recommended way to handle this?",options:["Execute both tools and return their results together in the next request — supports parallel tool execution","Execute only the first tool; ignore the second","Execute them sequentially across separate requests","Reject the response and ask again"],correct:0,explanation:"The API supports multiple tool calls per turn. Execute all requested tools and return all results together in the next request."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"Your multi-agent news aggregation system spawns four parallel subagents to fetch headlines from different regions. During today's run, the Asia-Pacific subagent returns a 503 timeout while the other three complete successfully. The coordinator receives three complete result sets and one failure notification. A junior engineer suggests aborting the entire workflow and retrying all four. What is the recommended recovery pattern?",options:["Re-delegate that specific subagent with a targeted retry, or proceed with partial results annotated as incomplete coverage","Terminate the entire workflow — partial results are unreliable and should not be synthesized","Re-run all four subagents from scratch to ensure data from the same time window","Mark the workflow as a permanent failure and alert the operator — partial data cannot be used"],correct:0,explanation:"Targeted re-delegation or graceful degradation with annotated partial results is the recommended pattern for transient subagent failures. Aborting the entire workflow (B) discards three successful subagents' work unnecessarily — a 503 is transient, not fundamental. Re-running all four (C) wastes resources on subagents that already succeeded and introduces inconsistent timing. Marking as permanent failure (D) over-reacts to a clearly recoverable timeout."},
{domain:"d2",task:"2.2 Structured errors",question:"For an MCP tool that returns information about a single record, what's the right response when no record is found?",options:["A successful response with an explicit empty result — a null or empty structured object signals 'not found' as a normal outcome without triggering the model's error-handling path","An <code>isError: true</code> response with a 'record not found' message — treating absence as an error causes the model to enter error-recovery mode for a normal, expected outcome","Raise an exception in the tool implementation — unhandled exceptions surface as tool errors to the model, again triggering error-recovery behavior for what should be a normal case","Return a dummy record with placeholder values — fabricating data prevents the model from knowing the record doesn't exist, leading to downstream reasoning on invented content"],correct:0,explanation:"A successful empty result correctly signals 'not found' as a normal, expected outcome — the model can handle it with normal logic rather than entering error recovery. isError: true and exceptions both trigger error handling for a situation that isn't an error. Returning dummy data fabricates facts the model will reason on."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",scenario:"Your team uses a monorepo with a React frontend (<code>src/frontend/</code>), a Node.js API (<code>src/api/</code>), Terraform infrastructure code (<code>infrastructure/</code>), and database migration scripts (<code>db/migrations/</code>). Each area has different code style conventions and review standards. Multiple engineers, each with personal preferences, use Claude Code.",question:"You see different agent behavior across two team members on the same project. What's a likely cause?",options:["Differences in user-level CLAUDE.md or per-user settings — use <code>/memory</code> to compare loaded files","The model's inherent generation variance — since Claude samples probabilistically, two identical prompts will occasionally produce different outputs even with the same configuration","Each developer is hitting a different API rate limit tier, causing the model to use a fallback configuration with reduced capability for the developer under rate pressure","One developer's network route passes through a different edge node, which applies a different inference configuration that changes the response distribution"],correct:0,explanation:"Per-user CLAUDE.md or per-user skills can cause inconsistent behavior. <code>/memory</code> shows what's loaded — useful diagnostic."},
{domain:"d4",task:"4.3 Structured output",scenario:"You are building an accounts-payable automation pipeline. The pipeline receives scanned invoices and must extract structured data into a JSON schema: <strong>vendor_name</strong>, <strong>invoice_number</strong>, <strong>total_amount</strong>, <strong>line_items</strong> (array), and <strong>due_date</strong>. Some invoices are handwritten, poorly scanned, or missing fields.",question:"For an extraction tool whose input is a JSON schema, where should format normalization rules live?",options:["In the prompt alongside the schema — schemas don't normalize input formats","In the schema itself","In a separate MCP server","In the system prompt only"],correct:0,explanation:"Schemas validate structure but don't normalize content. Normalization belongs in the prompt."},

{domain:"d1",task:"1.4 Enforcement & handoffs",question:"A workflow requires that <code>verify_identity</code> succeeds before <code>transfer_funds</code>. Production logs show <code>transfer_funds</code> running on failed verifications. What's the fix?",options:["Add a programmatic prerequisite gate that blocks <code>transfer_funds</code> unless a verified identity token from <code>verify_identity</code> is present in its parameters — deterministic enforcement at the application layer","Tighten the system prompt with uppercase 'CRITICAL: verify identity MUST succeed before transfer' — system prompt ordering rules are probabilistic; this pattern has already failed in production","Add few-shot examples showing the correct sequence across varied scenarios — examples improve consistency but are still probabilistic; the failure is a compliance violation requiring deterministic enforcement","Set <code>tool_choice: \"any\"</code> on each turn to force tool calls — this ensures some tool is called but provides no mechanism for enforcing call ordering between specific tools"],correct:0,explanation:"A programmatic prerequisite gate is the only deterministic fix — transfer_funds is blocked at the application layer unless verify_identity's token is present. System prompt rules, few-shot examples, and tool_choice all improve model behavior probabilistically but cannot provide ordering guarantees for compliance-critical sequencing."},
{domain:"d2",task:"2.4 MCP integration",question:"An MCP server you configured doesn't appear in the available tools list. Most likely cause?",options:["The server isn't connecting (auth failure, config path wrong, or server process not running)","The MCP server process crashed after startup — check the server logs rather than Claude Code's own output for the root cause","The Claude model being used doesn't support tool use — switch to a model in the claude-3 family or later that includes tools capability","The CLAUDE.md file is missing from the project — add it to enable automatic MCP server discovery for project-level configurations"],correct:0,explanation:"Missing tools usually mean the MCP server isn't connecting. Check config path, auth, and process status."},
{domain:"d3",task:"3.4 Plan vs direct",question:"You're combining plan mode for design with direct execution for implementation. Where does the Explore subagent typically fit?",options:["Inside plan mode, isolating verbose discovery so the main session retains a clean high-level context","Inside direct execution — the Explore subagent runs after plan mode produces the design, letting it discover implementation details without burdening the planning context","The Explore subagent replaces plan mode for tasks where discovery is the primary goal — use it instead of plan mode when you don't need a formal design document","The Explore subagent runs as a post-processing step after both phases complete, summarizing what was changed and generating a diff-based audit report"],correct:0,explanation:"Explore is a tool for plan mode's discovery phase, isolating verbose output to preserve main context."},
{domain:"d4",task:"4.5 Batch processing",question:"You're considering switching pre-merge linting from synchronous to batch API for cost savings. Should you?",options:["No — pre-merge checks are blocking; developers wait for results before merging, and the batch API's 24-hour processing window violates the latency requirements of a blocking workflow","Yes — the batch API costs 50% less per token, so switching linting to batch reduces CI costs without affecting developers","Yes, but only for style checks — security and correctness checks remain synchronous since they're blocking, while style is informational","Yes — run both APIs in parallel and return whichever responds first, combining the batch cost savings with synchronous latency guarantees"],correct:0,explanation:"Pre-merge linting is a blocking workflow: developers need results before they can merge. The batch API processes requests within 24 hours — incompatible with this latency requirement. Cost savings are irrelevant if the workflow breaks. Splitting by check type still blocks on some checks, and 'run both' doesn't work since batch responses aren't immediate."},
{domain:"d1",task:"1.5 Agent SDK hooks",question:"Your legal team requires a complete audit trail of every tool call made by your contract analysis agent — tool name, input parameters, timestamp, and output summary — stored in an immutable compliance log. Critically, the audit logging must not alter the tool results the agent receives, since any modification would invalidate the legal chain of custody. Which hook architecture satisfies both requirements?",options:["A read-only <code>PostToolUse</code> hook that logs call metadata and results to the compliance store without transforming the result passed to the model","A pre-execution interceptor that logs call parameters, then conditionally blocks calls matching sensitive patterns","A system prompt instruction telling the agent to self-report its tool calls in a structured JSON block after each turn","A wrapper tool the model must call before every substantive tool, passing parameters for logging before the actual call"],correct:0,explanation:"A read-only <code>PostToolUse</code> hook observes the full tool interaction (inputs + outputs) for logging without modifying the result the model receives, preserving chain of custody. Pre-execution interception (B) only captures inputs, not outputs, and introduces conditional blocking that could interfere with agent behavior. System prompt self-reporting (C) is probabilistic and can be skipped. A wrapper tool (D) the model calls voluntarily can be skipped and adds coupling."},
{domain:"d2",task:"2.1 Tool descriptions",scenario:"You are building a CRM automation agent. The agent has access to eight tools: <code>get_customer</code>, <code>update_customer</code>, <code>create_invoice</code>, <code>void_invoice</code>, <code>lookup_order</code>, <code>update_order</code>, <code>send_notification</code>, and <code>process_refund</code>. The agent must reliably select the correct tool and gracefully handle failures.",question:"You're writing a description for a <code>create_invoice</code> tool. The MOST important content is:",options:["What it does, expected inputs (with example values), the format of its output, and when to use it vs. similar tools","The author name, package version, and changelog link — so developers can track which version introduced the current behavior","The average cost per invocation and typical latency, so the model can make cost-aware decisions about when to call it","Internal implementation notes about the invoice generation algorithm, including database query patterns and caching strategy"],correct:0,explanation:"Descriptions for LLMs should equip the model to select and use the tool correctly — function, inputs, output, and disambiguation."},

{domain:"d4",task:"4.2 Few-shot",question:"Your extraction prompt produces inconsistent output formats across runs. The most reliable fix is:",options:["Add 2-3 few-shot examples that demonstrate the exact desired output format","Set temperature to 0 so the model always produces the same token sequence, locking in a consistent output structure","Provide an explicit JSON schema in the prompt that the model must conform to, using <code>tool_choice</code> to force schema adherence","Append a post-processing regex that normalizes the output into the expected format regardless of what the model returns"],correct:0,explanation:"Few-shot examples are the most reliable format-locking technique. Temperature 0 reduces randomness but doesn't address format intent."},
{domain:"d5",task:"5.2 Escalation",question:"An agent decides to escalate every case where its confidence falls below 0.9. After deployment, escalation volume balloons because the model's confidence is uncalibrated. Best fix?",options:["Replace confidence-based routing with explicit categorical escalation criteria (policy gaps, explicit customer requests, inability to progress)","Lower the threshold to 0.5 — a more permissive threshold reduces unnecessary escalations while keeping the confidence-based routing architecture","Calibrate the confidence scores against a labeled validation set before setting a threshold, making the 0.9 cutoff empirically grounded","Combine confidence-based routing with a secondary sentiment classifier so that only cases with both low confidence and negative sentiment are escalated"],correct:0,explanation:"LLM self-confidence is poorly calibrated. Explicit categorical criteria with few-shot examples is the recommended replacement."},
{domain:"d1",task:"1.3 Subagent invocation",question:"Your coordinator agent is processing a market research request that requires simultaneously gathering stock prices, news sentiment, and regulatory filings for five companies. The coordinator's response contains three <code>Task</code> tool calls: one for financial data, one for news, one for filings. How does the Agent SDK execute these three tasks?",options:["The Agent SDK runs all three <code>Task</code> calls in parallel — the coordinator's loop resumes when all three complete and their results are available","The three tasks are queued and executed sequentially — the SDK processes <code>Task</code> calls one at a time in the order they appear","Only the first <code>Task</code> call executes; the coordinator must emit each subsequent task in a separate turn after receiving the prior result","The tasks execute in non-deterministic order determined by which subagent's context window is available first"],correct:0,explanation:"Multiple <code>Task</code> tool calls emitted in a single coordinator response execute in parallel — this is the primary mechanism for concurrent subagent dispatch. The coordinator's loop resumes only when all spawned tasks complete, giving it all results at once. Sequential execution (B) defeats the purpose of parallel dispatch and would require separate turns. Only executing the first (C) is incorrect — all tool calls in a single response are processed. Non-deterministic ordering (D) is a distractor; the execution is parallel, not random."},
{domain:"d2",task:"2.3 Tool distribution",question:"For a synthesis subagent that should produce text and never call tools (output only), the correct constraint is:",options:["Restrict <code>allowedTools</code> to an empty set — removing all tools from the subagent's definition makes it impossible to call any tools, regardless of what the system prompt requests","<code>tool_choice: \"any\"</code> — this forces the model to call at least one tool every turn, which is the opposite of what's needed for a text-only output subagent","A system prompt instruction: 'Only produce text output; never call tools' — probabilistic; the model may still emit tool calls under certain inputs if tools are available","Use plan mode — plan mode controls whether files are modified before planning, not whether tool calls can be made during execution"],correct:0,explanation:"An empty allowedTools set is the deterministic constraint — with no tools registered, the model cannot call any tools regardless of instructions. tool_choice: \"any\" mandates tool calls, the opposite of the goal. System prompt prohibitions are probabilistic. Plan mode controls execution safety, not tool availability."},
{domain:"d3",task:"3.5 Iterative refinement",question:"You have three independent bugs to fix. The recommended pattern is:",options:["Fix each bug in a separate iteration so changes are isolated — this makes it easy to attribute any unexpected behavior to the specific fix that introduced it","Fix all three in a single pass to reduce the number of API calls and review cycles","Fix them in order of estimated difficulty, combining the two simplest into one pass to save time","Delegate all three bugs to parallel subagents that each make their own changes, then merge the results"],correct:0,explanation:"Separate iterations isolate cause and effect — if an unexpected regression appears, you know exactly which fix introduced it. Combining independent fixes in a single pass makes attribution ambiguous. Difficulty-based batching still mixes changes. Parallel subagents risk conflicting edits to shared code."},
{domain:"d4",task:"4.1 Explicit criteria",question:"A code reviewer prompt currently says 'flag bad code'. What is the most direct fix?",options:["Define 'bad code' as specific categorical criteria (e.g., 'flag unhandled exceptions, SQL injection, dead code')","Append a system prompt emphasizing the importance of precision: 'Be rigorous — only flag clear, high-impact issues with unambiguous evidence'","Replace 'flag bad code' with 'flag any code that could be improved' to broaden the scope and capture more issues","Add a few-shot example of a good code review to calibrate what 'bad code' should look like in context"],correct:0,explanation:"Categorical criteria operationalize 'bad code' so the model can apply judgment consistently."},
{domain:"d5",task:"5.4 Large codebase context",question:"During a long exploration, you summarize each phase before moving to the next. Why is this effective for context management?",options:["Phase summaries injected at the start of each new phase counteract context degradation by giving the model fresh, high-signal starting context instead of relying on attention over a long history","End-of-phase summaries allow /compact to retain high-level findings while discarding verbose tool output — /compact performs its own summarization; end-of-phase summaries are for the next phase, not for /compact optimization","Natural-language summaries are preserved preferentially by /compact over raw tool results — /compact doesn't apply content-type preferences; it summarizes based on recency and apparent importance","Each new phase begins with a dense, targeted context block rather than a sparse context spread across many earlier turns — accurate, but the key benefit is signal quality and resistance to context degradation, not density per se"],correct:0,explanation:"Phase summaries counteract context degradation: instead of the model attending over a long, diluted history, each phase starts with a compact, high-signal summary of what was learned. Options B and C describe /compact behavior incorrectly. Option D is partially accurate but frames the benefit as 'density' rather than the actual mechanism: providing fresh accurate context that doesn't compete with a long degraded history."},

{domain:"d2",task:"2.2 Structured errors",scenario:"You are building a CRM automation agent. The agent has access to eight tools: <code>get_customer</code>, <code>update_customer</code>, <code>create_invoice</code>, <code>void_invoice</code>, <code>lookup_order</code>, <code>update_order</code>, <code>send_notification</code>, and <code>process_refund</code>. The agent must reliably select the correct tool and gracefully handle failures.",question:"Your tool returns generic 'failed' for everything. The agent now retries permanent failures and tells users to 'try again later' for fixable issues. What single change has the biggest impact?",options:["Return structured error metadata with <code>errorCategory</code>, <code>isRetryable</code>, and a customer-friendly description","Add a <code>retry_after</code> timestamp to every error response, letting the agent calculate when to retry without needing to classify the error type","Increase the model's <code>max_tokens</code> budget so it has room to reason through the error context and make a more nuanced retry decision","Switch to a larger model with higher instruction-following reliability, reducing the likelihood that it misinterprets 'failed' as always retryable"],correct:0,explanation:"Structured error metadata is the single biggest improvement — it lets the agent route, retry, or explain appropriately."},
{domain:"d4",task:"4.4 Validation & retry",question:"Retry-with-error-feedback can succeed for:",options:["Format mismatches and structural output errors — the model made a formatting mistake it can correct when shown the violation","Missing required fields the model overlooked in the source document — retry with an explicit directive to re-check completeness","Hallucinated values in extraction outputs — providing the error prompts the model to reconsider and produce a grounded answer","Conflicting data within the source document — retry with a clarification request enables the model to reconcile the contradiction"],correct:0,explanation:"Format and structural errors are recoverable — the model made a formatting mistake and can correct it when told exactly what went wrong. Missing fields (B) are only retrievable if the data actually exists in the source; retry can help but the problem may be absent data, not oversight. Hallucinated values (C) cannot be reliably corrected by retry if the source lacks the information. Conflicting source data (D) is a real problem but requires clarification from the source owner, not a retry."},
{domain:"d1",task:"1.6 Task decomposition",question:"A junior engineer creates a fixed five-step pipeline for debugging unknown errors. What's the architectural problem?",options:["Fixed pipelines cannot adapt to what's discovered — in open-ended debugging, each finding should reshape the next step; a hardcoded sequence continues regardless of what the prior step revealed","Five steps is too few for thorough debugging — the number of steps is not the core problem; the issue is that steps are fixed rather than adaptive","Fixed pipelines are slower than dynamic ones — performance isn't the concern; a fixed pipeline may reach a wrong conclusion quickly rather than a right one slowly","The model cannot follow more than three sequential instructions reliably — sequential instructions are not inherently unreliable; the problem is the inability to branch based on findings"],correct:0,explanation:"Fixed pipelines can't adapt to intermediate discoveries — in debugging, what you find in step 2 determines what step 3 should be, not a predetermined sequence. The problem isn't the number of steps, speed, or instruction-following reliability; it's the inability to reshape the investigation based on findings."},
{domain:"d5",task:"5.6 Provenance",question:"Your synthesis output erases publication dates from individual findings to create a cleaner report. Why is this a problem?",options:["Downstream agents and readers cannot tell whether a finding reflects 2023 or 2024 conditions — temporal context is often essential for correctly interpreting regulatory, market, or scientific claims","Publication dates are required metadata under most data retention policies — compliance is a valid concern, but the core problem is interpretability, not regulatory compliance","Removing dates increases the synthesis report's token count because the model must add caveats — date removal actually reduces token count; the issue is loss of provenance, not token efficiency","The synthesis model needs dates to sort findings chronologically in the output — sorting is a presentation concern; the deeper issue is that readers can't assess whether findings are still current"],correct:0,explanation:"Erasing publication dates removes temporal context that's essential for correct interpretation — a regulatory finding from 2023 may be superseded by 2024 changes. Compliance and sorting are secondary concerns. The core issue is that readers and downstream systems cannot assess the currency or validity of findings without knowing when they were produced."},
{domain:"d2",task:"2.5 Built-in tools",question:"For finding all callers of a specific function, the recommended built-in tool is:",options:["<code>Grep</code> — searches file contents across the codebase for the function name by pattern, returning all matching locations","<code>Glob</code> — matches files by name pattern, useful for finding files named after the function but not for finding where it's called within file contents","<code>Read</code> on each file — loads complete file contents for manual inspection, but requires iterating through every file individually rather than searching in one pass","<code>Edit</code> — uses text matching as part of replacement operations; not designed for search-only operations across a codebase"],correct:0,explanation:"Grep searches file contents by pattern — the right tool for locating all callers of a function across a codebase. Glob matches filenames, not content. Read loads individual files; it can't efficiently search across many files. Edit uses matching for replacement, not as a standalone search."},
{domain:"d3",task:"3.3 Path-specific rules",question:"A rule should apply to React component files anywhere in the repo. The path glob pattern is:",options:["<code>**/*.tsx</code> (or <code>**/components/**/*.tsx</code> if components are in dedicated directories) — these patterns match .tsx files at any directory depth","<code>/*.tsx</code> — this matches only .tsx files at the repository root; the leading <code>*</code> without <code>**</code> doesn't recurse into subdirectories","<code>src/**</code> — this matches all files under src/ but doesn't filter to .tsx extension; it would load the rule for non-component files too","<code>components/*.tsx</code> — this matches .tsx files only in a top-level components/ directory, missing nested or differently organized component trees"],correct:0,explanation:"**/*.tsx matches all .tsx files at any depth in the repository. /*.tsx matches only at the root. src/** is too broad (no extension filter). components/*.tsx is too narrow (no recursive matching for nested structures)."},
{domain:"d4",task:"4.6 Multi-pass review",question:"You want to run a code review that's more reliable than self-review. The recommended pattern is:",options:["Use a second, independent Claude instance without the generator's context","Have the generator self-review with longer prompts","Use extended thinking only","Re-run the generator with higher temperature"],correct:0,explanation:"Independent review instances are more reliable than self-review because they aren't anchored on the generator's reasoning."},
{domain:"d1",task:"1.7 Session management",question:"You worked on a refactor yesterday in session <code>refactor-2024-11-01</code>. Today, several engineers changed the affected files while you were away. How do you continue effectively?",options:["Resume with <code>--resume refactor-2024-11-01</code>, then explicitly tell the agent which prior findings are still valid given yesterday's changes before continuing","Resume and let the agent figure out which prior context is still valid — the model will use prior tool results without knowing which files changed; it may reason from stale information without explicit guidance","Start completely fresh with no summary — loses all valid prior analysis unnecessarily; prior findings about unchanged parts of the codebase are still useful","Wait for the other engineers to finish their changes before resuming — collaboration requires resuming with awareness of changes, not pausing until all changes stop"],correct:0,explanation:"Resume with the named session but proactively inform the agent about which prior findings are stale due to yesterday's changes. Resuming without guidance risks reasoning from outdated tool results. Starting fresh discards valid analysis. Waiting is impractical; engineers need to work concurrently."},
{domain:"d5",task:"5.5 Human review",question:"You want to reduce human review workload while maintaining quality. The recommended approach is:",options:["Calibrate confidence using a validation set and route only low-confidence or flagged extractions to human review — high-confidence items are skipped only after confirming their accuracy through calibration","Reduce review uniformly by reviewing every other item regardless of confidence — uniform reduction ignores the signal in confidence scores; high-confidence items are treated identically to uncertain ones","Skip reviewing high-confidence extractions entirely without calibration — without calibration, 'high confidence' is the model's self-reported certainty, not an empirically validated accuracy level","Have humans review only items flagged by a secondary validation rule — rule-based flags catch known error patterns but miss novel errors that haven't been codified into rules yet"],correct:0,explanation:"Calibrated confidence-based routing is the right balance: validate confidence scores against labeled data, then confidently skip the high-confidence tier while reviewing uncertain or flagged items. Uniform reduction wastes review on obvious cases and skips uncertain ones equally. Uncalibrated skipping trusts self-reported confidence. Rule-based flags catch known errors but miss novel patterns."},
{domain:"d2",task:"2.1 Tool descriptions",scenario:"You are building a CRM automation agent. The agent has access to eight tools: <code>get_customer</code>, <code>update_customer</code>, <code>create_invoice</code>, <code>void_invoice</code>, <code>lookup_order</code>, <code>update_order</code>, <code>send_notification</code>, and <code>process_refund</code>. The agent must reliably select the correct tool and gracefully handle failures.",question:"A vague tool description leads to:",options:["Unreliable tool selection — especially when similar tools coexist; without clear descriptions, the model cannot consistently choose the right tool","Slower API response times — the model spends more inference time reasoning about tool choices when descriptions are ambiguous","Higher hallucination rates in tool outputs — vague descriptions affect tool selection, not the accuracy of the data tools return","Schema validation failures — tool descriptions don't affect parameter validation; schema errors come from incorrect parameter types, not description quality"],correct:0,explanation:"Vague descriptions cause unreliable tool selection — the model picks the wrong tool when it can't differentiate similar tools by description. API response time is not meaningfully affected by description quality. Hallucinations in outputs are driven by model and data issues, not description clarity. Schema validation is independent of descriptions."},
{domain:"d3",task:"3.1 CLAUDE.md hierarchy",question:"In what order does CLAUDE.md scope apply (most specific to least)?",options:["Subdirectory CLAUDE.md → Project CLAUDE.md → User CLAUDE.md — more specific (closer to the edited file) overrides less specific (farther away or global)","User CLAUDE.md → Project CLAUDE.md → Subdirectory CLAUDE.md — applying global settings first before progressively narrowing to project-specific rules","Project CLAUDE.md → User CLAUDE.md → Subdirectory CLAUDE.md — project rules take precedence over both user preferences and subdirectory overrides","All scopes are applied simultaneously with equal priority; conflicts are resolved by the model based on relevance to the current task"],correct:0,explanation:"CLAUDE.md scope is hierarchical: subdirectory (most specific, closest to the file) → project root → user-level (most general). More specific scopes override less specific ones. The order is not reversed; global settings don't take precedence over project-specific ones. Scopes are not equal-priority."},
{domain:"d4",task:"4.3 Structured output",scenario:"You are building an accounts-payable automation pipeline. The pipeline receives scanned invoices and must extract structured data into a JSON schema: <strong>vendor_name</strong>, <strong>invoice_number</strong>, <strong>total_amount</strong>, <strong>line_items</strong> (array), and <strong>due_date</strong>. Some invoices are handwritten, poorly scanned, or missing fields.",question:"For multiple extraction schemas when document type isn't known, the right <code>tool_choice</code> setting is:",options:["<code>\"any\"</code> — model must call one of the available extraction tools","<code>\"auto\"</code> — model may return text","Forced to a specific tool","No setting"],correct:0,explanation:"<code>\"any\"</code> guarantees a tool call when you have multiple candidate extraction schemas and don't know in advance which fits."},
{domain:"d5",task:"5.3 Error propagation",question:"For multi-agent error reporting, the recommended fields in a structured error are:",options:["Failure type, what was attempted, partial results, and potential alternatives","Random ID and timestamp only","The full stack trace","No fields — just a string"],correct:0,explanation:"These fields give the coordinator everything needed for intelligent recovery: type, context, partial work, and alternatives."},
{domain:"d1",task:"1.2 Coordinator-subagent",question:"A research coordinator decomposes 'how is AI impacting healthcare' into three subtopics and gets back narrow findings. The reports miss the broader picture. Root cause and fix?",options:["Decomposition was too narrow — broaden the partitioning (or use iterative refinement to evaluate gaps and re-delegate)","The synthesis subagent combined findings too aggressively, losing the nuance of each subtopic's independent results","The search subagents used overly specific query terms that excluded broader literature covering AI-healthcare intersections","The document analysis subagent applied relevance filters that excluded interdisciplinary sources bridging AI and healthcare"],correct:0,explanation:"The coordinator's decomposition is the root cause — subagents executed correctly within the narrow scope they were given. Blaming synthesis (B), search queries (C), or document filtering (D) misses that all three agents did exactly what the coordinator asked. The fix is broadening the decomposition or using iterative refinement to evaluate gaps."},
{domain:"d2",task:"2.4 MCP integration",question:"For a Jira integration, you have two options: an existing community MCP server, or building a custom one for your specific workflow. Best choice if the community server covers your needs?",options:["Use the community server — reserve custom servers for team-specific workflows that the community server doesn't cover","Always build a custom server to maintain full control over authentication scoping, input validation, and audit logging for production Jira workflows","Run both servers simultaneously — the community server handles generic queries while the custom server handles sensitive operations that require audit logs","Skip MCP entirely and have Claude call the Jira REST API directly using the <code>Bash</code> tool with <code>curl</code>, bypassing MCP overhead"],correct:0,explanation:"Community servers are usually the right choice for standard integrations. Custom is justified when your workflow has unique requirements."},
{domain:"d3",task:"3.5 Iterative refinement",question:"You're debugging unfamiliar code where you don't know the failure modes. Best refinement technique?",options:["The interview pattern — ask Claude to surface design considerations (e.g., cache invalidation, failure modes) before implementing","Start directly with hypotheses about likely failure modes based on the error message, iterating through candidate fixes without spending time on upfront discovery","Switch to a larger Claude model tier, since bigger models have broader code knowledge and are more likely to identify failure modes in unfamiliar codebases","Enable plan mode permanently for all debugging tasks — even for simple bugs, the exploration phase catches edge cases that direct execution would miss"],correct:0,explanation:"The interview pattern is recommended for unfamiliar domains — it surfaces considerations you may not have anticipated."},
{domain:"d4",task:"4.4 Validation & retry",scenario:"You are building an accounts-payable automation pipeline. The pipeline receives scanned invoices and must extract structured data into a JSON schema: <strong>vendor_name</strong>, <strong>invoice_number</strong>, <strong>total_amount</strong>, <strong>line_items</strong> (array), and <strong>due_date</strong>. Some invoices are handwritten, poorly scanned, or missing fields.",question:"For a field that may have a value not present in the source document, the best schema design is:",options:["Make the field nullable — allows the model to return null when no value is present rather than fabricating a placeholder or hallucinating a value","Make it required — required fields pressure the model to produce a value even when none exists in the source, increasing hallucination risk","Make it required with an empty-string default — an empty default still pressures the model to return something, and an empty string is ambiguous between 'not present' and 'genuinely empty'","Forbid extraction of that field entirely — eliminates the field from the schema, but also removes the ability to capture the value when it IS present in some documents"],correct:0,explanation:"Nullable fields are the correct design: they allow the model to distinguish 'value exists' from 'value not present' without fabricating content. Required fields create hallucination pressure. Empty-string defaults are ambiguous. Forbidding the field works around extraction but loses the value when it does appear."},
{domain:"d5",task:"5.1 Conversation context",question:"You're at 70% of context window with mostly verbose tool results from prior turns. Best mitigation?",options:["Trim verbose tool outputs to only the relevant fields BEFORE they accumulate (or summarize them) — preserve precise facts in a structured facts block","Run <code>/compact</code> immediately to summarize prior turns and reclaim context budget before the window fills completely","Switch to a model with a larger context window so the verbose history fits without any trimming or compaction intervention","Increase <code>max_tokens</code> for the current turn to give the model more room to reason about the accumulated context before the limit is reached"],correct:0,explanation:"Trim verbose outputs early; preserve precise facts in a structured block. Don't wait for context exhaustion."},
{domain:"d1",task:"1.1 Agentic loop",question:"In the loop, you accidentally append the model's assistant text to history without a corresponding <code>tool_result</code>. What happens?",options:["The next request is malformed — the API requires a <code>tool_result</code> message following any <code>tool_use</code> block; an assistant message without it creates an invalid conversation structure","Nothing observable — the API silently ignores orphaned tool_use blocks and treats the conversation as if the tool was never called","The model automatically generates a placeholder tool result — the API does not fabricate tool results; missing results cause an error, not auto-completion","The conversation silently resets to before the tool call — the API does not implement implicit rollback; it returns an error for the malformed request"],correct:0,explanation:"Missing a tool_result after a tool_use block creates a malformed request — the API requires paired tool_use/tool_result messages. The API doesn't silently ignore orphaned blocks, fabricate results, or silently roll back the conversation."},
{domain:"d2",task:"2.2 Structured errors",question:"A tool can't determine whether a write operation succeeded due to a timeout. How should it report?",options:["Communicate uncertainty explicitly with <code>retriable: false</code> and a clear human-readable message — don't auto-retry","Mark as <code>retriable: true</code> with exponential backoff — most databases support idempotency keys, making write retries safe as long as the key matches","Return a success response immediately to avoid alarming the user, then reconcile the actual outcome asynchronously in a background job","Throw a timeout exception and let the application framework's exception handler decide whether to retry, compensate, or surface the failure"],correct:0,explanation:"Uncertain state writes must not be auto-retried to avoid double charges. Communicate the uncertainty and let the human decide."},

{domain:"d4",task:"4.5 Batch processing",question:"You're batching 100 documents. Failures must be retried only for the affected items. What identifies each batch item uniquely for targeted retry?",options:["<code>custom_id</code> fields — you assign a unique ID to each request at submission; the batch API returns the same ID with each response, enabling precise failure identification","Response order — batch results are not guaranteed to arrive in submission order; position-based correlation silently misassigns responses when the order shifts","A content hash of the input document — hashes identify duplicate content but not unique request instances; identical documents would produce the same hash and be indistinguishable","A timestamp assigned at submission time — timestamps have millisecond granularity that can collide within a large batch, making them unreliable as unique identifiers"],correct:0,explanation:"custom_id is the supported correlation mechanism: assign a unique identifier per request at submission, and the batch API returns it with the result. Response order is not guaranteed. Content hashes can't distinguish identical documents. Timestamps aren't unique enough for high-volume batches."},
{domain:"d1",task:"1.4 Enforcement & handoffs",question:"A customer contacts your bank's support agent with three separate issues in one message: 'My credit card was charged twice for order #4421, my mailing address is wrong on my statement, and I can't log into online banking.' The agent currently handles these one at a time, resulting in a 12-minute average handle time. What is the recommended approach?",options:["Decompose the message into distinct concerns, investigate each in parallel using shared context, then synthesize a unified resolution","Handle the issues sequentially — verify identity once and proceed through the list in order","Escalate multi-concern messages to Tier-2 support, which has the tools and authority for complex cases","Ask the customer to restate each issue in a separate message so each can be handled by the appropriate specialist"],correct:0,explanation:"Multi-concern decomposition with parallel investigation using shared context is the recommended pattern: identify all three issues upfront, investigate them concurrently (a double charge, an address update, and an account access issue are independent), then synthesize a single resolution. Sequential handling (B) is inefficient — working on issue 1 doesn't prevent working on issue 2 simultaneously. Escalation (C) unnecessarily routes solvable issues to Tier-2. Asking for restatement (D) frustrates the customer and adds round-trips."},
{domain:"d2",task:"2.3 Tool distribution",question:"You want forced selection of a tool. The setting is:",options:["<code>tool_choice: {\"type\": \"tool\", \"name\": \"&lt;tool_name&gt;\"}</code>","<code>tool_choice: \"any\"</code>","<code>tool_choice: \"auto\"</code>","No <code>tool_choice</code>"],correct:0,explanation:"Forced tool selection requires the specific name. <code>\"any\"</code> only forces SOME tool; <code>\"auto\"</code> doesn't even require that."},
{domain:"d3",task:"3.4 Plan vs direct",question:"When are direct execution AND plan mode both appropriate to use together?",options:["Plan mode for investigation/design → direct execution for implementing the planned approach","They are mutually exclusive — Claude Code enforces a mode lock at the start of each session, preventing mode switching once an operation has begun","Plan mode always supersedes direct execution — when both are initiated for the same task, Claude Code defaults to the more conservative planning approach","Direct execution always supersedes plan mode — once any file has been modified in a session, plan mode cannot be activated without starting a new session"],correct:0,explanation:"Combining plan-for-design with direct-execution-for-implementation is a recommended pattern for large changes."},

{domain:"d1",task:"1.5 Agent SDK hooks",question:"Your financial data pipeline gives Claude access to <code>read_market_data</code>, <code>generate_report</code>, <code>send_email</code>, and <code>execute_trade</code>. Regulators require that <code>execute_trade</code> never runs without a human confirmation token from your approvals system. A system prompt instruction has failed twice in production — the agent called <code>execute_trade</code> directly during high-context scenarios. What is the correct architectural fix?",options:["A pre-execution interceptor that inspects every <code>execute_trade</code> call and blocks it unless a valid confirmation token is present in the parameters","A <code>PostToolUse</code> hook that detects unauthorized trades after execution and triggers an immediate reversal API call","A stronger system prompt: 'CRITICAL COMPLIANCE: <code>execute_trade</code> MUST include a human confirmation token. Violations are a regulatory breach.'","A routing classifier that analyzes each turn and disables <code>execute_trade</code> from the tool list unless the conversation context includes prior approval language"],correct:0,explanation:"A pre-execution interceptor is the only approach that deterministically prevents unauthorized execution — it blocks the call before any trade occurs if the confirmation token is absent. PostToolUse reversal (B) fires after the trade executes, meaning the violation already happened. Stronger prompts (C) are the pattern that already failed twice — prompt compliance is probabilistic. A routing classifier (D) relies on the same probabilistic model behavior that produced the failures."},
{domain:"d4",task:"4.6 Multi-pass review",question:"Calibrated review routing using model self-reported confidence requires:",options:["Validation against labeled data to map model confidence to actual accuracy","Trust in the model's raw confidence","Random sampling only","No calibration — confidence is reliable by default"],correct:0,explanation:"Raw LLM confidence is uncalibrated. Calibration against labeled data is required to make it useful for routing decisions."},


/* === FETCHED FROM CERTSAFARI (59 questions) === */
{domain:"d3",task:"3.1 Configure CLAUDE.md files with appropriate hierarchy, s",question:"A developer is working on a feature that spans both the <code>frontend/</code> and <code>backend/</code> directories. They notice that when they initiate a Claude Code session from the root directory and ask it to update the API, it hallucinates the database schema. However, if they change directories into <code>backend/</code> and ask the exact same question, Claude perfectly follows the schema rules defined in <code>backend/CLAUDE.md</code>. How should the architect investigate and resolve this scoping issue so the developer can work efficiently from the root directory?",options:["Run /memory in the root session to verify if backend/CLAUDE.md is loaded; if not, move the schema rules to .claude/rules/ in the root so they are auto-discovered regardless of the starting directory.","Run /config sync in the root directory to recursively aggregate all subdirectory CLAUDE.md files into the user's global memory.","Add an @import backend/CLAUDE.md to the user-level ~/.claude/CLAUDE.md file so the backend rules are globally available across all sessions.","Instruct the developer to always use the /memory load backend/CLAUDE.md command manually every time they start a session in the root directory."],correct:0,explanation:"Correct. The /memory command allows the developer to inspect which instructions and files are currently in the model's context. Claude Code discovery is hierarchical; it searches for CLAUDE.md in the current directory and its parents, but it does not automatically crawl into sub-directories to aggregate nested rule files. Moving backend-specific rules to .claude/rules/ in the root directory ensures they are modularized and automatically loaded regardless of which subdirectory the developer is working in."},
{domain:"d4",task:"4.3 Enforce structured output using tool use and JSON schem",question:"A sentiment analysis pipeline processes product reviews using a tool with a <code>sentiment</code> enum field restricted to <code>[\"positive\", \"negative\", \"neutral\"]</code>. For highly sarcastic or contradictory reviews, Claude struggles to classify the text and randomly selects \"positive\" or \"negative\", which skews your analytics. How should you adjust your schema design to handle this?",options:["Add <code>\"unclear\"</code> to the <code>sentiment</code> enum values and instruct Claude to use it for ambiguous or highly sarcastic text.","Change the schema to output a float between -1.0 and 1.0 to capture the nuance of sarcasm.","Force Claude to output a chain-of-thought reasoning field before the sentiment field to improve its final classification.","Use <code>tool_choice: {\"type\": \"auto\"}</code> so Claude can refuse to answer when the sentiment is too difficult to determine."],correct:0,explanation:"Adding an explicit enum value such as 'unclear', 'mixed', or 'ambiguous' provides the model with a 'safe' structured option to use when text doesn't fit the standard categories. This reduces the frequency of forced random guesses, which protects the integrity of the analytics and allows for easy filtering of data points that may require manual review."},
{domain:"d5",task:"5.1 Manage conversation context to preserve critical inform",question:"A cybersecurity incident response system uses a 'log analyzer' agent that reads raw server logs and passes its findings to a 'remediation' agent. The log analyzer includes its step-by-step chain of thought ('First I looked at port 80, then I noticed an anomaly on port 443...') in its output. The remediation agent frequently fails to generate a script because its context window is filled with the log analyzer's internal monologue. How should the architect optimize this interaction?",options:["Instruct the remediation agent to use a regular expression to strip out the log analyzer's chain of thought before processing.","Increase the max_tokens parameter on the log analyzer agent so it can finish its chain of thought completely.","Bypass the log analyzer agent entirely and send the raw server logs directly to the remediation agent.","Modify the log analyzer agent to output only a structured JSON object containing the identified threat IP, attack type, and recommended action, omitting the verbose reasoning chain."],correct:3,explanation:"Correct. Modifying the agent-to-agent interface to use a structured, machine-readable format like JSON is an architectural best practice. By omitting the internal reasoning chain (monologue) and only passing the necessary findings, the architect reduces token consumption, improves reliability, and establishes a clear interface contract."},
{domain:"d4",task:"4.2 Apply few-shot prompting to improve output consistency",question:"An automated system extracts required fields (Name, Date, Invoice Number, Tax ID) from vendor invoices using a Claude model. When an invoice lacks a Tax ID, the model frequently hallucinates a 9-digit number or returns a malformed JSON object, which breaks the downstream application. According to Anthropic's recommended practices, which approach is best for improving the model's reliability in handling this missing field?",options:["Set the <code>top_p</code> parameter to 0.1 to restrict the model's vocabulary and reduce the likelihood of hallucinations.","Add few-shot examples to the prompt that show correct extraction from varied formats, specifically demonstrating how to output a <code>null</code> or empty value when a required field is missing.","Instruct Claude in the system prompt to invent a standard placeholder value like '000-00-0000' if the Tax ID is missing.","Use a separate validation script to check if the document contains a Tax ID before sending it to Claude for extraction."],correct:1,explanation:"Correct. Anthropic's documentation explicitly recommends using few-shot examples to improve output consistency and quality, especially for empty or null extraction. This technique guides the model on the correct behavior when a field is not present, directly addressing the hallucination issue."},
{domain:"d5",task:"5.3 Implement error propagation strategies across multi-age",question:"A logistics routing agent queries a real-time traffic API. The API key has expired, resulting in an authentication failure. The agent currently catches the exception and returns <code>{\"status\": \"error\", \"message\": \"search unavailable\"}</code> to the coordinator. Why is this implementation problematic?",options:["It is not problematic; returning a simple error status is sufficient as the coordinator only needs to know the search failed.","It hides valuable context; the agent should return the specific authentication failure so the coordinator knows not to retry and can immediately switch to a historical traffic model.","It is problematic because exposing any error details is a security risk; the agent should return an empty traffic array instead.","It is problematic because the agent should automatically generate a new API key rather than returning an error to the coordinator."],correct:1,explanation:"Proper error propagation in multi-agent systems requires passing enough context for the orchestrator to make an informed decision. By identifying a non-transient authentication failure, the coordinator can avoid wasteful retries and immediately pivot to a fallback mechanism, such as a historical traffic model or an administrative alert for key rotation."},
{domain:"d5",task:"5.4 Manage context effectively in large codebase exploratio",question:"An automated agent is performing a deep-dive root cause analysis on a production outage. It executes a series of complex database queries and log aggregations, pulling massive amounts of verbose diagnostic data into its context. The agent needs to maintain the chronological narrative of its investigation and the key anomalies found, but it is rapidly approaching its maximum token limit. The agent cannot be restarted or lose its current reasoning chain. What is the most appropriate technique to manage the context window in this scenario?",options:["Programmatically trigger a /compact operation to have the model summarize the verbose diagnostic outputs while retaining the core investigative narrative and key anomalies.","Delegate the remaining database queries to a subagent, which will automatically inherit the main agent's full context window without counting against the token limit.","Export the current context to a structured state manifest, clear the agent's memory, and inject only the final database query into the new prompt.","Write the verbose diagnostic data to a scratchpad file and instruct the model to delete its internal memory array."],correct:0,explanation:"Summarization or compaction is a standard architectural pattern for long-running agents. It allows the model to reduce token usage by condensing verbose intermediate outputs (like raw logs) into concise summaries while preserving the critical reasoning trace, chronological narrative, and key findings required for the investigation."},
{domain:"d2",task:"2.5 Select and apply built-in tools (Read, Write, Edit, Bas",question:"You are using the Claude Code agent to refactor a logging utility in a large monorepo. The base <code>Logger</code> class is instantiated and re-exported as <code>appLogger</code> from a barrel file. You need to find all callsites of this specific <code>appLogger</code> instance across the 2,000-file codebase to update them. What is the most accurate and efficient strategy for the agent to locate these callsites?",options:["Use <code>grep</code> to trace the exports from <code>Logger</code> to <code>sysLog</code> and then to <code>appLogger</code>. Finally, run <code>grep</code> for <code>appLogger</code> across the entire codebase to find all callsites.","Use <code>ripgrep (rg)</code> to search for <code>appLogger</code> across the codebase, using language-specific flags like <code>rg -ttypescript 'appLogger'</code> to narrow the search.","Use the <code>Read</code> tool on the barrel file, then instruct the agent to use its own code analysis capabilities to build a dependency graph and find all references to the <code>appLogger</code> export.","Use the <code>Bash</code> tool to run the TypeScript compiler (<code>tsc</code>) with the <code>--noEmit</code> and <code>--declaration</code> flags to identify all files that depend on the logger's module."],correct:2,explanation:"Correct. This is the most accurate strategy because it involves semantic code analysis rather than simple text matching. The research confirms that advanced AI agents like Claude Code can perform complex code analysis, understand code structure, and build dependency graphs to find all true semantic references. This approach correctly identifies all callsites, even when the import is aliased, and avoids the false positives that a text-based search would produce, making it ideal for a large and complex refactoring task."},
{domain:"d4",task:"4.1 Design prompts with explicit criteria to improve precis",question:"During a migration from Python 2 to Python 3, Claude is used to identify incompatible code. The prompt asks Claude to 'Find all Python 2 code.' Claude is flagging code that is perfectly valid in both Python 2 and 3, causing unnecessary manual rework for the developers. Which prompt adjustment will best solve this?",options:["Instruct Claude to 'Only flag code if you are 100% sure it will break in Python 3.'","Add 'Be conservative when flagging Python 2 code' to the prompt.","Update the prompt to: 'Only flag code that uses Python 2 specific syntax that has been removed or changed in Python 3, such as print statements without parentheses. Skip code valid in both versions.'","Ask Claude to provide a confidence score for each flagged line and filter out low-confidence findings in the application layer."],correct:2,explanation:"This is the best solution as it provides explicit criteria, actionable rules, and negative constraints (skip code valid in both versions). By focusing on specific syntax changes (e.g., print statements) rather than broad labels, Claude can more accurately distinguish between version-specific and cross-compatible code."},
{domain:"d3",task:"3.4 Determine when to use plan mode vs direct execution",question:"You are reviewing a pull request and notice that a newly added form component in <code>Checkout.tsx</code> is missing a required date validation conditional to prevent users from selecting past dates. The scope of the change is entirely contained within the <code>handleSubmit</code> function of this single file. As an architect, what Claude Code workflow do you recommend the developer use to fix this?",options:["Plan mode, to ensure the date validation aligns with the overall application architecture.","Direct execution, because it is appropriate for simple, well-scoped changes like adding a validation conditional.","The Explore subagent, to summarize how dates are validated in other components before applying the fix.","Plan mode to draft the conditional logic, followed by Direct execution to insert it into the file."],correct:1,explanation:"Correct. Direct execution is the recommended choice for simple, well-defined changes that can be safely implemented in a single location. Since the validation logic is contained entirely within the `handleSubmit` function of one file, the developer should apply the fix directly."},
{domain:"d1",task:"1.4 Implement multi-step workflows with enforcement and han",question:"An insurance claims bot is designed to issue payouts. The prompt states: 'Only issue payout if <code>claim_status</code> is <code>APPROVED</code>'. Despite this, it occasionally issues payouts for <code>PENDING</code> claims. To achieve a zero failure rate for this deterministic compliance requirement, what is the most effective architectural change?",options:["Implement a self-correction loop where the agent double-checks its own work before finalizing the payout.","Remove the <code>issue_payout</code> tool from the LLM entirely. Instead, have the LLM call an <code>approve_claim</code> tool, and let the backend application logic deterministically trigger the payout system based on the claim status.","Rely on prompt-based guidance but switch to a more capable model to achieve 100% deterministic compliance.","Add a mandatory user confirmation step where a human must approve the action before the agent can call the payout tool."],correct:1,explanation:"Correct. This is the recommended architectural pattern. Research confirms that for reliability in regulated environments, critical and deterministic logic must be handled by the backend application. This approach correctly separates the LLM's non-deterministic reasoning from the sensitive action of payment, adheres to the principle of least privilege, and ensures the process is auditable and repeatable."},
{domain:"d3",task:"3.5 Apply iterative refinement techniques for progressive i",question:"A developer used Claude to generate a Python Flask application. Upon review, the developer notices two distinct issues: the application's logging format is missing timestamps, and the database connection string is hardcoded directly in the source code. What is the recommended workflow for using Claude to address these issues?",options:["Combine both issues into a single, complex prompt detailing how to fix the logging format and externalize the database connection string.","Address the logging format in one prompt, verify the fix, and then address the database connection string in a subsequent prompt.","Ask Claude to rewrite the entire application from scratch, instructing it to follow 12-factor app principles to resolve both issues.","Manually fix the critical database connection string issue, then ask Claude to fix only the logging format."],correct:1,explanation:"Correct. This iterative approach aligns with Anthropic's recommended best practices. It uses focused prompts for distinct tasks, which improves clarity and model performance. The verification step is crucial and aligns with the principle of evaluation and optimization. The research notes this sequential, verified process is a 'logical workflow for managing application security' and a standard part of a secure software development lifecycle."},
{domain:"d4",task:"4.4 Implement validation, retry, and feedback loops for ext",question:"A system analyzes scientific papers to extract experimental parameters. A paper's 'Methods' section states a temperature of '37 C', while the 'Results' section abstractly refers to the experiment being conducted at 'ambient room temperature'. The system needs to capture this potential inconsistency. What is the best schema design to handle this?",options:["Extract a single <code>temperature</code> field and instruct the model to choose the more specific value.","Design the schema with <code>methods_temperature</code>, <code>results_temperature</code>, and a boolean <code>conflict_detected</code>. Prompt the model to extract both values and set the flag to <code>true</code> if they represent different temperatures.","Initiate a retry loop if two different temperatures are found, forcing the model to decide on a single canonical value.","Ignore the 'Results' section temperature, as the 'Methods' section is always the authoritative source."],correct:1,explanation:"This schema design preserves the distinct values from each section while explicitly flagging conflicts. Storing section-specific values with a machine-actionable conflict flag supports automated checks, principled reconciliation, and human review without losing information from either source."},
{domain:"d2",task:"2.5 Select and apply built-in tools (Read, Write, Edit, Bas",question:"You have generated a new 4MB JSON payload that needs to completely replace an existing 3MB <code>data-dictionary.json</code> file in the repository. The file consists of a single, massive JSON object with no line breaks. Which tool approach is required to update this file effectively?",options:["Use the Edit tool with a regex pattern to match the entire JSON object and replace it inline.","Use the Grep tool to delete the existing contents, then use the Edit tool to insert the new JSON payload.","Use the Write tool to directly overwrite the entire file with the new contents, as Edit cannot handle massive single-line replacements.","Use the Read tool to load the file into memory, modify the JSON keys individually, and use Write to save it."],correct:2,explanation:"The Write tool is designed to create or completely overwrite a file with new contents. Since the requirement is to replace the entire 3MB single-line object with a new 4MB payload, using the Write tool is the most efficient, reliable, and straightforward method."},
{domain:"d5",task:"5.1 Manage conversation context to preserve critical inform",question:"A multi-agent system for medical diagnosis features a 'research agent' that searches medical literature and passes findings to a 'diagnostic agent.' The diagnostic agent has a strict context budget to ensure fast response times. Currently, the research agent passes its entire reasoning chain and verbose article excerpts, causing the diagnostic agent to truncate critical patient history. What is the most efficient architectural solution?",options:["Increase the context budget of the diagnostic agent by upgrading to a higher-tier model.","Have the research agent write its output to a local text file and pass only the file path to the diagnostic agent.","Instruct the diagnostic agent to read the research agent's output backwards to ensure it sees the final conclusion before truncation occurs.","Modify the research agent to return structured data containing only key facts, citations, and relevance scores instead of its full reasoning chain."],correct:3,explanation:"Modifying the research agent to output structured, high-signal data is the most efficient approach. By filtering out reasoning chains and verbose excerpts at the source, the diagnostic agent receives only essential information, maximizing its limited context budget and ensuring critical patient history remains intact."},
{domain:"d4",task:"4.4 Implement validation, retry, and feedback loops for ext",question:"A system extracts the <code>project_deadline</code> from meeting transcripts. A transcript states, 'We need to have this completed by EOD next Thursday.' The system runs on Friday, October 4th. The model correctly infers the deadline as 'October 17th'. However, a validation rule requires that the extracted date must have been explicitly written in the text (e.g., 'October 17th'), not inferred from relative terms. The validation fails. What is the correct action?",options:["Retry with the error 'The date is incorrect. Please find the explicit date in the text.'","Do not retry. Flag the extraction for manual review, noting that the deadline was specified in relative terms, which violates the validation rule.","Retry with a new prompt that includes the current date and asks the model to recalculate 'next Thursday' more accurately.","Accept the model's inferred date, as it is logically correct, and override the validation rule."],correct:1,explanation:"This is the correct architectural response. When validation fails because the source data cannot meet a specific business constraint (explicit vs. inferred) and a retry will not change the source text, the item should be flagged for manual human-in-the-loop (HITL) review. This preserves data integrity and provides a feedback loop for potential policy adjustments."},
{domain:"d4",task:"4.6 Design multi-instance and multi-pass review architectur",question:"An AI agent is tasked with generating daily delivery routes for a fleet of 50 trucks. The agent uses a chain-of-thought prompt to assign packages, calculate distances, and output the final manifest. To ensure feasibility, the developer appends a <critique> block asking the model to find any double-booked trucks. The model consistently fails to identify double-bookings, instead defending its original assignments. What is the primary architectural reason for the model's inability to spot its own routing errors?",options:["The model retains its prior reasoning context from the generation phase, making it less likely to question its own decisions in the same session.","The prompt suffers from attention dilution because the manifest for 50 trucks exceeds the model's effective context window for logical reasoning.","The <critique> block is placed at the end of the prompt, causing the model to prioritize the generation instructions over the review instructions.","The model lacks a cross-file integration pass, which is required to compare the schedules of different trucks effectively."],correct:0,explanation:"Correct. This is a known limitation of single-pass self-critique. When a model generates a reasoning chain (CoT) and an output, it treats those tokens as established context. This leads to confirmation bias, where the model rationalizes its earlier mistakes rather than identifying them. To solve this architecturally, a separate reviewer instance (multi-instance) or a multi-pass approach with a fresh context is required."},
{domain:"d1",task:"1.3 Configure subagent invocation, context passing, and spa",question:"An incident response system uses a coordinator to investigate server outages. When an outage occurs, the coordinator gathers initial logs. It then needs to investigate two competing hypotheses: a DDoS attack and a database failure. If it investigates the DDoS attack first and finds nothing, the context window is now polluted with irrelevant network logs, confusing the model when it subsequently investigates the database failure. How can the architect use fork-based session management to solve this context pollution problem?",options:["Branch the session state immediately after the initial log gathering, creating two isolated threads to investigate each hypothesis independently without cross-contaminating the context windows.","Configure the coordinator to emit two Task tool calls in parallel, ensuring the network logs and database logs are processed by separate subagents before merging the results.","Instruct the coordinator to output its findings in a structured JSON format, separating the DDoS logs from the database logs, to preserve attribution in a single thread.","Update the <code>AgentDefinition</code> for the subagents to include a <code>ClearContext</code> tool, allowing them to wipe the session memory before switching hypotheses."],correct:0,explanation:"Correct. Forking or branching the session state immediately after the shared initial log-gathering step creates two independent branches. This allows each hypothesis to be explored in isolation, ensuring that irrelevant DDoS-specific context (like network logs) does not bleed into and pollute the database-failure investigation thread."},
{domain:"d3",task:"3.6 Integrate Claude Code into CI/CD pipelines",question:"To ensure that Claude Code consistently adheres to repository-specific testing standards and utilizes custom test fixtures during automated workflows or developer interactions, which approach is recommended?",options:["Pass the directory containing the fixtures using the --fixtures-dir CLI flag during the CI invocation.","Document the testing standards, valuable test criteria, and available custom fixtures in the CLAUDE.md file.","Add a pre-commit hook that automatically rewrites unittest.mock calls to use the custom fixtures.","Execute Claude Code with the --strict-mocks=false flag to disable standard mocking libraries."],correct:1,explanation:"Correct. The CLAUDE.md file is the primary mechanism for providing repository-level context, guidelines, and standards to Claude. Documenting custom fixtures and testing criteria here ensures consistency, discoverability, and adherence to project requirements across all team members and pipelines."},
{domain:"d4",task:"4.4 Implement validation, retry, and feedback loops for ext",question:"A medical coding application uses Claude to extract ICD-10 codes from clinical notes. The system includes a <code>detected_pattern</code> field in its output, which logs the specific phrasing in the notes that led to the code extraction. Human reviewers frequently dismiss the extraction of 'E11.9 (Type 2 diabetes)' when the <code>detected_pattern</code> shows the phrase 'family history of diabetes'. How should the architect utilize this feedback loop data to improve extraction quality?",options:["Automatically delete any clinical note containing the phrase 'family history of diabetes' before sending it to the model.","Update the prompt's instructions to explicitly state: 'Do not extract E11.9 if the context indicates a family history rather than an active diagnosis,' using the identified pattern to create targeted negative constraints.","Fine-tune the model on all dismissed extractions to teach it to ignore the E11.9 code entirely across all documents.","Modify the tool schema to change the <code>detected_pattern</code> field from a string to a boolean, simplifying the analysis of false positives."],correct:1,explanation:"Correct. The feedback loop has identified a specific, recurring false-positive pattern. By updating the prompt to include explicit negative constraints or instructions to distinguish between active diagnoses and family history, the architect directly addresses the root cause of the error observed in the feedback loop."},
{domain:"d2",task:"2.2 Implement structured error responses for MCP tools",question:"A healthcare scheduling agent uses an MCP tool to book appointments. A user tries to book a specialist without a required primary care referral. The backend API returns a raw database error: <code>ConstraintViolationException: FK_Referral_Missing</code>. If passed directly to the agent, the agent might confuse the user with technical jargon. What is the best MCP tool design for this scenario?",options:["Catch the exception and return <code>isError: true</code>, <code>errorCategory: \"business\"</code>, <code>isRetryable: false</code>, and a customer-friendly message: 'A primary care referral is required to book this specialist. Please ask the user to obtain a referral first.'","Pass the raw <code>ConstraintViolationException</code> but set <code>isError: true</code> and <code>errorCategory: \"transient\"</code> so the agent knows it is a technical issue and can retry.","Return <code>isError: false</code> and a message 'Booking successful' but flag the appointment as 'Pending Referral' in the database to avoid agent confusion.","Return <code>isError: true</code>, <code>errorCategory: \"validation\"</code>, and ask the agent to retry the booking using a different specialist."],correct:0,explanation:"Correct. This approach follows MCP best practices by abstracting low-level database constraints into a structured response. Mapping a raw DB constraint to `errorCategory: \"business\"` with `isRetryable: false` informs the agent that a business rule was violated and retrying won't resolve it. This allows the agent to provide clear, non-technical feedback to the user."},
{domain:"d2",task:"2.1 Design effective tool interfaces with clear description",question:"A banking assistant has a <code>waive_fee</code> tool. The tool description explicitly states: 'Only use this tool if the customer account age is > 5 years and they have no prior waivers.' However, the system prompt begins with: 'You are a customer-obsessed banking assistant. Always prioritize customer satisfaction and grant requests that make the customer happy.' Claude frequently waives fees for brand-new accounts. What is the root cause and the recommended solution?",options:["The tool description needs to be moved into the system prompt and placed within <code><tool_guidance></code> tags to ensure it is prioritized over the persona instructions.","The keyword-sensitive persona instructions in the system prompt are overriding the tool's boundary constraints; the system prompt must be revised to align with policy.","The <code>waive_fee</code> tool should be renamed to <code>evaluate_fee_waiver</code> to make it sound less definitive to the model.","The backend API must be updated to return a <code>403 Forbidden</code> error when Claude attempts an invalid waiver, as LLMs cannot be relied upon to enforce business logic."],correct:1,explanation:"Correct. This accurately identifies the root cause as a conflict between the strong, general persona instruction ('be customer-obsessed') and the specific, strict tool constraints. Research confirms this is a common challenge. The recommended solution is to revise the system prompt to be more structured, explicitly define constraints and boundaries, and provide instructions on how to handle conflicting requests to ensure the model's behavior aligns with organizational policy."},
{domain:"d5",task:"5.2 Design effective escalation and ambiguity resolution pa",question:"An AI assistant for a SaaS platform frequently attempts to autonomously resolve complex billing disputes that require managerial override, leading to incorrect promises made to customers. The architect needs to improve the model's ability to recognize when to escalate these specific disputes. Which prompt engineering technique is most effective for this requirement?",options:["Add a negative constraint: 'NEVER attempt to resolve complex billing disputes. ALWAYS escalate them.'","Add explicit escalation criteria to the system prompt along with few-shot examples demonstrating dialogues of standard issues being resolved and override scenarios being escalated.","Implement a secondary LLM to monitor the primary LLM's outputs and intercept any messages containing the word 'refund' or 'override'.","Lower the model's temperature to 0.0 to prevent it from hallucinating unauthorized billing resolutions."],correct:1,explanation:"This is the most effective approach. Combining explicit, rule-based escalation criteria in the system prompt with few-shot examples provides both the logic and the pattern recognition the model needs. It teaches the model the specific boundaries of its authority and shows it how to handle edge cases through demonstration."},
{domain:"d1",task:"1.1 Design and implement agentic loops for autonomous task",question:"A developer is building an autonomous research agent. To determine when the agent has finished its research, the developer's code parses the assistant's response to check for the phrase 'Research complete:'. If this phrase is found, the agent's main loop terminates. What is the architectural assessment of this approach?",options:["This is a best practice, provided the system prompt strongly enforces the exact phrasing of 'Research complete:' using XML tags.","This is an anti-pattern because it relies on parsing non-deterministic natural language for control flow. The developer should instead instruct the model to call a specific tool, such as <code>research_complete()</code>, to signal task completion.","This is an anti-pattern; the developer should instead evaluate if <code>stop_reason</code> equals <code>end_turn</code> to safely and deterministically terminate the loop.","This is an anti-pattern; the developer should instead check for a specific <code>stop_sequence</code> like <code></research_complete></code> in the API request."],correct:1,explanation:"Correct. The research explicitly identifies parsing non-deterministic natural language for control flow as an anti-pattern. The recommended best practice is to provide the model with a specific tool, such as `research_complete()`, which it can call to provide a structured and reliable signal that the task is finished. This approach makes the agent's control flow deterministic and robust."},
{domain:"d2",task:"2.3 Distribute tools appropriately across agents and config",question:"An enterprise search agent is equipped with 20 distinct tools for querying different internal databases (Jira, Confluence, GitHub, Salesforce, etc.). Due to high decision complexity, the agent frequently selects the wrong tool. The architect plans to implement a Supervisor routing pattern. Which design best adheres to tool distribution principles?",options:["The Supervisor is given all 20 tools but uses <code>tool_choice: {\"type\": \"any\"}</code> to strictly evaluate them, while subagents are only used for conversational formatting.","The Supervisor has no search tools itself; it evaluates the user's query and routes it to specialized subagents (e.g., <code>Code_Search_Agent</code>, <code>CRM_Search_Agent</code>), each equipped with 2-3 relevant tools.","The Supervisor is given a single generic <code>search_all</code> tool, and the 20 specific tools are deprecated to reduce decision complexity.","The Supervisor is equipped with the 5 most popular tools, and routes requests to subagents only if the required tool is among the remaining 15."],correct:1,explanation:"This design follows the Supervisor routing pattern by focusing the Supervisor on classification and routing rather than direct tool execution. By delegating specific tasks to specialized subagents with only a few relevant tools each (e.g., 2-3 tools), decision complexity is significantly reduced, improving accuracy and reliability in tool selection."},
{domain:"d3",task:"3.6 Integrate Claude Code into CI/CD pipelines",question:"A software engineering team is integrating Claude Code into their CI/CD pipeline to automate accessibility reviews across multiple repositories. They need a solution that ensures review criteria are version-controlled, easy to update, and consistently applied across different environments. What is the most effective way to configure these accessibility standards?",options:["Create a global ~/.claude_a11y.json configuration file on the CI runner instances.","Move the accessibility review criteria and testing standards into each repository's CLAUDE.md file.","Use the --review-profile wcag flag to activate Claude's built-in accessibility ruleset.","Set the CLAUDE_REVIEW_CRITERIA environment variable in the CI/CD platform settings."],correct:1,explanation:"The CLAUDE.md file is the primary mechanism for providing repository-specific context and instructions to Claude Code. Placing accessibility standards in this file ensures they are version-controlled alongside the source code, easily discoverable by contributors, and automatically picked up by Claude during CI/CD pipeline execution."},
{domain:"d1",task:"1.3 Configure subagent invocation, context passing, and spa",question:"A data engineering team is building an ETL orchestration agent. The coordinator spawns a <code>DataCleaner</code> subagent via the Task tool. The <code>DataCleaner</code> needs to process a dataset, but the dataset is 500MB, far exceeding the model's context window. The architect decides the <code>DataCleaner</code> must use a <code>SQLQuery</code> tool to interact with the data directly in the database. How should the architect configure the system to ensure the <code>DataCleaner</code> knows exactly what data to process without overwhelming the context window?",options:["Enable the <code>shared_memory</code> flag in the <code>DataCleaner</code>'s <code>AgentDefinition</code> so it can automatically inherit the coordinator's database connection string and session state.","Use fork-based session management to stream the 500MB dataset into the <code>DataCleaner</code>'s context window in 100,000-token chunks across multiple parallel threads.","Configure the coordinator to pass the database schema, the specific table names, and the overarching cleaning objectives in the prompt when invoking the <code>DataCleaner</code> via the Task tool.","Instruct the <code>DataCleaner</code> to output its findings as a structured JSON array, separating the raw data from the metadata, to compress the 500MB dataset before processing."],correct:2,explanation:"Correct. By providing the metadata (schema and table names) and the specific objectives, the coordinator gives the subagent enough context to use the `SQLQuery` tool effectively. This allows the subagent to interact with the data at the source, keeping the LLM's prompt size small and focused while performing operations on the large dataset."},
{domain:"d2",task:"2.1 Design effective tool interfaces with clear description",question:"A legal assistant has two tools: <code>search_case_law</code> (for past court rulings) and <code>search_statutes</code> (for current legislation). When users ask 'What are the laws regarding tenant eviction?', Claude randomly selects between the two tools, often missing critical context. How should the tool descriptions be improved to fix this?",options:["Combine the tools into <code>search_legal_database</code> to allow the backend search engine to handle the routing.","Update both tool descriptions to explicitly define when to use each tool versus the other, including boundary explanations for ambiguous terms like 'laws'.","Add a <code>routing_tool</code> that Claude must call first to determine which legal database to query.","Modify the system prompt to require Claude to always call both tools sequentially for any legal query."],correct:1,explanation:"This is the optimal solution. Claude relies on tool descriptions to map user intent to specific tool functionality. By explicitly defining when to use one tool over the other and providing boundary explanations for ambiguous terms, the model can reliably select the appropriate source or determine if both are necessary based on the user's query."},
{domain:"d1",task:"1.6 Design task decomposition strategies for complex workfl",question:"A media company uses an AI agent to review 3-hour video transcripts for brand safety, narrative consistency, and pacing. Initially, the entire transcript was processed in one prompt, but the model hallucinated events due to attention dilution. The architect split the workflow into 10-minute local analysis chunks. Now, the agent accurately flags local brand safety issues but completely misses narrative inconsistencies that span across the entire video. How should the architect resolve this?",options:["Implement a cross-file integration pass that analyzes the summarized narrative outputs from the local chunk passes to evaluate global consistency.","Switch the entire workflow to dynamic adaptive decomposition so the agent can autonomously decide which 10-minute chunks to read.","Increase the model's temperature and use a map-reduce pattern to process all chunks in parallel without a final integration step.","Revert to a single prompt but use strict XML tags to separate the 10-minute chunks within the context window."],correct:0,explanation:"Correct. This approach utilizes a Map-Reduce or Synthesis pattern. By implementing a second-stage integration pass, the architect allows the system to reason over summarized outputs from the local passes. This enables the model to detect long-range narrative inconsistencies that no single local chunk can identify while maintaining the accuracy gained by avoiding attention dilution on the raw text."},
{domain:"d3",task:"3.1 Configure CLAUDE.md files with appropriate hierarchy, s",question:"A developer uses a personal, paid third-party API for advanced code analysis during their local development workflow. They want to instruct Claude Code to use a specific curl command with their personal API key to analyze complex files before refactoring them. They work across multiple company repositories. Where is the most appropriate place to store these instructions and the API key format so that Claude can use this workflow across all company projects without exposing the workflow to coworkers?",options:["In the user-level ~/.claude/CLAUDE.md file, as this configuration applies only to that user and is not shared via version control.","In a .claude/rules/personal.md file at the root of each company repository, added to the .gitignore file.","In the project-level .claude/CLAUDE.md file, wrapped in <private> XML tags so Claude knows not to commit it.","As an @import ~/.claude/personal.md statement inside each project's root CLAUDE.md file."],correct:0,explanation:"The user-level ~/.claude/CLAUDE.md file is the correct location for personal, machine-local instructions. Because this file resides in the user's home directory rather than the project directory, it applies to all Claude Code sessions for that user across any repository without being committed to version control, keeping sensitive data like API keys private."},
{domain:"d2",task:"2.4 Integrate MCP servers into Claude Code and agent workfl",question:"A platform engineering team built a custom semantic search MCP tool designed to find code snippets based on natural language intent. However, when developers ask Claude Code to 'find where we handle payment retries', the agent consistently ignores the custom semantic search tool and defaults to using the built-in <code>grep</code> tool, often missing relevant results that don't contain the exact keyword. How should the team resolve this issue?",options:["Disable the built-in <code>grep</code> tool in the ~/.claude.json file to force the agent to use the custom semantic search tool.","Rename the custom tool to <code>grep_advanced</code> so the agent recognizes it as an upgrade to the built-in tool.","Enhance the custom MCP tool's description to explicitly detail its semantic capabilities, output format, and specific scenarios where it outperforms standard text matching.","Change the custom tool's execution environment to run synchronously, as agents prefer built-in tools because they do not require asynchronous waiting."],correct:2,explanation:"Correct. Claude selects tools based on their descriptions, schemas, and usage examples. By explicitly detailing the tool's semantic capabilities—specifically how it handles natural language intent and concepts rather than just literal strings—the agent will understand when to prioritize this custom tool over keyword-based utilities like `grep`."},
{domain:"d5",task:"5.3 Implement error propagation strategies across multi-age",question:"A legal research agent is tasked with finding case precedents regarding a highly specific and obscure patent law. After executing a well-formed query against the legal database, the database returns zero matching cases. How should the subagent communicate this to the coordinator?",options:["Throw a <code>SearchTimeoutError</code> to force the coordinator to retry the query with different parameters.","Return a structured response indicating a successful query execution with zero matches, ensuring the coordinator knows this is a valid empty result rather than an access failure.","Return a generic 'Search Failed' error to prompt the coordinator to switch to a secondary legal database.","Suppress the empty result and return a closely related, but technically incorrect, case precedent to ensure the workflow continues."],correct:1,explanation:"Returning a structured success response with a result count of zero is the most accurate communication. It distinguishes between a system failure (error) and a data-driven outcome (empty set). This semantic clarity allows the coordinator to proceed with informed logic, such as broadening the search parameters or checking alternative sources."},
{domain:"d2",task:"2.1 Design effective tool interfaces with clear description",question:"A healthcare application uses a single <code>process_patient_record</code> tool. It handles extracting vitals, summarizing medical history, and verifying insurance. Claude frequently times out or returns incomplete JSON because the tool's output schema is massive and the model struggles to populate all fields accurately in a single pass. Which architectural change best resolves this issue while adhering to tool design best practices?",options:["Increase the API timeout limit and use a higher-tier model to handle the massive schema.","Split the generic tool into <code>extract_patient_vitals</code>, <code>summarize_medical_history</code>, and <code>verify_insurance_status</code> with defined input/output contracts.","Update the system prompt to instruct Claude to only request one piece of information at a time using the existing tool.","Add few-shot examples to the <code>process_patient_record</code> description showing partial data extraction to teach the model to return smaller payloads."],correct:1,explanation:"Splitting the monolithic tool into specialized, modular tools follows the core architectural best practice of creating clear boundaries and focused responsibilities. This reduces the schema size per call, improves model accuracy by narrowing the context, simplifies unit testing, and allows the model to compose tasks more reliably."},
{domain:"d3",task:"3.6 Integrate Claude Code into CI/CD pipelines",question:"A CI pipeline uses Claude Code to automatically generate documentation for new API endpoints based on source code changes. To ensure accuracy, the pipeline includes a validation step where Claude Code reviews the generated documentation against the project's OpenAPI specification. The team notices that when the validation step is executed within the same Claude Code session that generated the documentation, it rarely catches obvious discrepancies.\n\nAccording to Anthropic's best practices, why does this occur, and how should the pipeline be restructured?",options:["The model's context window overflows when handling both generation and validation tasks; the pipeline should use a model with a larger context window for the validation step.","The model experiences session context isolation issues, making it biased toward its own recent outputs; the pipeline should instantiate a completely independent Claude Code session for the validation step.","The same session caches the OpenAPI specification incorrectly; the pipeline should clear the session cache using the <code>--clear-memory</code> flag before running the validation step.","The validation step requires a higher temperature setting to be critical; the pipeline should dynamically adjust the <code>--temperature</code> flag to 0.0 during the same session."],correct:1,explanation:"Correct. When an LLM generates and then validates content within the same conversational context, it is prone to self-confirmation bias. It often inherits the same assumptions or errors made during the generation phase. Anthropic's best practice for robust validation is to use a fresh, independent Claude Code session to ensure the validator evaluates the output objectively without the influence of the prior generation's history."},
{domain:"d5",task:"5.2 Design effective escalation and ambiguity resolution pa",question:"An insurance claims bot searches for a customer using a provided phone number. The <code>search_policy</code> tool returns two distinct policies: one that is currently active, and another that was cancelled five years ago. The system cannot confirm if both policies belong to the same person. The developer's primary goal is to resolve the ambiguity while minimizing user friction. According to best practices, how should the agent be instructed to proceed?",options:["The agent should ask the user for an additional, more stable identifier, such as a policy number or date of birth, to clarify the match.","The agent should immediately escalate to a human agent because multiple matches indicate a potential data issue or fraud attempt.","The agent should use a heuristic to automatically select the active policy, as users are most likely inquiring about their current coverage.","The agent should be instructed to automatically merge the two profiles in the backend database to resolve the ambiguity for future interactions."],correct:0,explanation:"Correct. According to industry best practices for identity resolution in sensitive domains like insurance, asking for an additional identifier is the recommended approach to resolve ambiguity. This method directly and safely clarifies the user's identity with minimal friction before proceeding. This also aligns with Anthropic's guidance on designing tools that can guide an agent to ask for more information when needed."},
{domain:"d4",task:"4.3 Enforce structured output using tool use and JSON schem",question:"A customer support pipeline uses a router agent to classify tickets, followed by specific extraction agents. For a new VIP client, all tickets are guaranteed to be \"billing\" related. To reduce latency and API costs for this VIP client, you want to bypass the router agent and directly extract the billing details using the <code>extract_billing</code> tool, even though the system prompt still contains instructions for general support.\n\nHow can you guarantee Claude immediately executes the billing extraction?",options:["Set <code>tool_choice: {\"type\": \"tool\", \"name\": \"extract_billing\"}</code> in the API request to force the specific tool execution.","Set <code>tool_choice: {\"type\": \"any\"}</code> and only provide the <code>extract_billing</code> tool in the request array.","Remove the general support instructions from the system prompt and set <code>tool_choice: {\"type\": \"auto\"}</code>.","Set <code>tool_choice: {\"type\": \"tool\", \"name\": \"extract_billing\"}</code> and remove the tool definition from the <code>tools</code> array to save tokens."],correct:0,explanation:"Correct. In the Anthropic Messages API, setting `tool_choice` to `{\"type\": \"tool\", \"name\": \"tool_name\"}` explicitly forces Claude to use that specific tool in its response. This overrides Claude's decision-making process, ensuring immediate execution of the specified tool regardless of context or general instructions in the system prompt, which is the most direct way to bypass a routing step."},
{domain:"d4",task:"4.5 Design efficient batch processing strategies",question:"An enterprise uses Anthropic's Batch API to process a high volume of customer support transcripts. Transcripts are generated continuously, and each one must be processed within a strict 48-hour Service Level Agreement (SLA). To maximize batch size for cost-efficiency while ensuring no transcript ever violates the SLA, what is the most appropriate batch submission frequency?",options:["Submit a new batch every 12 hours.","Submit a new batch every 24 hours.","Submit a new batch every 48 hours.","Submit a new batch every 6 hours."],correct:1,explanation:"Correct. A 24-hour frequency is the optimal choice as it maximizes the batch size while safely meeting the 48-hour SLA. In the worst-case scenario, a transcript generated just after a batch submission waits 24 hours for the next batch, leaving a full 24 hours for processing. This aligns with Anthropic's documented recommendation to run scheduled batches daily, as noted in patterns like \"Run the agent once daily on all transcripts created since the last run.\""},
{domain:"d5",task:"5.1 Manage conversation context to preserve critical inform",question:"A financial research system uses multiple subagents to analyze different quarterly reports. A master agent synthesizes these analyses into a final executive brief. However, the master agent frequently attributes Q3 revenue figures to Q4, or mixes up data from different regional reports, because the subagent outputs are provided as plain text narrative summaries. How should the architect modify the subagents to support accurate downstream synthesis?",options:["Require the subagents to output structured data that explicitly includes metadata such as dates, source document locations, and methodological context.","Merge all subagents into a single massive prompt so the master agent processes the raw quarterly reports directly.","Instruct the master agent to infer the correct quarter based on the numerical trends present in the plain text summaries.","Reduce the temperature of the master agent to 0.0 to prevent the hallucination of dates and regions."],correct:0,explanation:"Correct. Requiring subagents to emit structured output (such as JSON) with explicit metadata (e.g., quarter, region, numeric values, source locations) ensures that critical information is preserved and unambiguous. This approach minimizes the risk of misattribution inherent in free-text narratives and supports validation, traceability, and robust programmatic synthesis by the master agent."},
{domain:"d1",task:"1.4 Implement multi-step workflows with enforcement and han",question:"A wealth management bot receives a prompt: 'Analyze my current portfolio risk and suggest three new tech stocks to buy.' The architect decomposes this into a <code>RiskAnalyzer</code> agent and a <code>StockRecommender</code> agent running in parallel. Both agents need access to the user's current holdings to perform their tasks accurately. How should the shared context be managed?",options:["Query the user's holdings twice, once inside each agent's execution loop, to ensure they operate completely independently.","Fetch the user's holdings once in the orchestration layer and inject this data as shared context into the system prompts of both parallel agents before execution.","Execute the <code>RiskAnalyzer</code> first, append its output to the user's holdings, and pass the combined data to the <code>StockRecommender</code> to ensure data consistency.","Store the user's holdings in a vector database and require both agents to use a RAG tool to retrieve the specific holdings they need."],correct:1,explanation:"Fetching the data once at the orchestration layer and injecting it into both parallel agents is the most efficient and reliable pattern. It ensures both agents work from an identical snapshot of the user's data, which is essential for consistent multi-step task execution."},
{domain:"d3",task:"3.4 Determine when to use plan mode vs direct execution",question:"Your organization is upgrading a core UI framework from version 17 to 18. This upgrade introduces several breaking changes that will affect multiple components. You need to identify these breaking changes, decide on a repository-wide migration strategy, and then apply the code modifications. Which workflow represents the optimal use of Claude Code for this multi-phase task?",options:["Use direct execution for the entire process, allowing Claude to discover and fix breaking changes dynamically as it encounters them.","Use the Explore subagent to both investigate the breaking changes and implement the code modifications in an isolated environment.","Use plan mode to investigate the breaking changes and formulate a migration strategy, followed by direct execution to implement the planned approach.","Use direct execution to investigate the breaking changes, followed by plan mode to safely apply the code modifications across the repository."],correct:2,explanation:"This represents the optimal workflow for complex refactoring. Plan mode allows Claude to explore the codebase, identify breaking changes, and formulate a comprehensive migration strategy that can be reviewed before any code is changed. Once the strategy is validated, direct execution provides a systematic way to apply the planned modifications reliably across the repository."},
{domain:"d3",task:"3.4 Determine when to use plan mode vs direct execution",question:"While implementing a complex MongoDB aggregation pipeline, you realize you need to understand the schema of a massive, undocumented legacy analytics database dump (consisting of large JSON files) to properly join the data. You need Claude to analyze these files, but you cannot afford to have the massive JSON structures flood your current context window and push out your ongoing pipeline work. Which approach is best?",options:["Use Plan mode to safely read the JSON files without executing any database commands.","Use the Explore subagent to isolate the verbose discovery output and return a concise schema summary to the main conversation.","Use Direct execution to write a script that parses the JSON files and outputs the schema to a new file.","Use Plan mode to investigate the JSON files, then switch to Direct execution for the pipeline."],correct:1,explanation:"Correct. The Explore subagent is specifically designed for discovery and analysis tasks that involve high-verbosity data. It operates in a separate context to perform the 'heavy lifting' of analyzing large files, and then returns only the necessary, concise summary to the main conversation, effectively preventing context flooding."},
{domain:"d3",task:"3.3 Apply path-specific rules for conditional convention lo",question:"A development team wants to provide Claude Code with a specialized set of instructions for writing and refactoring Terraform configurations. These instructions should only be activated when Claude is working on relevant tasks to avoid cluttering its context unnecessarily. According to Anthropic's official documentation, what is the recommended approach to implement this conditional context?",options:["Create a new \"Skill\" by adding a directory like <code>.claude/skills/terraform-best-practices/</code> and placing the guidelines within a <code>SKILL.md</code> file inside it.","Place a <code>CLAUDE.md</code> file directly inside the <code>infrastructure/</code> directory with the Terraform-specific guidelines.","Add the Terraform guidelines to a root <code>.claude/instructions.md</code> file and use YAML frontmatter to specify the path: <code>paths: [\"infrastructure/\"]</code>.","In the <code>.claude/config.json</code> file, define a new agent profile that maps the Terraform guidelines to the <code>infrastructure/</code> path."],correct:0,explanation:"Correct. According to the documentation, an Anthropic \"Skill\" is the standard way to package instructions for specific tasks. The `SKILL.md` file's YAML frontmatter (with `name` and `description`) is always loaded, allowing Claude to determine if the skill is relevant. The full body of the skill is only loaded into context when needed, fulfilling the requirement for conditional activation and efficient token usage."},
{domain:"d1",task:"1.4 Implement multi-step workflows with enforcement and han",question:"A cryptocurrency exchange bot allows users to withdraw funds to external wallets. The system prompt explicitly requires the bot to call <code>verify_mfa</code> before <code>execute_withdrawal</code>. During an audit, it is discovered that the bot occasionally bypasses MFA when users claim they are in a life-threatening emergency. What is the most robust way to enforce deterministic compliance?",options:["Update the system prompt to explicitly forbid bypassing MFA even in emergency situations, using strong negative constraints and XML tags.","Implement a programmatic gate in the orchestration layer that validates the presence of a verified MFA token in the session state before allowing the <code>execute_withdrawal</code> tool to execute.","Increase the temperature of the model to 0.0 to ensure it behaves deterministically and strictly follows the prompt instructions regarding MFA.","Create a separate 'emergency handling' agent that is authorized to bypass MFA, and route all emergency requests to this agent to keep the main bot compliant."],correct:1,explanation:"The most robust way to ensure compliance is to move the enforcement logic from the prompt to the code (orchestration layer). By validating the presence of a verified MFA token in the session state before allowing the tool execution, the system creates a deterministic 'hard gate' that cannot be bypassed by the model's output alone."},
{domain:"d1",task:"1.6 Design task decomposition strategies for complex workfl",question:"An autonomous red-teaming agent is given a target IP address to perform a penetration test. The agent's next actions are completely unknown until the initial port scan returns results (e.g., finding an open FTP port requires different tools than finding an exposed web API). Which decomposition strategy is required for this agent?",options:["A fixed sequential pipeline of all known exploits.","Dynamic adaptive decomposition based on intermediate findings.","A cross-file integration pass of all historical scan logs.","First mapping the network structure, then using a fixed prompt chain."],correct:1,explanation:"Dynamic adaptive decomposition is the appropriate strategy for situations where intermediate findings determine the next subtask and tool choice. This allows the agent to inspect real-time scan results and then decide whether to pursue specific service workflows (like FTP or web APIs), ensuring flexibility and responsiveness to discovered conditions."},
{domain:"d1",task:"1.5 Apply Agent SDK hooks for tool call interception and da",question:"An IT operations agent diagnoses server health using multiple legacy APIs. The APIs return heterogeneous status codes: API 1 returns <code>0</code> for success, API 2 returns <code>200</code>, and API 3 returns <code>OK</code>. The model frequently misinterprets <code>0</code> as a failure state, leading to unnecessary escalation alerts. What is the most robust architectural solution?",options:["Modify the system prompt to include a comprehensive mapping table of all possible status codes for each legacy API.","Use a PreToolCall hook to intercept the request and route it through a middleware proxy that standardizes the API responses at the network level.","Instruct the model to call a <code>verify_status</code> tool immediately after receiving a <code>0</code> to confirm if it represents a success or failure.","Implement a PostToolUse hook that maps the heterogeneous status codes to a unified schema (e.g., <code>{\"status\": \"SUCCESS\"}</code>) before passing the tool result to the model."],correct:3,explanation:"Implementing a PostToolUse hook is the most robust solution. It allows for deterministic normalization of tool outputs before they reach the LLM. By mapping heterogeneous legacy codes into a unified schema (e.g., 'SUCCESS'), you remove ambiguity and ensure the model consistently receives clear, standardized information, following best practices for agentic orchestration."},
{domain:"d3",task:"3.6 Integrate Claude Code into CI/CD pipelines",question:"An enterprise maintains a monorepo containing multiple microservices. Each microservice uses entirely different testing frameworks and fixture conventions. A single CI pipeline runs Claude Code to generate unit tests for whichever microservice was modified in a given pull request. Currently, Claude Code frequently hallucinates fixtures from Service A when generating tests for Service B.\n\nHow should the architect resolve this context confusion in the CI pipeline without writing complex dynamic prompt injection scripts?",options:["Create a global CLAUDE.md file at the root of the monorepo that uses conditional markdown blocks to specify which fixtures belong to which service.","Place a distinct CLAUDE.md file in the root directory of each microservice detailing its specific testing standards; Claude Code will automatically resolve context based on the nearest CLAUDE.md in the directory tree.","Use the --context-dir flag in the CI pipeline to point Claude Code to a centralized repository of JSON-formatted fixture definitions.","Configure the CI pipeline to pass the --isolate-context flag, which forces Claude Code to ignore all files outside the immediate working directory."],correct:1,explanation:"Correct. Claude Code is designed to respect directory-level instructions. By placing a CLAUDE.md file in the root of each microservice, the tool automatically identifies the nearest context-relevant instructions. This allows each service to define its own testing frameworks, linting rules, and fixture conventions without leaking that context into other services in the monorepo."},
{domain:"d2",task:"2.4 Integrate MCP servers into Claude Code and agent workfl",question:"A retail enterprise is building an agentic workflow to manage their e-commerce platform. The workflow requires interacting with Shopify (for storefront data), Zendesk (for customer tickets), and a highly proprietary, 20-year-old AS/400 mainframe (for warehouse inventory). The engineering director wants to minimize development time while ensuring robust functionality. Which MCP server strategy should the architect recommend?",options:["Develop custom MCP servers for all three systems to ensure a unified logging and error-handling framework across the entire workflow.","Utilize existing community MCP servers for Shopify and Zendesk, and allocate engineering resources to build a custom MCP server exclusively for the proprietary AS/400 mainframe.","Build a single, monolithic custom MCP server that wraps the APIs of Shopify, Zendesk, and the AS/400 mainframe to simplify tool discovery for the agent.","Use community MCP servers for all three systems, utilizing a generic REST API community server to interface with the AS/400 mainframe."],correct:1,explanation:"This is the most efficient strategy. Leveraging existing community MCP servers for common SaaS platforms like Shopify and Zendesk reduces implementation effort and speeds up delivery. This allows the engineering team to focus their custom development efforts where they are truly needed: the proprietary legacy AS/400 mainframe, which lacks a pre-built integration."},
{domain:"d4",task:"4.6 Design multi-instance and multi-pass review architectur",question:"An automated code review assistant processes pull requests containing up to 20 files. Currently, all 20 files are concatenated into a single prompt for Claude to review. Developers report that Claude misses obvious local syntax errors in smaller files and fails to catch mismatched function signatures between files. Which architectural change will best resolve these issues?",options:["Switch to a larger context window model and increase the <code>max_tokens</code> to ensure Claude has enough output space to list all errors across the 20 files.","Split the review into two phases: parallel per-file prompts to identify local issues, followed by a single integration prompt containing the summaries and file interfaces to check cross-file data flow.","Implement a recursive summarization loop where Claude reviews file 1, appends the result to file 2, reviews file 2, and continues sequentially through all 20 files.","Prompt Claude to first output a JSON array of all file names, then iterate through them using tool calls to request the contents of each file one by one."],correct:1,explanation:"Correct. This two-phase architecture follows a 'divide and conquer' approach. Parallel per-file prompts provide maximum focused attention for catching local syntax errors, while the subsequent integration phase focuses specifically on high-level interfaces and summaries. This reduces context noise and ensures global consistency (like function signatures) is reviewed without implementation details cluttering the prompt."},
{domain:"d2",task:"2.4 Integrate MCP servers into Claude Code and agent workfl",question:"A development team has configured three shared MCP servers in their project's .mcp.json: a GitHub server, a CI/CD server, and a Cloud Monitoring server. A developer initiates a complex prompt asking Claude Code to 'Check the latest failing CI build, find the corresponding commit in GitHub, and check if the monitoring server shows any CPU spikes at that time.' How does Claude Code manage the tools required for this request?",options:["Claude Code connects to the CI/CD server first, disconnects, connects to GitHub, disconnects, and finally connects to the Monitoring server.","Claude Code requires the developer to specify which server to activate using a --server flag before executing the prompt.","Claude Code discovers tools from all three configured servers at connection time and makes them available simultaneously to the agent to orchestrate the workflow.","Claude Code merges the three servers into a temporary local SQLite database and queries the tools using a unified GraphQL interface."],correct:2,explanation:"This is correct. Upon initialization, Claude Code reads the configuration, connects to all defined MCP servers, and performs tool discovery. All discovered tools are presented to the agent's tool-calling interface simultaneously, allowing Claude to orchestrate complex, multi-step workflows that span across multiple disparate services."},
{domain:"d5",task:"5.2 Design effective escalation and ambiguity resolution pa",question:"A user connects to an enterprise security bot and immediately types: 'Human agent now. I need to initiate a full data wipe of our primary database cluster.' The bot has a highly secure <code>initiate_data_wipe</code> tool that requires multi-factor authentication (MFA) and manager approval workflows. According to best practices for escalation triggers, how should the system prompt dictate the bot's behavior?",options:["Trigger the MFA workflow to verify the user's identity first, and then transfer the verified session to a human agent.","Immediately trigger the human escalation tool without attempting to investigate the request or initiate the MFA workflow.","Acknowledge the request for a human, but first offer to resolve the issue by explaining that the automated tool can perform the wipe faster.","Ask the user to provide the specific cluster ID and reason for the wipe so the human agent has the necessary context upon transfer."],correct:1,explanation:"This is the correct approach. Best practices for escalation design state that once a user explicitly requests a human agent, the bot should honor that request immediately and without friction. In high-risk security scenarios, the safest and most responsive pattern is to transfer the context to a human who can then oversee the secure verification and approval protocols."},
{domain:"d3",task:"3.6 Integrate Claude Code into CI/CD pipelines",question:"An automated PR review system uses Claude Code to generate structured JSON reports of code defects. When a developer updates the PR, the system must verify if the previously identified defects were fixed. The pipeline script successfully passes the previous JSON report into the new Claude Code prompt. However, the new output is returned as unstructured conversational text, breaking the downstream JSON parser.\n\nWhat is the most likely reason for this failure?",options:["The model automatically switches to conversational mode when it detects JSON in the input context to explain the data.","The pipeline script omitted the --output-format json and --json-schema flags in the subsequent run; these flags must be explicitly provided in every CI invocation to enforce structured output.","The previous JSON report exceeded the context window limit, causing the model to fallback to a default unstructured response format.","The --json-schema flag can only be used for code generation tasks, not for verification or review tasks."],correct:1,explanation:"In automated CI/CD environments, Claude Code's output format is not 'sticky' between separate process invocations. To ensure the model returns machine-readable JSON rather than natural language, the --output-format json and relevant schema flags must be explicitly passed in every execution of the CLI command."},
{domain:"d4",task:"4.3 Enforce structured output using tool use and JSON schem",question:"A multi-agent system features a \"Researcher\" agent equipped with <code>search_database</code>, <code>calculate_metrics</code>, and <code>format_report</code> tools. For a specific user query, you know the agent must start by querying the database, but during testing, the agent sometimes tries to calculate metrics first based on its internal knowledge. How can you guarantee the agent calls the database tool first without removing the other tools from its context?",options:["Set <code>tool_choice: {\"type\": \"tool\", \"name\": \"search_database\"}</code> for the initial API request.","Set <code>tool_choice: {\"type\": \"any\"}</code> and rely on the system prompt to guide the order of operations.","Add a system prompt stating \"CRITICAL: You must use search_database first.\"","Set <code>tool_choice: {\"type\": \"auto\"}</code> and use a temperature of 0.0 to ensure deterministic tool selection."],correct:0,explanation:"Correct. In the Claude API, setting `tool_choice` to a specific tool name via the `tool` type forces the model to use that specific tool in its response. This is the only programmatic way to guarantee a specific tool is called first, regardless of the model's internal reasoning or planning."},
{domain:"d3",task:"3.4 Determine when to use plan mode vs direct execution",question:"You are starting a multi-phase task to internationalize a React application. The very first step is to scan through hundreds of component files to identify all hardcoded string literals. You know this will generate a massive list of strings that could overwhelm the AI's memory for the subsequent implementation phases. Which feature should you use to perform this search?",options:["Plan mode, to ensure the string extraction strategy is architecturally sound.","Direct execution, to iteratively find and replace strings one file at a time.","Plan mode combined with Direct execution to simultaneously search and extract the strings.","The Explore subagent, to isolate the verbose discovery phase and prevent context window exhaustion."],correct:3,explanation:"The Explore subagent is specifically designed for broad discovery and inspection tasks. It operates in a separate context to gather information and summarize findings, effectively isolating the verbose discovery phase. This prevents the primary conversation window from being overwhelmed by large amounts of search data, preserving memory for the actual implementation and refactoring phases."},
{domain:"d2",task:"2.5 Select and apply built-in tools (Read, Write, Edit, Bas",question:"You need to change a <code>setTimeout(..., 1000)</code> call to <code>2000</code> inside a specific <code>fetchData</code> function within a 2,000-line <code>api.ts</code> file. There are dozens of identical <code>setTimeout(..., 1000)</code> calls scattered throughout the file. You want to use the Edit tool to minimize token usage and avoid reading the entire file. How should you construct the Edit tool request to ensure success on the first try?",options:["Provide only the <code>setTimeout</code> line as the anchor text and specify the exact line number in the Edit tool parameters.","Include the unique <code>function fetchData()</code> signature and the intervening lines in the anchor text to create a globally unique match block.","Use the Read tool first to load the file into context, then use the Edit tool with a standard 2-line anchor text.","Use Grep to find the <code>setTimeout</code> call, then use the Write tool to overwrite just that specific function block."],correct:1,explanation:"This is the correct approach. The Edit tool requires the `old_str` to be unique within the file to execute the replacement successfully. By including surrounding context like the `fetchData()` function signature, you create a unique block that ensures the tool targets the specific occurrence you intend to change without needing to load the entire file into context."},
{domain:"d4",task:"4.2 Apply few-shot prompting to improve output consistency",question:"A DevOps automation tool uses Claude to translate natural language deployment requirements into a strict YAML configuration file for a CI/CD pipeline. Despite detailed instructions demanding \"only valid YAML, ready for execution,\" Claude frequently wraps the output in Markdown code blocks and includes helpful explanatory text before and after the YAML, which breaks the downstream parser. What is the most effective technique to enforce the strictly actionable, YAML-only format?",options:["Provide few-shot examples that demonstrate the exact desired output format, showing only the raw YAML configuration without any surrounding descriptive text or Markdown.","Append a post-processing instruction asking Claude to review its own output and remove any Markdown formatting or explanations.","Use the stop_sequences parameter to halt generation as soon as Claude attempts to output a newline character followed by explanatory text.","Increase the presence_penalty to discourage the model from generating common conversational words found in explanations."],correct:0,explanation:"Few-shot examples are the most robust method for enforcing strict output schemas and formats. By demonstrating multiple instances of raw YAML without Markdown fences or commentary, the model's pattern-matching capabilities are leveraged to produce output that aligns precisely with the provided structure, significantly reducing unwanted preamble or postscript text."},
{domain:"d1",task:"1.5 Apply Agent SDK hooks for tool call interception and da",question:"A travel application uses a Claude-powered agent to call a <code>search_flights</code> tool. The external flight API requires the <code>date</code> parameter to be in a strict <code>YYYY-MM-DD</code> format and will fail otherwise. Despite prompt engineering, the model occasionally provides natural language dates (e.g., 'tomorrow'). Which of the following is the most robust and recommended method to ensure the model provides a correctly formatted date string to the tool?",options:["In the tool's <code>input_schema</code>, define the <code>date</code> parameter with a <code>type</code> of <code>string</code> and a <code>pattern</code> property containing a regex for <code>YYYY-MM-DD</code>. To guarantee compliance, also set <code>strict: true</code> in the tool definition.","In the application code, implement a validation function that checks the date format *after* receiving the tool call from Claude but *before* calling the external API. If validation fails, return an error message to the model.","Refine the system prompt with a stronger negative constraint, such as: 'CRITICAL: The <code>date</code> parameter must always be in <code>YYYY-MM-DD</code> format. Failure to comply will result in an error.'","Implement a <code>PostToolUse</code> hook that catches the API crash, parses the natural language date into the correct format, and automatically re-triggers the <code>search_flights</code> tool call."],correct:0,explanation:"Correct. According to Anthropic's documentation, setting `strict: true` on a tool definition guarantees that Claude's tool inputs will match the provided JSON Schema. This is achieved by constraining the model's token sampling to only generate schema-valid outputs. This is the most robust and recommended method as it prevents the invalid tool call from ever being generated, ensuring reliability without extra application logic."},
{domain:"d2",task:"2.4 Integrate MCP servers into Claude Code and agent workfl",question:"A QA engineering team has developed a custom visual_diff_analyzer MCP tool that uses a specialized computer vision model to compare UI screenshots. However, when developers prompt Claude Code to 'compare the staging UI to production', the agent consistently attempts to use built-in bash tools to run diff or cmp on the raw image binaries, which fails. What is the most architecturally appropriate way to correct this behavior?",options:["Remove the built-in bash tools from the agent's execution environment by applying a strict restriction policy in the .mcp.json file.","Enhance the visual_diff_analyzer tool description to explicitly detail its computer vision capabilities, expected image inputs, and why it must be used instead of standard binary comparison tools.","Rename the custom tool to diff to intentionally overwrite the built-in tool in the agent's tool registry.","Configure the custom tool as an MCP resource instead of a tool, forcing the agent to read the visual diff passively rather than executing a comparison."],correct:1,explanation:"Claude selects tools based on the semantic descriptions provided in the MCP server metadata. By enhancing the tool's description to include specific use cases (like UI comparison), input requirements (image formats), and its superiority over standard binary comparison utilities for this context, you provide the necessary information for Claude's reasoning engine to route the task to the specialized tool instead of generic shell commands."},
{domain:"d1",task:"1.6 Design task decomposition strategies for complex workfl",question:"An architecture review agent is analyzing a 100-page technical specification. It must verify security compliance, performance requirements, and data model consistency. Currently, the agent is instructed to check all three domains in a single pass, but it frequently misses critical security flaws due to attention dilution. How should the architect restructure the workflow?",options:["Implement a fixed prompt chain that performs three separate sequential passes over the document, one dedicated to each specific domain.","Use dynamic decomposition to allow the agent to decide which pages are most relevant for security, performance, and data.","Compress the document using a summarization agent before performing the three-domain review.","Increase the context window size and use a more complex system prompt with strict XML formatting."],correct:0,explanation:"Correct. Implementing a fixed prompt chain with separate passes ensures that the agent focuses on one domain at a time (e.g., security), effectively eliminating attention dilution. This structured approach enhances thoroughness and recall, ensuring that every page is evaluated against a specific set of criteria without the interference of unrelated objectives."},
{domain:"d1",task:"1.6 Design task decomposition strategies for complex workfl",question:"An architect is designing an agentic system to add comprehensive unit tests to a massive, undocumented legacy codebase. The task is highly open-ended, and the dependencies between modules are unknown. What is the optimal decomposition strategy for this workflow?",options:["Implement a fixed prompt chain that iterates through the repository alphabetically, writing tests for each file.","First map the codebase structure, identify high-impact areas, and create a prioritized plan that adapts as dependencies are discovered.","Use a single prompt with the maximum context window to read all files and output all tests in one pass.","Deploy parallel agents to write tests for all files simultaneously, then use a map-reduce chain to merge them."],correct:1,explanation:"This strategy uses a discovery-driven approach suitable for open-ended tasks with unknown dependencies. By mapping the codebase and identifying high-impact areas first, the system can prioritize critical modules and refine its strategy as it learns more about the codebase's interdependencies, allowing for efficient and accurate decomposition of a massive task."},
{domain:"d5",task:"5.5 Design human review workflows and confidence calibratio",question:"A legal discovery tool extracts 'Key Dates' from complex email threads. The model frequently outputs dates with very high raw confidence (>0.98). However, human reviewers note that the model occasionally hallucinates dates that are logically inferred but not explicitly stated in the text. How should the architect use a labeled validation set to calibrate the review workflow and mitigate this overconfidence?",options:["Lower the global review threshold to 0.80 so that a larger volume of documents is reviewed, regardless of the model's high confidence scores.","Use the validation set to map the model's raw confidence scores to empirical accuracy (expected calibration error), and adjust the routing threshold based on the calibrated probability rather than the raw score.","Prompt the model to output a secondary 'Hallucination Risk' score alongside the date, and route to human review if this secondary score is high.","Filter out any extracted dates that fall outside the chronological bounds of the email thread's metadata before applying the confidence threshold."],correct:1,explanation:"Correct. A labeled validation set allows the architect to perform calibration (such as measuring Expected Calibration Error) to compare raw confidence scores against actual empirical correctness. By mapping raw scores to calibrated probabilities, the architect can set a review threshold based on the actual likelihood of error rather than misleadingly high raw scores."},

];
